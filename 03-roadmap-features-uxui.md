# แนวทางการขยายการพัฒนา, ฟีเจอร์แนะนำ และมาตรฐาน UX/UI
### GPO Xchange Portal

**จัดทำเมื่อ:** 30 กรกฎาคม 2569

เอกสารนี้ต่อยอดจากภาพรวมระบบและผลประเมินความพร้อม โดยเสนอแนวทางพัฒนาต่อ แบ่งเป็น 3 ส่วน: ฟีเจอร์ใหม่ที่น่าสนใจ, มาตรฐาน UX/UI ให้ทำงานลื่นไหลตามมาตรฐานสากล และลำดับความสำคัญของ roadmap โดยทุกข้อเสนอคำนึงถึงหลักการที่ระบบใช้อยู่แล้ว คือ **ออกแบบความปลอดภัยตาม workflow (security-by-design)** และ **พร้อมสำหรับ self-host เสมอ** (ไม่ผูกกับบริการเฉพาะ vendor โดยไม่จำเป็น)

---

## 1. ฟีเจอร์แนะนำเพิ่มเติม

### 1.1 การแจ้งเตือนและการสื่อสาร

- **ระบบแจ้งเตือนหลายช่องทาง (Notification Center)** — ปัจจุบันแจ้งเตือนผ่านอีเมลเป็นหลัก ควรเพิ่ม in-app notification bell (ใช้ Supabase Realtime subscribe ตาราง `status_logs`/`request_pings` ที่มีอยู่แล้ว) และพิจารณา LINE Notify หรือ LINE OA เนื่องจากบริบทโรงพยาบาลไทยใช้ LINE เป็นช่องทางหลักในการทำงาน
- **SLA tracking พร้อม auto-escalation** — ต่อยอดจากตาราง `request_pings` ที่มีอยู่แล้ว (ปุ่มเร่งงาน) ให้ระบบคำนวณเวลาที่คำขอค้างในแต่ละสถานะ แล้วแจ้งเตือน manager อัตโนมัติเมื่อเกิน threshold ที่กำหนด (เช่น ค้างเกิน 3 วันทำการ) แทนที่จะรอให้ลูกค้ากดปุ่มเร่งงานเอง
- **สรุปรายวันอัตโนมัติให้ผู้จัดการ** — ส่งอีเมล/LINE สรุปคำขอค้าง สถิติวันนี้ ให้ manager ทุกเช้า ใช้ query ที่มีอยู่แล้วใน `lib/manager-stats.ts` เป็นฐาน

### 1.2 การจัดการเอกสารและข้อมูลสินค้า

- **Barcode/QR scanning สำหรับรายการยา** — ฝั่ง WH เวลาตรวจรับสินค้า มักมี lot number/expiry date บนบรรจุภัณฑ์อยู่แล้ว การสแกน QR/barcode ด้วยกล้องมือถือ (ผ่าน `getUserMedia` API) แทนการพิมพ์ ลดความผิดพลาดจากการคีย์ข้อมูลและเร็วกว่า
- **Document versioning** — เอกสาร PDF ที่สร้างในแต่ละขั้น (ยืนยันคำร้อง, ยืนยันการลงทะเบียน) ควรเก็บ version history แทนการ overwrite เพื่อรองรับกรณีต้องตรวจสอบย้อนหลังว่าเอกสารฉบับที่ลูกค้าเซ็นจริงคือฉบับใด
- **Export ข้อมูลสำหรับหน่วยงานกำกับดูแล** — เพิ่มปุ่ม export CSV/Excel ของ `status_logs`+`access_logs` ในช่วงเวลาที่กำหนด สำหรับกรณีต้อง audit ตาม PDPA หรือหน่วยงานภายนอกขอตรวจสอบ

### 1.3 การเชื่อมต่อระบบภายนอก

- **Integration กับระบบ ERP/คลังของโรงพยาบาล** — เปิด webhook หรือ REST endpoint ให้โรงพยาบาลที่มีระบบเภสัชกรรมของตัวเองยิงคำขอคืน/แลกเปลี่ยนเข้ามาโดยอัตโนมัติแทนการกรอกฟอร์มมือ (ใช้ API key ต่อโรงพยาบาล คู่กับ rate-limit ที่มีโครงสร้างอยู่แล้ว)
- **SSO สำหรับพนักงานโรงพยาบาล** — ถ้าในอนาคตมีลูกค้าจำนวนมาก การรองรับ SAML/OIDC ผ่านระบบ identity ของโรงพยาบาลจะลดภาระการจัดการบัญชีทั้งสองฝั่ง (ควรทำแบบ opt-in ต่อองค์กร ไม่บังคับทุกราย เพราะเพิ่มความซับซ้อน)

### 1.4 การวิเคราะห์ข้อมูล

- **Forecast แนวโน้มการคืน/แลกเปลี่ยนตามฤดูกาล** — ต่อยอด Recharts ที่มีอยู่แล้วด้วย time-series ง่ายๆ (เช่น moving average) ให้ manager วางแผนกำลังคนคลัง/โลจิสติกส์ล่วงหน้าได้
- **Dashboard เปรียบเทียบประสิทธิภาพระหว่างแผนก** — เวลาเฉลี่ยต่อขั้นตอนต่อแผนก เพื่อหา bottleneck ที่แท้จริงในกระบวนการ

### 1.5 ฟีเจอร์ที่ควร "ชะลอไว้ก่อน" โดยเจตนา

- **การให้ chatbot ลูกค้าเช็คสถานะคำร้องได้เอง** — มี comment เตือนไว้ในโค้ดแล้วว่าต้องออกแบบเรื่อง auth-scoping ให้รอบคอบก่อน เพราะ endpoint บอทลูกค้าปัจจุบันไม่มีการเช็ค session เหมือนบอทพนักงาน หากเปิดให้เช็คสถานะได้โดยไม่ผูก session อาจเปิดช่องให้ enumerate ref_id ผ่านการสนทนาได้ ควรแก้เรื่อง auth ให้เสร็จก่อนต่อยอดฟีเจอร์นี้

---

## 2. มาตรฐาน UX/UI ให้ทำงานลื่นไหลและเป็นไปตามมาตรฐานสากล

### 2.1 หลักการออกแบบฟอร์ม (Form UX)

ระบบใช้ `react-hook-form` + `zod` + shadcn/ui อยู่แล้วซึ่งเป็น pattern ที่ดี ควรคงมาตรฐานต่อไปนี้ในทุกฟอร์มใหม่:

- ใช้ไอคอนจาก **Lucide** (`lucide-react` มีอยู่ในโปรเจกต์แล้ว) ประกอบทุก field ที่มีความหมายชัดเจน (เช่น ไอคอนซองจดหมายคู่กับ email, ไอคอนโทรศัพท์คู่กับเบอร์ติดต่อ) เพื่อช่วยให้ผู้ใช้สแกนฟอร์มด้วยสายตาได้เร็วขึ้น — เป็นแนวทางเดียวกับที่ระบบเลือกใช้อยู่แล้ว ควรคงรูปแบบนี้ให้สม่ำเสมอทุกหน้า ไม่ใช่แค่บางฟอร์ม
- Validation แบบ **inline, real-time** (แสดง error ทันทีที่ blur ไม่ใช่รอ submit) พร้อมข้อความภาษาไทยที่บอกวิธีแก้ ไม่ใช่แค่บอกว่าผิด
- ฟอร์มยาวหลายขั้น (เช่น ฟอร์มยื่นคำขอ) ควรมี progress indicator ชัดเจนว่าอยู่ขั้นไหนจากกี่ขั้น และอนุญาตให้ย้อนกลับแก้ไขได้โดยไม่เสียข้อมูลที่กรอกไปแล้ว
- ปุ่ม submit ต้อง disable ทันทีหลังกด (ป้องกัน double-submit) — ระบบเคยมีบั๊กนี้ในปุ่ม Google login มาก่อนและแก้แล้ว ควรตรวจสอบว่าทุกปุ่ม action สำคัญ (อนุมัติ, ปฏิเสธ, ยืนยัน) มี guard นี้ครบทุกจุด

### 2.2 มาตรฐาน Accessibility (WCAG 2.1 AA)

- Contrast ratio ของสีตัวอักษร/พื้นหลังต้องผ่านเกณฑ์ AA (4.5:1 สำหรับตัวอักษรปกติ) โดยเฉพาะสีสถานะ (เขียว=อนุมัติ, แดง=ปฏิเสธ) ต้องแยกแยะได้แม้สำหรับผู้ใช้ตาบอดสี — แนะนำเสริม icon/label ควบคู่กับสีเสมอ ไม่ใช้สีสื่อความหมายเพียงอย่างเดียว
- ทุก interactive element ต้อง keyboard-navigable และมี `aria-label` ที่เหมาะสม โดยเฉพาะ dropdown เหตุผลปฏิเสธและปุ่ม action ในตาราง dashboard ของพนักงาน
- ฟอนต์ไทย Sarabun ที่ใช้ใน PDF ควรใช้เป็นฟอนต์หลักในหน้าเว็บด้วยเพื่อความสอดคล้อง (ปัจจุบันตรวจสอบว่าใช้ font ใดในหน้าเว็บ หากต่างจาก PDF ควรรวมให้เป็นมาตรฐานเดียว)

### 2.3 Responsive & Mobile-first

- พนักงานคลังสินค้า (WH) มักใช้งานผ่านแท็บเล็ต/มือถือขณะยืนตรวจของหน้าคลัง ไม่ใช่นั่งหน้าคอมพิวเตอร์ — หน้าจอ WH dashboard ควรออกแบบแบบ **mobile-first** เป็นพิเศษ ปุ่มขนาดใหญ่พอสำหรับกดด้วยนิ้วขณะถือสินค้า (ขนาดขั้นต่ำ 44×44px ตามแนวทาง Apple HIG/Material Design)
- ทดสอบ responsive ตั้งแต่ 320px (มือถือจอเล็กสุด) ถึง desktop ทุกหน้าใหม่ — ระบบมีตัวอย่างที่ทำถูกต้องมาแล้วในหน้ายืนยันการลงทะเบียน ควรใช้เป็นมาตรฐานอ้างอิง

### 2.4 Feedback และ Error Handling

- ทุก action ที่เรียก server action ต้องมี loading state ที่มองเห็นได้ (skeleton หรือ spinner) ไม่ปล่อยให้หน้าจอดูค้างเฉยๆ
- Error message ต้องเจาะจงและนำไปแก้ปัญหาได้จริง (ระบบเคยมีปัญหานี้ที่ `withCSRAuth` กลืน error message เฉพาะเจาะจงจนเหลือแค่ "เกิดข้อผิดพลาดในการเชื่อมต่อ" ซึ่งแก้แล้ว — ควรคงมาตรฐานนี้ในโค้ดใหม่ทุกจุด)
- Success state ต้องนำทางผู้ใช้ไปขั้นต่อไปเสมอ ไม่ใช่แค่ปิด modal แล้วค้างอยู่หน้าเดิม (ระบบเคยมีปัญหานี้ในหน้าลงทะเบียนลูกค้าและแก้แล้วด้วย modal ยืนยัน + auto-redirect)

### 2.5 Design System และความสม่ำเสมอ

- คง shadcn/ui + Tailwind เป็นฐาน design system เดียวทั้งระบบ หลีกเลี่ยงการผสม UI library อื่นเข้ามาเพิ่มโดยไม่จำเป็น เพื่อลด bundle size และความไม่สอดคล้องของหน้าตา
- รวม component ที่ใช้ pattern ซ้ำ (เช่น dropdown เลือกเหตุผลปฏิเสธ ที่ตอนนี้มี `ReasonSelectFields` แล้ว) ให้เป็น shared component เดียว ไม่ copy-paste ซ้ำตามแผนกต่างๆ
- Dark mode: โปรเจกต์มี `next-themes` ติดตั้งอยู่แล้ว แนะนำเปิดใช้งานให้ครบทุกหน้า (ปัจจุบันอาจใช้เฉพาะบางส่วน) เนื่องจากพนักงานคลัง/โลจิสติกส์อาจทำงานในที่แสงจ้าหรือกลางคืน

---

## 3. ลำดับความสำคัญของ Roadmap (แนะนำ)

| ลำดับ | รายการ | เหตุผล |
|---|---|---|
| **เร่งด่วน** | เปิด leaked-password protection, ปิด gap ตาม UAT ก่อน go-live | เป็นเงื่อนไขก่อน production ตามเอกสาร valuation |
| **ระยะสั้น (1–2 เดือน)** | SLA tracking + แจ้งเตือนอัตโนมัติ, notification center ในแอป, pagination หน้าสถิติ | เพิ่มคุณค่าให้ผู้ใช้ทันทีโดยต่อยอดจากโครงสร้างที่มีอยู่แล้ว (`request_pings`, Realtime) |
| **ระยะกลาง (3–6 เดือน)** | Barcode scanning ฝั่งคลัง, export ข้อมูลสำหรับ audit, mobile-first ปรับปรุง WH dashboard | ยกระดับประสิทธิภาพการทำงานจริงหน้างาน |
| **ระยะยาว (6 เดือนขึ้นไป)** | Integration กับ ERP โรงพยาบาล, SSO, forecast วิเคราะห์เชิงลึก | ต้องมีจำนวนลูกค้า/ข้อมูลมากพอจะคุ้มค่าการลงทุน ควรทำหลังพิสูจน์ product-market fit จาก pilot แล้ว |
