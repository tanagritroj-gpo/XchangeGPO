# ภาพรวมระบบ GPO Xchange Portal
### เอกสารแนะนำระบบฉบับสมบูรณ์ — สำหรับนำเสนอฝ่ายบริหาร

**จัดทำเมื่อ:** 30 กรกฎาคม 2569 · **ปรับปรุงล่าสุด (ฉบับสมบูรณ์):** 2 สิงหาคม 2569
**อ้างอิงจาก:** อ่านโค้ดทั้งหมดใน repository `newgpo-exchange` (branch `RLS_supabase`, 165 commits, ~20,600 บรรทัด TypeScript ใน 131 ไฟล์), live schema/RLS/advisor จริงบน Supabase project `tanagritroj-gpo` (`qgztveswduffskucqppt`, Postgres 17.6, ap-southeast-1) ตรวจสอบสดวันที่ปรับปรุง, migration history 35 ไฟล์ใน `supabase/migrations/`, และเอกสารประเมินระบบภายใน `SYSTEM_ASSESSMENT.md`

---

## สารบัญ

1. [สรุปสำหรับผู้บริหาร (Executive Summary)](#1-สรุปสำหรับผู้บริหาร-executive-summary)
2. [ระบบนี้คืออะไรและแก้ปัญหาอะไร](#2-ระบบนี้คืออะไรและแก้ปัญหาอะไร)
3. [ผู้ใช้งานและบทบาท (Actors) — ครบ 6 บทบาท](#3-ผู้ใช้งานและบทบาท-actors--ครบ-6-บทบาท)
4. [Workflow หลักของระบบ (State Machine)](#4-workflow-หลักของระบบ-state-machine)
5. [รายการฟีเจอร์ทั้งหมด แยกตามกลุ่มผู้ใช้](#5-รายการฟีเจอร์ทั้งหมด-แยกตามกลุ่มผู้ใช้)
6. [สถาปัตยกรรมเทคนิค](#6-สถาปัตยกรรมเทคนิค)
7. [โครงสร้างข้อมูลหลัก (Data Model)](#7-โครงสร้างข้อมูลหลัก-data-model)
8. [สถาปัตยกรรมความปลอดภัย](#8-สถาปัตยกรรมความปลอดภัย)
9. [จุดแข็งของระบบ](#9-จุดแข็งของระบบ)
10. [จุดที่ควรทราบ (ช่องว่าง/หนี้เทคนิคที่ยังไม่กระทบการใช้งาน)](#10-จุดที่ควรทราบ-ช่องว่างหนี้เทคนิคที่ยังไม่กระทบการใช้งาน)
11. [สถิติโครงการและปริมาณข้อมูลปัจจุบัน](#11-สถิติโครงการและปริมาณข้อมูลปัจจุบัน)
12. [ประเมินศักยภาพเชิงธุรกิจโดยย่อ](#12-ประเมินศักยภาพเชิงธุรกิจโดยย่อ)
13. [การเปลี่ยนแปลงล่าสุด (Changelog)](#13-การเปลี่ยนแปลงล่าสุด-changelog)

---

## 1. สรุปสำหรับผู้บริหาร (Executive Summary)

**GPO Xchange Portal** คือระบบดิจิทัลที่แปลงกระบวนการ **รับคืน/แลกเปลี่ยนยาและเวชภัณฑ์แบบ B2B** ระหว่างหน่วยงาน (ชื่อโดเมนและโค้ดสื่อถึงองค์การเภสัชกรรมหรือหน่วยงานลักษณะเดียวกัน) กับโรงพยาบาล/คลินิก/ร้านยาคู่ค้า จากกระบวนการกระดาษที่ต้องผ่านมือหลายแผนก ให้กลายเป็นระบบที่ติดตามสถานะได้แบบเรียลไทม์ มี audit trail ครบทุกขั้นตอน และลดภาระงานประสานงานข้ามแผนก

**ระบบครอบคลุมผู้ใช้งาน 6 บทบาท** (ลูกค้า, CSR, คลังสินค้า, โลจิสติกส์, ผู้จัดการ, และ **ฝ่ายขาย/Sale ที่เพิ่งเพิ่มเข้ามาล่าสุด**) พร้อม workflow ที่มีสถานะครบ 10 สถานะ, ระบบ chatbot AI 2 ตัว (ตอบคำถามลูกค้า + วิเคราะห์ข้อมูลให้ผู้บริหาร), ระบบสร้างเอกสาร PDF อัตโนมัติ 2 ประเภท (ยืนยันคำร้อง + ยืนยันการลงทะเบียน), และระบบ export รายงาน Excel สำหรับ CSR/ผู้จัดการ

**สถานะโครงการ:** พัฒนาโดยทีม/ผู้พัฒนาขนาดเล็ก เริ่ม 12 มิถุนายน 2569 ถึงปัจจุบันรวม 165 commits ในเวลาไม่ถึง 2 เดือน — ความเร็วในการพัฒนาสูงมากเมื่อเทียบกับขอบเขตงาน (6 บทบาทผู้ใช้, ตาราง 15 ตาราง, RPC function หลายสิบตัว, chatbot 2 ตัว) จุดที่เคยเป็นความเสี่ยงระดับวิกฤต (rate-limit ใช้งานไม่ได้จริง, RLS policy ขาดหายใน 9 ตาราง, ระบบ auth ปนกันสองระบบ, ไม่มี automated test) **ได้รับการแก้ไขครบทั้งหมดแล้ว** ปัจจุบันมี CI (typecheck+test+build) รันอัตโนมัติทุก push, unit test 54 เคสครอบคลุม state machine หลัก, และ Supabase security advisor เหลือคำเตือนระดับต่ำเพียง 1 รายการ

**ข้อสรุปสำหรับผู้บริหาร:** ระบบอยู่ในสถานะ **พร้อมสำหรับทดสอบ UAT และ pilot กับกลุ่มลูกค้าจำกัดก่อนขยายสู่การใช้งานจริงทั้งองค์กร** รายละเอียดคะแนนประเมินความพร้อมและแผนทดสอบอยู่ในเอกสาร `02-valuation-uat.md`

---

## 2. ระบบนี้คืออะไรและแก้ปัญหาอะไร

### ปัญหาเดิม (Pain Point)

กระบวนการคืน/แลกเปลี่ยนยาแบบเดิมต้องผ่านหลายแผนกต่อเนื่องกัน:

```
ลูกค้า (โรงพยาบาล) → CSR (รับเรื่อง/อนุมัติ) → โลจิสติกส์ (ขนส่ง) → คลังสินค้า (ตรวจรับ) → CSR (ปิดงาน)
```

ด้วยเอกสารกระดาษและการสื่อสารแยกส่วน (โทรศัพท์/อีเมล/ไลน์) ทำให้เกิดปัญหาซ้ำๆ: **"งานหาย"** (ไม่รู้ว่าเอกสารอยู่ขั้นไหน), **"ตามสถานะไม่ได้"** (ลูกค้าต้องโทรถามเป็นระยะ), และ **"ไม่มีหลักฐานย้อนหลัง"** ว่าใครอนุมัติ/ปฏิเสธอะไรตอนไหนด้วยเหตุผลอะไร — ปัญหาสุดท้ายนี้มีน้ำหนักเป็นพิเศษเพราะเกี่ยวข้องกับยาและเวชภัณฑ์ที่ต้องตรวจสอบย้อนหลังได้ตามข้อกำหนดกำกับดูแล

### ทางแก้ที่ระบบนี้ให้

ระบบ digitize ทั้งกระบวนการเป็น workflow เดียวที่ทุกแผนกเห็นสถานะร่วมกันแบบเรียลไทม์ พร้อมบันทึก audit trail ทุกจุดเปลี่ยนสถานะ (`status_logs`), ทุกการแก้ไขข้อมูลย้อนหลัง (`data_correction_logs`), และทุกการเข้าถึงเอกสาร (`access_logs` — รองรับข้อกำหนด PDPA) ลูกค้าติดตามสถานะได้เองผ่านหน้าเว็บสาธารณะโดยไม่ต้องโทรถาม และมี chatbot ตอบคำถามนโยบายเบื้องต้นแทนการรอ CSR

---

## 3. ผู้ใช้งานและบทบาท (Actors) — ครบ 6 บทบาท

| บทบาท | เข้าถึงผ่าน | สิทธิ์การยืนยันตัวตน | หน้าที่หลัก |
|---|---|---|---|
| **ลูกค้า (โรงพยาบาล/คลินิก/ร้านยา)** | `/` → `/welcome` | OTP ทางอีเมล หรือ Google OAuth (ผูกกับอีเมลที่อนุมัติแล้วเท่านั้น) | ยื่นคำขอคืน/แลกสินค้าพร้อมเซ็นชื่อดิจิทัล, ติดตามสถานะ, ดูประวัติของตัวเองและของทั้งหน่วยงาน, ดาวน์โหลดเอกสาร PDF, คุยกับ chatbot ตอบคำถามนโยบาย |
| **CSR (Customer Service)** | `/admin/csr` | username/password (bcrypt) + ต้องผ่านผู้จัดการอนุมัติก่อน | อนุมัติ/ปฏิเสธคำขอสมัครสมาชิกใหม่ (พร้อมกำหนดรหัสลูกค้า), กรอกฟอร์มแทนลูกค้า (walk-in/โทรศัพท์), ตรวจสอบ/อนุมัติ/ปฏิเสธคำขอและรายการยาแต่ละชิ้น, แท็ก compliance, ดู Report Center และ export รายงาน Excel |
| **คลังสินค้า (Warehouse)** | `/admin/wh/dashboard` | username/password (bcrypt) | ตรวจรับสินค้า 2 ขั้นตอน: ตรวจสภาพ (checked-in) → รับเข้าคลัง (receiving) ทำได้ทั้งรายชิ้นและเป็นชุด (batch) |
| **โลจิสติกส์ (Logistics)** | `/admin/logistics/dashboard` | username/password (bcrypt) | เปลี่ยนสถานะ อนุมัติแล้ว → อยู่ระหว่างขนส่ง → ถึงคลัง หรือปฏิเสธพร้อมระบุเหตุผล |
| **ผู้จัดการ (Manager)** | `/admin/manager/staff-approvals` | username/password (bcrypt) | อนุมัติบัญชีพนักงานใหม่ทุกแผนก, ดูกราฟ/สถิติสรุปผลการดำเนินงานทุกแผนก, คุยกับ chatbot วิเคราะห์ข้อมูลภายใน (query ข้อมูลสดผ่าน function calling) |
| **ฝ่ายขาย (Sale) — บทบาทใหม่ล่าสุด** | `/admin/sale` | username/password (bcrypt) + กำหนดขอบเขตดูแล (จังหวัด + ประเภทลูกค้า) ตอนสร้างบัญชี | ดูประวัติการแลกเปลี่ยนของลูกค้าเฉพาะในขอบเขตที่ตัวเองดูแล (ตามจังหวัด + ประเภทลูกค้าเอกชน/ภาครัฐ) — เป็นบทบาทแบบ **read-only ไม่มีสิทธิ์อนุมัติ/แก้ไข** |

### รายละเอียดบทบาทฝ่ายขาย (Sale) — เพิ่มเข้ามาล่าสุด 1–2 สิงหาคม 2569

บทบาทนี้ถูกออกแบบมาสำหรับพนักงานขายที่ดูแลลูกค้าเป็นรายภูมิภาค โดยขอบเขตการมองเห็นข้อมูลถูกกำหนดจาก 2 มิติร่วมกัน:

1. **จังหวัด** — เลือกจาก 7 จังหวัดภาคใต้ (สงขลา, ตรัง, สตูล, พัทลุง, ยะลา, ปัตตานี, นราธิวาส) — คอลัมน์ `staff_users.sale_provinces` (array)
2. **ประเภทลูกค้า** — เลือกจาก "ลูกค้าเอกชน" (โรงพยาบาลเอกชน/คลินิก/ร้านยา) หรือ "ลูกค้าภาครัฐ" (โรงพยาบาลรัฐ/หน่วยงานรัฐอื่นๆ) — คอลัมน์ `staff_users.sale_customer_types` (array), ขยายเป็น `org_type` จริงผ่านฟังก์ชัน `expandToOrgTypes()` ใน [lib/sale-coverage.ts](lib/sale-coverage.ts)

ทุก query ของบทบาทนี้ (`getSaleCustomerHistory`, `getSaleRequestDetail` ใน [app/actions/sale-actions.ts](app/actions/sale-actions.ts)) ดึงขอบเขตจาก **session ที่ verify แล้วเท่านั้น** ไม่รับ parameter ขอบเขตจากภายนอกเลย และกรองผ่าน RPC `get_sale_customer_history` บน Postgres โดยตรง — ออกแบบมาป้องกันไม่ให้ sale คนหนึ่งเห็นข้อมูลลูกค้านอกเขตของตัวเองแม้จะพยายามปลอมพารามิเตอร์จากฝั่ง client

---

## 4. Workflow หลักของระบบ (State Machine)

สถานะของคำขอ (`requests.current_status`) และรายการยาแต่ละชิ้น (`drug_items.current_status`) เดินตาม CHECK constraint เดียวกันที่บังคับไว้ระดับฐานข้อมูลจริง (ยืนยันจาก live schema):

```
pending_review → approved → in_transit → at_warehouse → checked_in → receiving → exchanging → completed
                     ↘ out_for_delivery                      ↘ rejected (จากเกือบทุกขั้น ทั้งระดับคำขอและระดับรายการยา)
```

**หมายเหตุ:** สถานะ `out_for_delivery` เป็นค่าที่อนุญาตไว้ใน constraint ของฐานข้อมูลแล้ว (สำหรับกรณีจัดส่งสินค้าแลกเปลี่ยนกลับให้ลูกค้า) แต่ยังไม่พบจุดในโค้ดปัจจุบันที่ set สถานะนี้อย่างชัดเจน — เป็นช่องที่เตรียมไว้สำหรับอนาคตหรือฟีเจอร์ที่กำลังพัฒนาต่อ ควรตรวจสอบเจตนาการใช้งานจริงกับทีมพัฒนา

**หลักการคำนวณสถานะรวม:** สถานะของ "คำขอ" ทั้งใบคำนวณจากสถานะรวมของ "รายการยา" ย่อยภายในคำขอนั้น เช่น ถ้ายาบางรายการถูกปฏิเสธแต่บางรายการผ่าน คำขอจะยังคงเดินหน้าต่อด้วยรายการที่เหลือ — ออกแบบมาให้รองรับความเป็นจริงที่คำขอหนึ่งใบมียาได้สูงสุด **5 รายการ** (บังคับทั้งฝั่ง UI และ server) และแต่ละรายการมีชะตากรรมต่างกันได้

**Audit trail:** ทุกการเปลี่ยนสถานะถูกบันทึกลงตาราง `status_logs` (ปัจจุบันมี 82 แถว) พร้อมผู้กระทำ (`staff_id`), แผนก, ประเภทผู้กระทำ (`actor_type`: staff/system/customer), หมายเหตุ, และรหัสเหตุผลปฏิเสธแบบมีโครงสร้าง (`rejection_reason_code` — enum จริงมี CHECK constraint จำกัดค่าเป็น `damaged`/`incomplete_docs`/`expired_window`/`customer_cancelled`/`other`) ทำให้สรุปสถิติ "เหตุผลปฏิเสธยอดนิยม" ได้แม่นยำโดยไม่ต้องพึ่งการเดาจากข้อความอิสระ

**ช่องทางการสร้างคำขอ 2 ทาง:** คอลัมน์ `requests.submission_channel` แยกระหว่าง `customer_portal` (ลูกค้ายื่นเอง) กับ `csr_manual` (CSR กรอกแทนกรณี walk-in/โทรศัพท์) — ทำให้แยกสถิติและตรวจสอบย้อนหลังได้ว่าใบงานไหนเกิดจากช่องทางไหน

---

## 5. รายการฟีเจอร์ทั้งหมด แยกตามกลุ่มผู้ใช้

### 5.1 ฝั่งลูกค้า

| ฟีเจอร์ | รายละเอียด |
|---|---|
| **สมัครสมาชิก** | ฟอร์มลงทะเบียนพร้อมเซ็นชื่อดิจิทัลและยินยอม PDPA ([components/auth/RegisterForm.tsx](components/auth/RegisterForm.tsx)) — เลือกประเภทหน่วยงาน/จังหวัด/ตำแหน่งผู้ติดต่อจาก dropdown ที่กำหนดไว้ (ปรับปรุงล่าสุด 2 ส.ค. — เดิมตำแหน่งเป็น free-text) |
| **เข้าสู่ระบบ** | OTP ทางอีเมล (ป้องกัน email enumeration) หรือ Google OAuth (เฉพาะอีเมลที่อนุมัติแล้ว) |
| **ยื่นคำขอคืน/แลกเปลี่ยน** | ฟอร์ม wizard 4 ขั้นตอน ([app/(authenticated)/form/](app/(authenticated)/form/)): ข้อมูลผู้ส่ง → รายการยา (สูงสุด 5 รายการ) → เหตุผล → เซ็นชื่อดิจิทัล → ตรวจทาน แล้วส่งผ่าน RPC `create_exchange_request` ที่คำนวณมูลค่ารวมฝั่ง server เสมอ (ไม่เชื่อค่าที่ client ส่งมา) |
| **ติดตามสถานะสาธารณะ** | `/tracking` — ใช้แค่ ref_id ไม่ต้องล็อกอิน เห็นเฉพาะข้อมูลปลอดภัย (column allowlist ตัดมูลค่าเงิน/เลขที่ invoice ออก) มี rate-limit เข้มกว่าปกติเมื่อค้นหา ref_id ที่ไม่พบซ้ำๆ เพื่อชะลอการ brute-force |
| **ประวัติของตัวเอง vs ประวัติทั้งหน่วยงาน** | `getCustomerExchangeHistory` แสดงเฉพาะคำขอที่ login นี้ยื่นเอง ส่วน `getOrgExchangeHistory` (`/customer/org-history`) รวมคำขอของ**ทุกบัญชี login ที่ใช้ `customer_code` เดียวกัน** — รองรับกรณีหน่วยงานเดียวมีผู้ใช้หลายคน |
| **Chatbot ลูกค้า** | ตอบคำถามนโยบายคืน/แลกเปลี่ยนแบบ streaming, จงใจไม่ผูก session/ไม่รู้ข้อมูลส่วนตัวลูกค้า (ดูหัวข้อ 5.3) เพื่อไม่เปิดช่องข้อมูลรั่ว, เก็บคำถามที่ตอบ "ไม่แน่ใจ" ไว้ให้ manager ทบทวน |
| **เร่งงาน** | ปุ่มบนหน้า tracking กดแจ้ง CSR/Manager ผ่าน `request_pings` (cooldown 6 ชม./คำร้อง กันกดสแปม) |

### 5.2 ฝั่ง CSR

| ฟีเจอร์ | รายละเอียด |
|---|---|
| **อนุมัติสมาชิกใหม่** | บังคับกรอกรหัสลูกค้า (`customer_code`) ก่อนอนุมัติเสมอ — แก้จากบั๊กที่เคยพบว่าคอลัมน์นี้ไม่เคยถูกกรอกเลยสักราย (ดู `SYSTEM_ASSESSMENT.md` ข้อ 7) พร้อม atomic compare-and-swap ป้องกัน double-submit สร้างบัญชีซ้ำ |
| **เอกสารยืนยันการลงทะเบียน** | เมื่ออนุมัติสำเร็จ ระบบสร้าง PDF อัตโนมัติ (ข้อมูลหน่วยงาน+ผู้ติดต่อ+รหัสลูกค้า+ลายเซ็นลูกค้า+ส่วนของพนักงาน GPO) เก็บใน bucket ส่วนตัว `registration-documents` แล้วส่งอีเมลลิงก์ดาวน์โหลดให้ลูกค้า |
| **กรอกฟอร์มแทนลูกค้า** | สำหรับกรณี walk-in/โทรศัพท์ บันทึกเป็น `submission_channel='csr_manual'` พร้อม `created_by_staff_id` |
| **ตรวจสอบ/อนุมัติคำขอ** | อนุมัติ/ปฏิเสธรายการยาแต่ละชิ้น พร้อมแท็ก compliance และเหตุผลปฏิเสธจาก dropdown ที่กำหนดไว้ (ไม่ใช่ free-text) |
| **Report Center** | `/admin/csr/reports` — ฟิลเตอร์ตามช่วงวันที่/สถานะ (10 สถานะ)/ประเภทคำขอ/ค้นหา ref-id หรือชื่อโรงพยาบาล พร้อม export เป็น Excel (`exceljs`) 3 ชีท: สรุปภาพรวม, รายการใบงาน, รายการยา |
| **Export รายชื่อลูกค้า** | ปุ่ม export `b2b_customers` ทั้งหมดเป็น Excel 1 ชีท (รหัสลูกค้า, ชื่อหน่วยงาน, ประเภท, จังหวัด, ผู้ติดต่อ, ตำแหน่ง, เบอร์, อีเมล) |

### 5.3 ฝั่งผู้จัดการ (Manager)

| ฟีเจอร์ | รายละเอียด |
|---|---|
| **อนุมัติพนักงานใหม่** | ทุกแผนก (CSR/WH/Log/Sale) ต้องผ่าน manager อนุมัติก่อน login ได้จริง (`is_approved=true`) |
| **Dashboard สรุปผล** | กราฟ (Recharts) สรุปเวลาเฉลี่ยแต่ละขั้นตอน, เหตุผลปฏิเสธยอดนิยม, สถิติแยกจังหวัด/ลูกค้า — คำนวณจาก [lib/manager-stats.ts](lib/manager-stats.ts) ซึ่งเป็น **single source of truth เดียว** ใช้ร่วมกันทั้งกราฟหน้าจอและ chatbot วิเคราะห์ข้อมูล (ป้องกันตัวเลขไม่ตรงกันระหว่าง 2 ช่องทาง) |
| **Chatbot พนักงาน** | ไม่ใช่แค่สรุปสถิติคงที่ — ใช้ Gemini function calling เรียก 3 tools จริงตามคำถาม: `get_rejection_breakdown`, `get_period_stats`, `get_customer_stats` ([app/actions/staff-chat-tools.ts](app/actions/staff-chat-tools.ts), มี unit test 9 เคส) |
| **ทบทวนคำถามที่บอทลูกค้าตอบไม่ได้** | ดูรายการจาก `chatbot_unanswered_questions` เพื่อพิจารณาเพิ่มเข้า FAQ |

### 5.4 ฝั่งคลังสินค้า (WH) และโลจิสติกส์

| ฟีเจอร์ | รายละเอียด |
|---|---|
| **โลจิสติกส์** | เปลี่ยนสถานะ `approved` → `in_transit` → `at_warehouse` หรือปฏิเสธพร้อมเหตุผลบังคับ (dropdown) |
| **คลังสินค้า — ตรวจสภาพ (checked-in)** | ตรวจรับรายชิ้น สถานะคำขอเปลี่ยนเป็น `checked_in` อัตโนมัติเมื่อครบทุกรายการ |
| **คลังสินค้า — รับเข้าคลัง (receiving)** | ยืนยันรับเข้าคลังได้ทั้งรายชิ้นและเป็นชุด (batch) — คำนวณสถานะคำขอจากสัดส่วนรายการที่ผ่าน/ไม่ผ่านให้ถูกต้อง |

### 5.5 ฝั่งฝ่ายขาย (Sale)

| ฟีเจอร์ | รายละเอียด |
|---|---|
| **ดูประวัติลูกค้าในเขตที่ดูแล** | `/admin/sale/history` — filter อัตโนมัติตามจังหวัด+ประเภทลูกค้าที่ผูกกับบัญชี ไม่ต้องเลือกเอง และไม่เห็นข้อมูลนอกเขต |
| **ดูรายละเอียดใบงานเดี่ยว** | timeline/stepper/รายการยาแบบเดียวกับที่ CSR เห็น แต่ต้องผ่านการ join ตรวจ `org_type`/`province` ของลูกค้าเจ้าของใบงานก่อนทุกครั้ง |

---

## 6. สถาปัตยกรรมเทคนิค

| ส่วนประกอบ | เทคโนโลยีที่ใช้จริง |
|---|---|
| Frontend/Backend | **Next.js 16.2.9** (App Router, pin เวอร์ชันชัดเจนแล้ว — เดิม pin เป็น `"latest"`) + React 19 + Server Actions, TypeScript, Tailwind CSS + shadcn/ui |
| ฐานข้อมูล/BaaS | Supabase (**PostgreSQL 17.6**) — จัดการ schema ผ่าน 35 migration files ใน `supabase/migrations/` (เดิมไม่มีไฟล์ migration เก็บใน repo เลย ภายหลังได้ reconcile ให้ตรงกับ history จริงบน production แล้ว) |
| Authentication | ระบบ session แบบเขียนเอง (ตาราง `sessions` + httpOnly cookie) แยกอิสระจาก Supabase Auth — ยืนยันตัวตนลูกค้าด้วย OTP ทางอีเมล หรือ Google OAuth (ผูกกับอีเมลที่อนุมัติแล้วเท่านั้น), พนักงานด้วย username/password (bcrypt เทียบ dummy hash เสมอกันการ timing attack) |
| การเข้าถึงฐานข้อมูล | Server action ทุกจุดใช้ Supabase client แบบ `service_role` ควบคุมสิทธิ์ในชั้นแอปพลิเคชัน โดยมี RLS policy เป็นเกราะป้องกันชั้นที่สอง (defense-in-depth) — ทั้ง 15 ตารางเปิด RLS ครบ |
| เอกสาร/PDF | `pdf-lib` + `fontkit` (รองรับฟอนต์ไทย Sarabun) สร้าง PDF 2 ประเภท: ยืนยันคำร้อง (customer/CSR) และยืนยันการลงทะเบียน (CSR อนุมัติสมาชิกใหม่) |
| อีเมล | Resend — ส่ง OTP, ยืนยันการลงทะเบียนเบื้องต้น (`lib/emails/OTPEmail.tsx`, `WelcomeEmail.tsx`), แจ้งผลอนุมัติ, แนบลิงก์เอกสาร (อีเมลแจ้งผลอนุมัติ/ส่ง PDF ส่วนใหญ่เขียนเป็น inline HTML ในแต่ละ action ไม่ได้ใช้ React-Email template ร่วม) |
| ลายเซ็นดิจิทัล | react-signature-canvas — เซ็นบน canvas แล้วอัปโหลดเป็นไฟล์ภาพผ่าน server เท่านั้น (ไม่เชื่อ URL จาก client), จำกัดขนาดไฟล์ 2MB |
| กราฟ/สถิติ | Recharts — ใช้ร่วมกันทั้งหน้า Manager Dashboard และ CSR Report Center |
| Excel export | `exceljs` — CSR Report Center (3 ชีท) และ export รายชื่อลูกค้า |
| Chatbot | เรียก **Google Gemini API โดยตรง** (โมเดล `gemini-3.1-flash-lite`) 2 ตัวแยกกัน: บอทลูกค้า (ตอบ FAQ นโยบายคืนสินค้า แบบ streaming, ไม่มี function calling) และบอทพนักงาน (function calling query ข้อมูลสด, ต้องผ่าน session manager/CSR) |
| Rate limiting | ทำเองบน PostgreSQL (ตาราง `rate_limits` + RPC `increment_rate_limit`) แทนการพึ่ง Redis — ออกแบบมาเพื่อรองรับการ self-host โดยไม่ต้องมีบริการภายนอกเพิ่ม |
| CI/CD | GitHub Actions (`.github/workflows/ci.yml`) รัน typecheck → unit test → lint (ยังไม่ block, มี error เดิมค้างอยู่ ~200 จุดรอทยอยแก้) → build อัตโนมัติทุก push เข้า `main`/`RLS_supabase` และทุก PR เข้า `main` |
| Testing | Vitest — unit test **54 เคส** ครอบคลุม state machine ของ WH/Logistics/CSR และ chatbot function-calling tools, มี chatbot eval แยกต่างหาก (`npm run eval:chatbot`, skip อัตโนมัติถ้าไม่มี API key กันยิง Gemini จริงโดยไม่ตั้งใจ) |
| Extensions ที่ติดตั้งจริงบน Postgres | `pgcrypto`, `uuid-ossp`, `pg_stat_statements`, `pg_cron`, `supabase_vault`, `plpgsql` (ตรวจสอบจาก live `list_extensions` — extension อื่นที่ Supabase เสนอให้ยังไม่ได้ติดตั้ง) |

---

## 7. โครงสร้างข้อมูลหลัก (Data Model)

ตรวจสอบจาก live schema ของ Supabase จริง (verbose) — ทั้งหมด **15 ตาราง เปิด RLS ครบทุกตาราง**

| ตาราง | แถวปัจจุบัน | ความหมาย |
|---|---|---|
| `requests` | 6 | คำขอคืน/แลกเปลี่ยนหลัก 1 ใบต่อการยื่นคำร้อง 1 ครั้ง — มี `submission_channel` แยกช่องทาง portal/CSR manual |
| `drug_items` | 12 | รายการยา/เวชภัณฑ์แต่ละชิ้นในคำขอ (สูงสุด 5 รายการ/คำขอ) — มี `value_amount`/`invoice_number` ที่ไม่เปิดเผยแบบสาธารณะ |
| `b2b_customers` | 3 | บัญชีลูกค้า (โรงพยาบาล/คลินิก/ร้านยา) ที่อนุมัติแล้ว — มี `customer_code`, `org_type`, `province` สำหรับกรองขอบเขตของ Sale |
| `clients` | 3 | คำขอสมัครสมาชิกใหม่ที่รอ CSR อนุมัติ — มี `pdpa_consented_at` |
| `staff_users` | 5 | บัญชีพนักงาน แบ่งตามแผนก csr / log (โลจิสติกส์) / wh (คลัง) / manager / **sale** (ใหม่) — มี `sale_customer_types`/`sale_provinces` เฉพาะบทบาท sale |
| `sessions` | 122 | session token ของทั้งลูกค้าและพนักงาน |
| `status_logs` | 82 | audit trail การเปลี่ยนสถานะทุกจุด พร้อมเหตุผลปฏิเสธแบบมีโครงสร้าง (`rejection_reason_code`) |
| `data_correction_logs` | 0 | audit trail การแก้ไขข้อมูลย้อนหลัง (ยังไม่เคยถูกใช้จริง — ฟีเจอร์พร้อมแต่ยังไม่มี use case เกิดขึ้น) |
| `access_logs` | 15 | log การเข้าถึงเอกสาร เพื่อรองรับ PDPA |
| `document_attachments` | 7 | ที่อยู่ไฟล์ PDF ที่สร้างแล้ว (เข้าถึงผ่าน signed URL เท่านั้น) — รองรับทั้งเอกสารยืนยันคำร้อง (`request_id`/`ref_id`) และเอกสารยืนยันการลงทะเบียน (`client_id`) |
| `otp_logs` | 42 | OTP hash และเวลาหมดอายุ |
| `timeline_summary` | 30 | สรุป timeline สำหรับหน้าติดตามสถานะสาธารณะ |
| `request_pings` | 3 | ปุ่ม "เร่งงาน" ที่ลูกค้ากดจากหน้าติดตามสถานะ แจ้งเตือนพนักงานผ่าน chat widget (cooldown 6 ชม./คำร้อง) |
| `rate_limits` | 7 | ตัวนับสำหรับ rate limiting แบบ fixed-window |
| `chatbot_unanswered_questions` | 0 | คำถามที่บอทลูกค้าตอบว่า "ไม่แน่ใจ" เก็บให้ manager ทบทวนเพื่อเพิ่มเข้า FAQ |

**ความสัมพันธ์สำคัญ:** `requests` ↔ `drug_items` (1-ต่อ-กลาย), `requests` ↔ `b2b_customers`, `staff_users` ถูกอ้างอิงจาก 7 ตาราง (เป็นศูนย์กลางของทุก audit trail ผ่าน `staff_id`) — ยืนยันว่าออกแบบมาให้ทุกการกระทำของพนักงานสามารถสืบย้อนกลับไปหาตัวบุคคลได้เสมอ

---

## 8. สถาปัตยกรรมความปลอดภัย

- **Defense-in-depth 2 ชั้น:** ชั้นแอปพลิเคชัน (server action ตรวจสอบสิทธิ์ก่อน query เสมอ) + ชั้นฐานข้อมูล (RLS เปิดครบทุกตาราง เป็นเกราะป้องกันสำรองหาก service_role key รั่วหรือมีบั๊กในโค้ด)
- **ป้องกัน enumeration:** OTP flow คืนค่า success เสมอไม่ว่าอีเมลจะมีอยู่จริงหรือไม่, staff login เทียบ bcrypt กับ dummy hash เสมอแม้ user ไม่พบ (ป้องกัน timing attack), หน้า tracking สาธารณะ throttle เข้มกว่าปกติเมื่อค้นหา ref_id ที่ไม่พบซ้ำๆ
- **เอกสารเข้าถึงผ่าน signed URL อายุสั้นเท่านั้น** ไม่มี public URL ของไฟล์ PDF/ลายเซ็นลูกค้า
- **Column allowlist บนหน้าสาธารณะ:** หน้า tracking ที่ไม่ต้องล็อกอินไม่เปิดเผย `value_amount`/`invoice_number`/`compliance_remark`
- **Rate limiting ครบทุกจุดที่เคยมีบั๊ก** (เดิมเคยพบว่า 7 จุดเช็คผิดจน rate-limit ไม่ทำงานจริง — แก้ครบแล้วทั้งหมด รวม 8 จุดที่มีปัญหาเรื่อง argument order ผิด)
- **สถานะ Security Advisor ปัจจุบัน (ตรวจสด 2 ส.ค. 2569):** เหลือคำเตือนเดียวระดับ WARN คือยังไม่เปิด **Leaked Password Protection** ใน Supabase Auth settings — เป็นการตั้งค่าที่ทำได้ทันทีจาก dashboard ไม่ใช่บั๊กในโค้ด
- **Performance Advisor:** พบเฉพาะระดับ INFO (foreign key ไม่มี index รองรับ 3 จุด, index ที่ยังไม่เคยถูกใช้ 11 จุด) — ปกติสำหรับระบบที่ข้อมูลยังน้อย ยังไม่กระทบการใช้งานจริง

รายละเอียดคะแนนประเมินความปลอดภัยแบบเต็มและ evidence อยู่ในเอกสาร `02-valuation-uat.md`

---

## 9. จุดแข็งของระบบ

- **Workflow ครบวงจรจริง** ครอบคลุมทั้งฝั่งลูกค้าและ **5 แผนกภายใน** (เพิ่มขึ้นจาก 4 แผนกเดิม) ไม่ใช่แค่ฟอร์มบันทึกข้อมูลเดียว
- **Audit trail ระดับที่เหมาะกับงานกำกับดูแล** — `status_logs`, `data_correction_logs`, `access_logs` ครอบคลุมทั้งการเปลี่ยนสถานะ การแก้ไขข้อมูลย้อนหลัง และการเข้าถึงเอกสาร ซึ่งมักเป็นข้อกำหนดของหน่วยงานรัฐ/รัฐวิสาหกิจ
- **สัญชาตญาณด้านความปลอดภัยที่ดีหลายจุด** ที่ไม่ใช่แค่ทำตามเช็คลิสต์ทั่วไป แต่ออกแบบเฉพาะให้เข้ากับ threat model ของระบบนี้ (เช่น การขยายขอบเขต Sale จาก session เท่านั้น ไม่รับ parameter จาก client)
- **เลือกใช้ PostgreSQL แทน Redis สำหรับ rate-limit** สะท้อนการออกแบบที่คำนึงถึงการ self-host ตั้งแต่ต้น เหมาะกับหน่วยงานที่มีข้อจำกัดเรื่อง cloud vendor
- **Chatbot สองชั้นที่ออกแบบขอบเขตสิทธิ์ต่างกันอย่างตั้งใจ** — บอทลูกค้าจงใจไม่ผูก session (ป้องกันข้อมูลรั่ว) ส่วนบอทพนักงานผูก session และ function calling เต็มรูปแบบ ไม่ใช่ implement เดียวกันแล้วเปิด/ปิดฟีเจอร์
- **มี automated test และ CI แล้วครบ** ครอบคลุม state machine ของแผนกหลักซึ่งเป็นหัวใจของธุรกิจ (54 เคส) พร้อม chatbot regression eval แยกต่างหาก
- **Single source of truth สำหรับ business logic ที่ใช้ซ้ำหลายที่** — เช่น `lib/manager-stats.ts` (สถิติ), `lib/sale-coverage.ts` (ขอบเขต Sale), `lib/return-policy.ts` (นโยบายคืนสินค้า) แทนการ copy เนื้อหาซ้ำในหลายไฟล์ ลดความเสี่ยงข้อมูล drift
- **มี migration history ครบและ sync กับ production จริงแล้ว** (35 ไฟล์) หลังจากเคยมีช่วงที่ schema จัดการนอก repo ทั้งหมด — เป็นพื้นฐานสำคัญสำหรับการ self-host หรือ disaster recovery ในอนาคต

---

## 10. จุดที่ควรทราบ (ช่องว่าง/หนี้เทคนิคที่ยังไม่กระทบการใช้งาน)

รายการนี้ไม่มีจุดไหนเป็นช่องโหว่ความปลอดภัยหรือบั๊กที่กระทบผู้ใช้จริง — เป็นรายการที่พบระหว่างรีวิวรอบนี้ เหมาะสำหรับวางแผนงานบำรุงรักษาต่อเนื่อง ไม่ใช่ของเร่งด่วน:

- **`app/welcome/dashboard/page.tsx` ดูเหมือนหน้าที่ยังสร้างไม่เสร็จ** — มี placeholder comment `{/* รายการเมนู 4 รายการ */}` เป็นกริดว่าง และไม่มีจุดไหนในระบบลิงก์มาที่หน้านี้เลย (หน้า `/welcome` หลักที่ลูกค้าใช้จริงมีเมนู 4 รายการอยู่แล้วสมบูรณ์) ควรตรวจสอบว่าเป็นหน้าที่ตั้งใจทำต่อ หรือเป็นโค้ดค้างที่ลบทิ้งได้
- **`README.md` ยังเป็น boilerplate ของ Vercel/Supabase starter kit ทั้งหมด** ไม่มีคำอธิบายเฉพาะของโปรเจกต์นี้เลย (ไม่มี env var จริงที่ต้องตั้ง เช่น `GEMINI_API_KEY`/`RESEND_API_KEY`/`OTP_PEPPER`, ไม่มีขั้นตอน setup ที่ตรงกับระบบจริง) — เอกสารอ้างอิงหลักตอนนี้คือ `SYSTEM_ASSESSMENT.md` และเอกสารชุดนี้แทน ควรปรับ README ให้ตรงกับโปรเจกต์จริงก่อนมีทีมใหม่เข้าร่วม
- **สถานะ `out_for_delivery`** มีอยู่ใน CHECK constraint ของฐานข้อมูลแล้วแต่ยังไม่พบจุดในโค้ดที่ set สถานะนี้จริง — อาจเป็นการเตรียมไว้ล่วงหน้าสำหรับฟีเจอร์ "จัดส่งสินค้าแลกเปลี่ยนกลับลูกค้า" ที่ยังไม่ implement ควรยืนยันเจตนากับทีมพัฒนา
- **`eslint-config-next` เพิ่งอัปเดตให้ตรงกับ Next.js 16 แล้ว** (2 ส.ค. 2569) แต่ยังมี lint error เดิมค้างอยู่ราว 200 จุด (ส่วนใหญ่เป็น `@typescript-eslint/no-explicit-any`) ที่ยังไม่ block CI — เป็นหนี้เทคนิคระดับ code quality ไม่ใช่บั๊ก
- **`data_correction_logs` ยังไม่เคยมีข้อมูลจริง (0 แถว)** — ฟีเจอร์แก้ไขข้อมูลย้อนหลังพร้อมใช้งานแล้วแต่ยังไม่เคยถูกเรียกใช้จริง เป็นเรื่องปกติสำหรับระบบที่เพิ่งเริ่มใช้งาน ไม่ใช่สัญญาณของปัญหา

---

## 11. สถิติโครงการและปริมาณข้อมูลปัจจุบัน

| ตัวชี้วัด | ค่า |
|---|---|
| ระยะเวลาพัฒนา | 12 มิถุนายน 2569 – ปัจจุบัน (~7 สัปดาห์) |
| จำนวน commits | 165 |
| จำนวนไฟล์ TypeScript/TSX | 131 ไฟล์ |
| จำนวนบรรทัดโค้ด (TS/TSX รวม) | ~20,600 บรรทัด |
| จำนวน migration files | 35 ไฟล์ |
| จำนวน unit test | 54 เคส (5 test files ผ่านทั้งหมด) |
| จำนวนตารางฐานข้อมูล | 15 ตาราง (RLS เปิดครบ 100%) |
| จำนวนบัญชีลูกค้าที่อนุมัติแล้ว | 3 ราย |
| จำนวนคำขอคืน/แลกเปลี่ยนสะสม | 6 ใบ (12 รายการยา) |
| จำนวน session สะสม | 122 ครั้ง (สะท้อนว่ามีการทดสอบ/ใช้งานจริงพอสมควรแล้วเทียบกับจำนวนข้อมูลธุรกิจ) |
| จำนวนบัญชีพนักงาน | 5 บัญชี ครอบคลุม 5 แผนก |

---

## 12. ประเมินศักยภาพเชิงธุรกิจโดยย่อ

ระบบนี้เริ่มพัฒนาในลักษณะ pilot/prototype แต่ business logic และ workflow ที่ออกแบบไว้ครบถ้วนและตรงกับ pain point จริงของกระบวนการคืน/แลกเปลี่ยนยาระหว่างองค์กร ความเร็วในการพัฒนา (165 commits ใน ~7 สัปดาห์ ครอบคลุม 6 บทบาทผู้ใช้) แสดงถึงความสามารถในการส่งมอบฟีเจอร์ใหม่ได้รวดเร็วเมื่อมีความต้องการทางธุรกิจเพิ่มขึ้น (เช่น การเพิ่มบทบาท Sale ทั้งระบบภายในเวลาไม่กี่วัน)

จุดที่เคยเป็นความเสี่ยงระดับวิกฤต (rate-limit ใช้งานไม่ได้จริง, RLS policy ขาดหาย, ระบบ auth ปนกันสองระบบ, ไม่มี automated test) ได้รับการแก้ไขครบแล้วตามการติดตามผลล่าสุด และปัจจุบัน Supabase security advisor เหลือเพียงคำเตือนระดับต่ำ 1 รายการ (เปิดใช้ leaked-password protection) ซึ่งเป็นการตั้งค่าที่ทำได้ทันทีจาก dashboard

**สรุป:** ระบบอยู่ในสถานะที่ **เหมาะสำหรับนำไปทดสอบ UAT และ pilot กับกลุ่มลูกค้าจำกัดก่อนขยายสู่การใช้งานจริงทั้งองค์กร** รายละเอียดการประเมินความพร้อมแบบละเอียด (คะแนนราย dimension พร้อม evidence) และแผนทดสอบเต็มรูปแบบอยู่ในเอกสาร `02-valuation-uat.md`

---

## 13. การเปลี่ยนแปลงล่าสุด (Changelog)

**2 สิงหาคม 2569 (ฉบับนี้):**
- รีเสิร์ชระบบใหม่ทั้งหมดจากโค้ดจริง + live Supabase schema — เพิ่มบทบาท **Sale** ที่ไม่เคยถูกบันทึกไว้ในฉบับก่อนหน้า, เพิ่มสถานะ `out_for_delivery` ในรายการ state machine, เพิ่มรายละเอียดฟีเจอร์ที่ไม่เคยกล่าวถึง (Report Center, org-history, welcome page, tracking allowlist)
- ยืนยัน Next.js pin เป็น `16.2.9` และ `eslint-config-next` อัปเดตตรงกันแล้ว (เดิมทั้งคู่ยังไม่ตรงกัน)
- ยืนยัน staff-chat function calling ทดสอบกับ Gemini API จริงแล้ว ใช้งานได้ปกติ

**นับจากฉบับ 30 กรกฎาคม 2569 (ก่อนฉบับสมบูรณ์นี้):**
- **หน้าแรก (`app/page.tsx`):** เพิ่ม cover slideshow 2 ภาพ เลื่อนอัตโนมัติทุก 2 นาที พร้อมปุ่มลูกศรและ dot indicator
- **ฟอร์มลงทะเบียนลูกค้า:** จัดกลุ่มข้อมูลใหม่ให้ตรงกับลำดับที่ผู้ใช้กรอกจริง และเปลี่ยนฟิลด์ตำแหน่งจาก free-text เป็น dropdown

ไม่มีการเปลี่ยนแปลงด้าน schema, RLS, หรือ security posture ระหว่างช่วงนี้ — ตรวจสอบ live advisor ซ้ำแล้วยืนยันตรงกับฉบับก่อนหน้าทุกจุด
