# ภาพรวมระบบ GPO Xchange Portal
### เอกสารแนะนำระบบและประเมินศักยภาพ

**จัดทำเมื่อ:** 30 กรกฎาคม 2569 · **ปรับปรุงล่าสุด:** 2 สิงหาคม 2569
**อ้างอิงจาก:** โค้ด repository `newgpo-exchange` (Next.js 15 + Supabase, branch `RLS_supabase`), schema จริงบน Supabase project `tanagritroj-gpo` (`qgztveswduffskucqppt`) ตรวจสดอีกครั้งวันที่ปรับปรุง, และเอกสารประเมินระบบภายใน `SYSTEM_ASSESSMENT.md`

---

## 1. ระบบนี้คืออะไร

GPO Xchange Portal เป็นเว็บแอปพลิเคชันที่ **แปลงกระบวนการรับคืน/แลกเปลี่ยนยาและเวชภัณฑ์แบบ B2B** ระหว่างหน่วยงาน (สื่อถึงองค์การเภสัชกรรมหรือหน่วยงานลักษณะเดียวกัน จากชื่อโดเมน "GPO" ในโค้ด) กับโรงพยาบาลคู่ค้า จากกระบวนการกระดาษเดิมให้กลายเป็นระบบดิจิทัลที่ติดตามสถานะได้แบบเรียลไทม์ มี audit trail ครบ และลดภาระงานประสานงานระหว่างแผนก

**ปัญหาที่ระบบแก้:** กระบวนการคืน/แลกเปลี่ยนยาแบบเดิมต้องผ่านหลายแผนก (ลูกค้า → ฝ่ายบริการลูกค้า → โลจิสติกส์ → คลังสินค้า → ฝ่ายบริการลูกค้าอีกครั้ง) ด้วยเอกสารกระดาษ ทำให้ "งานหาย", "ตามสถานะไม่ได้", และ "ไม่มีหลักฐานย้อนหลังว่าใครอนุมัติ/ปฏิเสธอะไรตอนไหน" — ระบบนี้ digitize ทั้งกระบวนการพร้อมเก็บ log ทุกการเปลี่ยนสถานะ

---

## 2. ผู้ใช้งานและบทบาท (Actors)

| บทบาท | เข้าถึงผ่าน | หน้าที่หลัก |
|---|---|---|
| **ลูกค้า (โรงพยาบาล/B2B)** | หน้าเว็บสาธารณะ + พื้นที่หลัง login | ล็อกอินด้วย OTP ทางอีเมล (หรือ Google), กรอกฟอร์มขอคืน/แลกสินค้าพร้อมเซ็นชื่อดิจิทัล, ติดตามสถานะคำร้อง, ดูประวัติ, ดาวน์โหลดเอกสาร PDF, คุยกับ chatbot ตอบคำถามนโยบาย |
| **CSR (Customer Service)** | `/admin/csr` | อนุมัติ/ปฏิเสธคำขอสมัครสมาชิกใหม่, กรอกฟอร์มแทนลูกค้า (walk-in/โทรศัพท์), ตรวจสอบและอนุมัติ/ปฏิเสธคำขอและรายการยาแต่ละชิ้น, แท็กความสอดคล้อง (compliance) ของสินค้า |
| **คลังสินค้า (Warehouse)** | `/admin/wh` | ตรวจรับสินค้า 2 ขั้นตอน: ตรวจสภาพ (checked-in) → รับเข้าคลัง (receiving) ทำได้ทั้งรายชิ้นและเป็นชุด |
| **โลจิสติกส์ (Logistics)** | `/admin/logistics` | เปลี่ยนสถานะ อนุมัติแล้ว → อยู่ระหว่างขนส่ง → ถึงคลัง หรือปฏิเสธ พร้อมระบุเหตุผล |
| **ผู้จัดการ (Manager)** | `/admin/manager` | อนุมัติบัญชีพนักงานใหม่, ดูกราฟ/สถิติสรุปผลการดำเนินงานทุกแผนก, คุยกับ chatbot วิเคราะห์ข้อมูลภายใน (เช่น ลูกค้ารายนี้เคยคืนกี่ครั้ง, เหตุผลปฏิเสธยอดนิยม) |

---

## 3. Workflow หลักของระบบ

สถานะของคำขอ (`requests.current_status`) และรายการยาแต่ละชิ้น (`drug_items.current_status`) เดินตาม state machine เดียวกัน:

```
pending_review → approved → in_transit → at_warehouse → checked_in → receiving → exchanging → completed
                                                ↘ rejected (ปฏิเสธได้จากเกือบทุกขั้น ทั้งระดับคำขอและระดับรายการยา)
```

สถานะของ "คำขอ" ทั้งใบคำนวณจากสถานะรวมของ "รายการยา" ย่อยภายในคำขอนั้น เช่น ถ้ายาบางรายการถูกปฏิเสธแต่บางรายการผ่าน คำขอจะยังคงเดินหน้าต่อด้วยรายการที่เหลือ — ออกแบบมาให้รองรับความเป็นจริงที่คำขอหนึ่งใบอาจมียาได้สูงสุด 5 รายการ และแต่ละรายการมีชะตากรรมต่างกันได้

ทุกการเปลี่ยนสถานะถูกบันทึกลงตาราง `status_logs` พร้อมผู้กระทำ, แผนก, หมายเหตุ และรหัสเหตุผลปฏิเสธแบบมีโครงสร้าง (`rejection_reason_code`) ทำให้สามารถสรุปสถิติ "เหตุผลปฏิเสธยอดนิยม" ได้แม่นยำโดยไม่ต้องพึ่งการเดาจากข้อความอิสระ

---

## 4. สถาปัตยกรรมเทคนิค

| ส่วนประกอบ | เทคโนโลยีที่ใช้ |
|---|---|
| Frontend/Backend | Next.js 15 (App Router) + React 19 + Server Actions, TypeScript, Tailwind CSS + shadcn/ui |
| ฐานข้อมูล/Backend-as-a-Service | Supabase (PostgreSQL 17) — จัดการ schema ผ่าน migration files ใน `supabase/migrations/` |
| Authentication | ระบบ session แบบเขียนเอง (ตาราง `sessions` + httpOnly cookie) แยกอิสระจาก Supabase Auth — ยืนยันตัวตนลูกค้าด้วย OTP ทางอีเมล หรือ Google OAuth (ผูกกับอีเมลที่อนุมัติแล้วเท่านั้น), พนักงานด้วย username/password (bcrypt) |
| การเข้าถึงฐานข้อมูล | Server action ทุกจุดใช้ Supabase client แบบ `service_role` ควบคุมสิทธิ์ในชั้นแอปพลิเคชัน โดยมี RLS policy เป็นเกราะป้องกันชั้นที่สอง (defense-in-depth) |
| เอกสาร/PDF | `pdf-lib` + `fontkit` (รองรับฟอนต์ไทย Sarabun) สร้างเอกสารยืนยันคำร้อง/การลงทะเบียน |
| อีเมล | Resend (ส่ง OTP, แจ้งผลอนุมัติ, แนบลิงก์เอกสาร) |
| ลายเซ็นดิจิทัล | react-signature-canvas — เซ็นบน canvas แล้วอัปโหลดเป็นไฟล์ภาพผ่าน server (ไม่เชื่อ URL จาก client) |
| กราฟ/สถิติ | Recharts |
| Chatbot | เรียก Gemini API โดยตรง 2 ตัว: บอทลูกค้า (ตอบ FAQ นโยบายคืนสินค้า) และบอทพนักงาน (query ข้อมูลสดผ่าน function calling) |
| Rate limiting | ทำเองบน PostgreSQL (ตาราง `rate_limits` + RPC `increment_rate_limit`) แทนการพึ่ง Redis — ออกแบบมาเพื่อรองรับการ self-host โดยไม่ต้องมีบริการภายนอกเพิ่ม |
| CI/CD | GitHub Actions (`.github/workflows/ci.yml`) รัน typecheck + test + build อัตโนมัติ |
| Testing | Vitest — มี unit test ครอบคลุม state machine ของ WH/Logistics/CSR และ chatbot eval แยกต่างหาก |

---

## 5. โครงสร้างข้อมูลหลัก (Supabase, live schema)

| ตาราง | ความหมาย |
|---|---|
| `requests` | คำขอคืน/แลกเปลี่ยนหลัก 1 ใบต่อการยื่นคำร้อง 1 ครั้ง |
| `drug_items` | รายการยา/เวชภัณฑ์แต่ละชิ้นในคำขอ (สูงสุด 5 รายการ/คำขอ) |
| `b2b_customers` | บัญชีลูกค้า (โรงพยาบาล) ที่อนุมัติแล้ว |
| `clients` | คำขอสมัครสมาชิกใหม่ที่รอ CSR อนุมัติ |
| `staff_users` | บัญชีพนักงาน แบ่งตามแผนก csr / log (โลจิสติกส์) / wh (คลัง) / manager |
| `sessions` | session token ของทั้งลูกค้าและพนักงาน |
| `status_logs` | audit trail การเปลี่ยนสถานะทุกจุด พร้อมเหตุผลปฏิเสธแบบมีโครงสร้าง |
| `data_correction_logs` | audit trail การแก้ไขข้อมูลย้อนหลัง |
| `access_logs` | log การเข้าถึงเอกสาร เพื่อรองรับ PDPA |
| `document_attachments` | ที่อยู่ไฟล์ PDF ที่สร้างแล้ว (เข้าถึงผ่าน signed URL เท่านั้น) |
| `otp_logs` | OTP hash และเวลาหมดอายุ |
| `timeline_summary` | สรุป timeline สำหรับหน้าติดตามสถานะสาธารณะ |
| `request_pings` | ปุ่ม "เร่งงาน" ที่ลูกค้ากดจากหน้าติดตามสถานะ แจ้งเตือนพนักงานผ่าน chat widget |
| `rate_limits` | ตัวนับสำหรับ rate limiting แบบ fixed-window |
| `chatbot_unanswered_questions` | คำถามที่บอทลูกค้าตอบว่า "ไม่แน่ใจ" เก็บให้ manager ทบทวนเพื่อเพิ่มเข้า FAQ |

---

## 6. จุดแข็งของระบบ

- **Workflow ครบวงจรจริง** ครอบคลุมทั้งฝั่งลูกค้าและ 4 แผนกภายใน ไม่ใช่แค่ฟอร์มบันทึกข้อมูลเดียว
- **Audit trail ระดับที่เหมาะกับงานกำกับดูแล** — `status_logs`, `data_correction_logs`, `access_logs` ครอบคลุมทั้งการเปลี่ยนสถานะ การแก้ไขข้อมูลย้อนหลัง และการเข้าถึงเอกสาร ซึ่งมักเป็นข้อกำหนดของหน่วยงานรัฐ/รัฐวิสาหกิจ
- **สัญชาตญาณด้านความปลอดภัยที่ดีหลายจุด** เช่น OTP flow ป้องกัน email enumeration, staff login เทียบ bcrypt กับ dummy hash เสมอเพื่อป้องกัน timing attack, เอกสารเข้าถึงผ่าน signed URL อายุสั้นแทน public URL, หน้าติดตามสถานะสาธารณะมี column allowlist ไม่เผยข้อมูลการเงิน/invoice
- **เลือกใช้ PostgreSQL แทน Redis สำหรับ rate-limit** สะท้อนการออกแบบที่คำนึงถึงการ self-host ตั้งแต่ต้น เหมาะกับหน่วยงานที่มีข้อจำกัดเรื่อง cloud vendor
- **Chatbot สองชั้น** (ลูกค้า + พนักงาน) เป็นฟีเจอร์เสริมมูลค่าที่ลดภาระ CSR ตอบคำถามซ้ำๆ และช่วยผู้บริหารดูภาพรวมได้เร็วขึ้น
- **มี automated test และ CI แล้ว** ครอบคลุม state machine ของแผนกหลัก ซึ่งเป็นหัวใจของธุรกิจ

---

## 7. ประเมินศักยภาพเชิงธุรกิจโดยย่อ

ระบบนี้เริ่มพัฒนาในลักษณะ pilot/prototype โดยทีมพัฒนาขนาดเล็ก แต่ business logic และ workflow ที่ออกแบบไว้ครบถ้วนและตรงกับ pain point จริงของกระบวนการคืน/แลกเปลี่ยนยาระหว่างองค์กร จุดที่เคยเป็นความเสี่ยงระดับวิกฤต (rate-limit ใช้งานไม่ได้จริง, RLS policy ขาดหาย, ระบบ auth ปนกันสองระบบ) ได้รับการแก้ไขครบแล้วตามการติดตามผลล่าสุด และปัจจุบัน Supabase security advisor เหลือเพียงคำเตือนระดับต่ำ (เปิดใช้ leaked-password protection) ซึ่งเป็นการตั้งค่าที่ทำได้ทันทีจาก dashboard

**สรุป:** ระบบอยู่ในสถานะที่ **เหมาะสำหรับนำไปทดสอบ UAT และ pilot กับกลุ่มลูกค้าจำกัดก่อนขยายสู่การใช้งานจริงทั้งองค์กร** รายละเอียดการประเมินความพร้อมและแผนทดสอบอยู่ในเอกสาร `02-valuation-uat.md`

---

## 8. การเปลี่ยนแปลงล่าสุด (นับจากฉบับ 30 กรกฎาคม 2569)

ตรวจสอบ live schema/advisor ซ้ำวันที่ปรับปรุงเอกสาร (2 สิงหาคม 2569) — **ไม่มีการเปลี่ยนแปลงด้าน schema, RLS, หรือ security posture** จากฉบับก่อนหน้า (ตาราง 14 ตารางเปิด RLS ครบเหมือนเดิม, security advisor เหลือคำเตือนเดียวเรื่อง leaked-password protection เหมือนเดิม) การเปลี่ยนแปลงในรอบนี้เป็นระดับหน้าจอ/ฟอร์มเท่านั้น:

- **หน้าแรก (`app/page.tsx`):** เพิ่ม cover slideshow 2 ภาพ เลื่อนอัตโนมัติทุก 2 นาที พร้อมปุ่มลูกศรและ dot indicator ให้ผู้ใช้เลื่อนเองได้ ใช้ `object-contain` เพื่อแสดงภาพเต็มโดยไม่ครอปสัดส่วน
- **ฟอร์มลงทะเบียนลูกค้า (`components/auth/RegisterForm.tsx`):** จัดกลุ่มข้อมูลใหม่ให้ตรงกับลำดับที่ผู้ใช้กรอกจริง (ย้ายประเภทหน่วยงานขึ้นก่อนชื่อหน่วยงาน, ย้ายฟิลด์ตำแหน่งจากหมวด "ข้อมูลหน่วยงาน" ไปอยู่หมวด "ข้อมูลผู้ติดต่อ" ถัดจากชื่อผู้ติดต่อ) และเปลี่ยนฟิลด์ **ตำแหน่ง** จาก free-text เป็น dropdown ที่มีตัวเลือกคงที่ (เภสัชกร / เจ้าหน้าที่พัสดุ / เจ้าของกิจการ / อื่นๆ ระบุเอง) ช่วยให้ข้อมูลตำแหน่งผู้ติดต่อสม่ำเสมอพอที่จะนำไปทำสถิติ/แบ่งกลุ่มลูกค้าได้ในอนาคต แทนที่จะเป็นข้อความอิสระที่สะกดต่างกันไปแต่ละคน
