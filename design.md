# Design system — GPO Xchange Portal

แนวทาง UI ที่ใช้อยู่: **Option B — Institutional Green** (ดูที่มา/ตัวเลือกอื่นในเอกสารเสนอทิศทางที่คุยกันไว้ก่อนหน้า) เก็บสีเขียวแบรนด์เดิมของระบบไว้ ตัดของตกแต่งที่ทำให้หน้าตาดูเป็นเทมเพลต AI ออก ใช้แล้วที่:

- [app/admin/manager/page.tsx](app/admin/manager/page.tsx) — hub
- [app/admin/sale/page.tsx](app/admin/sale/page.tsx) — hub
- [app/admin/sale/history/page.tsx](app/admin/sale/history/page.tsx)
- [app/admin/sale/workflow/page.tsx](app/admin/sale/workflow/page.tsx)
- [app/admin/sale/component/SaleActiveWorkflow.tsx](app/admin/sale/component/SaleActiveWorkflow.tsx)
- [components/StatCard.tsx](components/StatCard.tsx), [components/MiniStat.tsx](components/MiniStat.tsx) — component กลาง แก้ตรงตัว component แล้ว (ไม่ใช่แค่ prop สี) จึงส่งผลถึงทุกหน้าที่ใช้ร่วมด้วย (CSR, WH, Logistics, Manager staff-approvals) — ดูหมายเหตุท้ายไฟล์
- [app/admin/manager/tracking/page.tsx](app/admin/manager/tracking/page.tsx) — เคยพลาดตอนทำ Manager hub รอบแรก (ตัวหน้านี้ไม่ได้ผูกกับ hub โดยตรง) แก้ตามให้ตรงตอนทำ CSR tracking คู่แฝด

**โซน Manager ทำครบทุกหน้าย่อยแล้ว** (hub เคยทำไปก่อนหน้านี้แล้ว รอบนี้ไล่หน้าย่อยที่เหลือ):

- [app/admin/manager/sla/page.tsx](app/admin/manager/sla/page.tsx) — SLA Monitoring System ทั้ง 3 แท็บ (ภาพรวม/ตั้งค่ากฎ/Audit Trail) — `TAB_ACCENTS` เดิมมี 3 โทน (แดง/ม่วง/ทีล) พร้อม glow shadow + gradient bar ต่อแท็บ ลดเหลือ 2 กลุ่ม: `red` (destructive) สำหรับแท็บ "ภาพรวม SLA" เพราะเกี่ยวกับใบงานเกินกำหนดจริง กับ `neutral` (accent) สำหรับอีก 2 แท็บที่ไม่มีความหมายเชิงสถานะ — ตัด glow/gradient bar ออกหมด เหลือแท่งซ้าย 1px สีทึบ
- [app/admin/manager/staff-approvals/page.tsx](app/admin/manager/staff-approvals/page.tsx) — 4 แท็บ (จัดการสิทธิ์พนักงาน/ใบงานทั้งหมด/ภาพรวม & สถิติ/รายงานผู้บริหาร) — เดิม `TAB_ACCENTS` มี 4 โทน (indigo/violet/gold/teal) ทั้งที่ไม่มีแท็บไหนเป็นสถานะเชิงความหมายเลย ตัดออกทั้งหมดเหลือ accent เดียว, ปุ่ม/ไอคอนหมวดต่างๆ (จัดการสิทธิ์=ส้ม, ใบงานทั้งหมด=น้ำเงิน, รายงาน=ทีล, พอร์ตลูกค้า=อำพัน) รวมเป็น `bg-accent`/`text-accent-foreground` เดียวกันหมด
- [app/admin/manager/staff-approvals/component/ManagerInsights.tsx](app/admin/manager/staff-approvals/component/ManagerInsights.tsx) — dashboard กราฟ (ใช้ร่วมกับ `app/admin/csr/reports/page.tsx` ด้วย) — แก้เฉพาะ **UI chrome** (การ์ด wrapper, ไอคอนหัวข้อ, toggle ปุ่ม, font size/weight) ส่วน **สี chart ภายใน recharts ไม่แตะ** (`fill`, `stroke`, `tick.fill`, `contentStyle` ของ `<Tooltip>`) เพราะเป็น raw hex ที่ recharts SVG props ต้องการจริง ไม่ใช่ Tailwind class ได้ — ชุดสี categorical (`PALETTE`, `REQUEST_TYPE_COLORS`) เป็นของที่ถูกต้องแล้วสำหรับกราฟ (กราฟต้องการสีแยกแยะประเภทจริง ต่างจาก UI ตกแต่งที่ควรเหลือ accent เดียว)
  - **`accentBg`/`accentColor` ของการ์ดสถิติ (ไม่ใช่ตัวกราฟ)** — รอบแรกรวมเป็น `bg-accent` เดียวหมด (11 สีรุ้งเดิม → เขียวเดียว) แต่ผู้ใช้ feedback ว่าแบนไปสำหรับหน้า analytics โดยเฉพาะ ขอสีสันกลับมาบ้างแบบเดียวกับแถบ MiniStat ของ CSR — รอบสองจึงจัดกลุ่มสีตาม**ประเภทตัวเลข** แทนการสุ่มต่อการ์ด: `blue-100/blue-600` = ตัวเลขจำนวน/นับ (ใบงาน, ลูกค้าตามจำนวน, จังหวัดตามจำนวน), `emerald-100/emerald-600` = ตัวเลขมูลค่า/เงินบาท, `amber-100/amber-600` = เวลา/ระยะเวลา, `violet-100/violet-600` = หมวดหมู่เชิงเหตุผล (เหตุผลที่ลูกค้าส่งเรื่อง), `rose-100/rose-600` = ข้อมูลเกี่ยวกับยา, `bg-destructive/10`/`text-destructive` = การปฏิเสธ (คงเดิมจากรอบแรก) — **กติกา**: การ์ด analytics/stat-dashboard แบบนี้ยกเว้นจากกฎ "accent เดียว" ได้ ถ้าจัดกลุ่มสีตามความหมายของตัวเลข (ไม่ใช่สุ่มสีต่อการ์ดแบบเดิม) ต่างจาก tile นำทาง/action ปกติที่ยังต้องเป็น accent เดียวเหมือนเดิม

**โซน CSR ทำครบทุกหน้าแล้ว (hub + หน้าย่อยทั้งหมด รวมเนื้อหา ไม่ใช่แค่ topbar):**

- [app/admin/csr/page.tsx](app/admin/csr/page.tsx) — hub
- [app/admin/csr/dashboard/page.tsx](app/admin/csr/dashboard/page.tsx) — topbar + เนื้อหาทั้งหมด: stat cards, sidebar tabs, sub-tabs, ตารางใบงาน, Active Workflow board, 3 confirm modal
- [app/admin/csr/customers/page.tsx](app/admin/csr/customers/page.tsx) — topbar + ทั้ง 4 แท็บ (รออนุมัติ/ต่ออายุ/ค้นหา/Export) + เอกสารโมดัล
- [app/admin/csr/reports/page.tsx](app/admin/csr/reports/page.tsx) — topbar + filter bar + ตาราง + pagination (ไม่รวม `ManagerInsights` ที่ import มาใช้ — component แยกไฟล์ ยังไม่ได้แตะ)
- [app/admin/csr/chatbot/page.tsx](app/admin/csr/chatbot/page.tsx) — topbar + เนื้อหา
- [app/admin/csr/tracking/page.tsx](app/admin/csr/tracking/page.tsx) — ตัวหน้าเต็มรูปแบบ (topbar + wrapper) เนื้อหาหลักอยู่ใน `TrackingDetailView.tsx` ด้านล่าง

**โซน WH/Logistics ทำครบแล้ว** (ไม่มี hub ของตัวเอง — แต่ละแผนกมีแค่หน้า dashboard เดียว ไม่มี customers/reports/chatbot แบบ CSR):

- [app/admin/wh/dashboard/page.tsx](app/admin/wh/dashboard/page.tsx) — topbar + hero + local StatCard/TabButton + WHRequestList + RequestCard confirm/hint banner (ตัดอีโมจิ 🏭 ออก ใช้ไอคอน `Warehouse` แทน, ตัดตัวอักษรลูกศร "←" ออก ใช้ `ArrowLeft` แทน ให้ตรงกับปุ่มย้อนกลับหน้าอื่นทั้งระบบ) — เพิ่ม liveliness รอบหลังแล้ว (ดูหัวข้อ "ความมีชีวิตชีวา" ด้านล่าง)
- [app/admin/wh/dashboard/component/WHDrugrow.tsx](app/admin/wh/dashboard/component/WHDrugrow.tsx) — แถวรายการยา + 3 confirm modal (ตรวจรับ/จัดเก็บ/ปฏิเสธ) — `MODAL_META` เปลี่ยนจาก inline gradient string เป็น Tailwind class (`solidBg`) ตามหลักการเดียวกับที่ปรับ CSR dashboard modal
- [app/admin/logistics/dashboard/page.tsx](app/admin/logistics/dashboard/page.tsx) — topbar + hero + local StatCard/TabButton + LogisticsRequestList + ปุ่มส่งรถ (คงสีน้ำเงินไว้ตามเดิม เพราะ "ส่งรถ" ทำให้สถานะเข้ากลุ่ม in-transit ตรงกับ semantic "กำลังดำเนินการ=น้ำเงิน" อยู่แล้ว) + ตัดตัวอักษร "▾" ออก ใช้ `ChevronDown` แทน — เพิ่ม liveliness รอบหลังแล้ว (ดูหัวข้อ "ความมีชีวิตชีวา" ด้านล่าง)
- [app/admin/logistics/dashboard/component/LOGDrugrow.tsx](app/admin/logistics/dashboard/component/LOGDrugrow.tsx) — แถวรายการยา + confirm modal เดียว (ตรวจรับ/ปฏิเสธ)

พบบั๊กระหว่างแก้ WH dashboard: การลบ wrapper div ชั้นนอกของ confirm banner (Step 1b) โดยไม่ลบ closing tag คู่กัน ทำให้ JSX ไม่ balance (`tsc` จับได้ทันทีเป็น syntax error) — เป็นเหตุผลว่าทำไมต้องรัน `tsc --noEmit` หลังทุกการแก้ไขที่ลด/เพิ่มระดับ wrapper div ไม่ใช่แค่เปลี่ยน className
- [components/tracking/TrackingDetailView.tsx](components/tracking/TrackingDetailView.tsx) — component กลาง ใช้ร่วมกับหน้า Track & Trace ของ Manager **และหน้าติดตามสถานะของลูกค้าเอง** (`app/(authenticated)/customer/tracking/page.tsx`) ไฟล์นี้ไม่มี hex ผิดมาตรฐานอยู่แล้ว (เขียนดีมาก่อน) แก้แค่จุดเล็ก: gradient icon, `font-black`, `rounded-2xl/3xl`→`rounded-lg`, `shadow-sm/2xl`→ตัด/ลดเหลือ `shadow-lg` เฉพาะ modal, `text-[10px]`→`text-[11px]` — **ผลข้างเคียงที่ตั้งใจ**: หน้าติดตามสถานะฝั่งลูกค้าได้รับการแก้ไปด้วยจุดเล็กๆ เหล่านี้ แม้ยังไม่ได้ทำ Option B เต็มรูปแบบให้ทั้งโซนลูกค้า
- [components/skeletons/DashboardSkeleton.tsx](components/skeletons/DashboardSkeleton.tsx) — `SkeletonSidebarTabs`, `SkeletonSubTabs`, `SkeletonFilterBar`, `SkeletonTableRows`, `SkeletonSimpleRows`, `SkeletonTopBar`, `SkeletonStatCards`, และ default `bgClassName` ของ `StaffDashboardSkeleton` แก้ตามให้ตรง token ใหม่แล้วทั้งหมด (ใช้ร่วมกับ WH/Logistics ด้วย — ดูหมายเหตุท้ายไฟล์)

ไฟล์นี้คือ reference ตอนจะทำหน้าอื่นต่อ ให้ผลลัพธ์ออกมาสม่ำเสมอกันทั้งระบบ

## หลักการ

1. **Accent เดียวทั้งระบบ** — สีเขียวแบรนด์ (`--primary`) เท่านั้นที่ใช้เป็นของตกแต่ง/CTA แต่ละหน้า/แต่ละ tile ห้ามคิดสีของตัวเอง (เดิมเป็นปัญหาหลัก: หน้า manager ใช้ม่วง/ทอง/ทีล/แดงปนกัน, หน้า sale ใช้ indigo/ทอง/ส้ม/อำพัน — เห็นแล้วรู้ทันทีว่าไม่มีระบบ)
2. **สีเชิงความหมาย (semantic) แยกจาก accent** — สีที่สื่อสถานะจริง (รอ/เสร็จ/ปฏิเสธ/เกินกำหนด) ยังคงแยกสีได้ตามความหมาย ไม่ใช่ข้อยกเว้นของกฎข้อ 1 เพราะสีตรงนี้ "บอกอะไรบางอย่าง" ไม่ใช่การตกแต่ง — ดูตารางด้านล่าง
3. **ไม่มี gradient ตกแต่ง** — ห้ามใช้ `bg-gradient-to-br`, `linear-gradient(...)`, blob วงกลมลอยพื้นหลัง, noise texture ทุกจุดที่ไม่ได้สื่อความหมายอะไร (เคยมี 29 ไฟล์ใช้ gradient แบบนี้)
4. **Radius 2 ระดับเท่านั้น** — ไม่ไล่ `rounded-xl` → `rounded-3xl` แบบสุ่มอีกต่อไป
5. **เงาน้อยที่สุด** — ใช้ border 1px แทน `shadow-md/lg/xl/2xl` สีตามธีมการ์ด เงามีไว้เฉพาะ hover ของ tile ที่คลิกได้ (เปลี่ยนสี border ตอน hover ไม่ใช่เพิ่มเงา)
6. **Weight ตามลำดับชั้นจริง** — เลิกใช้ `font-black` เกร่อทุก label (เคยมี 157 จุด) เมื่อทุกอย่างหนักเท่ากันหมด ไม่มีอะไรเด่นจริง
7. **Animation มีความหมายเท่านั้น** — `animate-pulse`/`animate-ping` ใช้ได้เฉพาะจุดที่สื่อสถานะ live จริง (เช่น "Active" ของบัญชี) ไม่ใช่จุดตกแต่งเปล่าๆ (เช่น dot หน้าคำว่า "ยินดีต้อนรับ" — ตัดออก)
8. **Font size floor = 11px ไม่มีข้อยกเว้น** — ห้ามมี `text-[9px]`/`text-[10px]`/`text-[10.5px]` อีก ภาษาไทยต้องการพื้นที่แนวตั้งมากกว่าอังกฤษ (สระบน-ล่าง/วรรณยุกต์) ที่ขนาดต่ำกว่านี้อ่านไม่ออกจริงโดยเฉพาะมือถือ — ดูสเกลเต็มด้านล่าง
9. **Icon stroke width ตามขนาด** — ไอคอนหลัก/ใหญ่ (≥20px เช่น icon header, hero) ใช้ default lucide `2` ไม่ใส่ prop ก็ได้ — ไอคอนเล็กในชิป/badge/ปุ่มจิ๋ว (<16px) ใช้ `2.5` เพราะเส้นบางจะจางหายที่ขนาดเล็ก ห้ามสลับใช้ `2.25` ปนแบบสุ่ม
10. **ข้อยกเว้นกฎ accent เดียว: สี categorical ของกราฟ (recharts)** — ชุดสีที่ใช้แยกประเภทข้อมูลในกราฟ (`fill`/`stroke`/`tick.fill` ของแท่ง/เส้น/แกน, `PALETTE`/`REQUEST_TYPE_COLORS` ใน `ManagerInsights.tsx`) ไม่ต้องรวมเป็น accent เดียว เพราะกราฟต้องมีสีแยกแยะประเภทจริงถึงจะอ่านออก (ตรงข้ามกับ UI ตกแต่งที่ควรมีสีเดียว) และ recharts SVG props รับได้แค่ raw hex ไม่รับ Tailwind class อยู่แล้ว — ที่ต้องแก้คือแค่ **UI chrome รอบกราฟ** (การ์ด wrapper, ไอคอนหัวข้อ, toggle ปุ่ม) ไม่ใช่สีข้างในกราฟเอง
11. **ข้อยกเว้นกฎ accent เดียว: การ์ดสถิติในหน้า analytics** — ต่างจาก tile นำทาง/action ทั่วไปที่ต้อง accent เดียวเสมอ การ์ดสรุปตัวเลขในหน้าสถิติ (เช่น `ManagerInsights.tsx`) ใส่สีได้หลายโทน **ถ้าจัดกลุ่มตามความหมายของตัวเลข** ไม่ใช่สุ่มสีต่อการ์ด — ดูตัวอย่างการจัดกลุ่มที่ใช้จริงในหัวข้อแรกของไฟล์นี้ (บรรทัด ManagerInsights.tsx)
12. **Flat ไม่ได้แปลว่าไม่มีชีวิตชีวา** — หลังตัด gradient/เงาเยอะ/สีรุ้งออกหมดตามกฎ 1-6 ห้ามปล่อยให้หน้าดูเย็นชา ให้เพิ่มชีวิตชีวากลับด้วย hero glow วงเดียว + hero quick-stat + hover lift เฉพาะ element ที่คลิกได้จริง + เงาสีตามสีของ element เอง เท่านั้น (ไม่ใช้ gradient หลายสี/บล็อบสุ่มสี/glassmorphism) — ดูรายละเอียดเต็มที่หัวข้อ "ความมีชีวิตชีวา" ด้านล่าง

## Token (มาจาก `app/globals.css` เดิม ไม่ได้เพิ่มใหม่)

ทุกอย่างอิง CSS variable ที่ตั้งไว้แล้วในระบบ ไม่ hardcode hex ในหน้าใหม่อีก:

| Tailwind class | ใช้ทำอะไร |
|---|---|
| `bg-background` | พื้นหลังหน้า (paper neutral อุ่นๆ) |
| `bg-card` / `border-border` | พื้นการ์ด + เส้นขอบมาตรฐาน |
| `bg-primary` / `text-primary-foreground` | พื้นทึบสีเขียวแบรนด์ — ใช้กับ hero และปุ่ม/ลิงก์หลักเท่านั้น |
| `text-primary` | ข้อความ/ไอคอน accent บนพื้นขาว (CTA text, icon สีเดียวไม่มีกล่อง) |
| `bg-accent` / `text-accent-foreground` | ไอคอนสี่เหลี่ยมทึบ (เขียวอ่อน+เขียวเข้ม) แทนไอคอนไล่เฉด, ใช้กับ chip ข้อมูลทั่วไปที่ไม่ใช่สถานะ |
| `bg-secondary` / `text-muted-foreground` | พื้นที่ placeholder / disabled / "เร็วๆ นี้" |
| `border-destructive` / `bg-destructive/10` / `text-destructive` | เฉพาะสถานะเร่งด่วน/ผิดพลาดจริง (เช่น SLA เกินกำหนด) |

Radius: `rounded-md` (การ์ดย่อย/ไอคอน/ปุ่ม) และ `rounded-lg` (การ์ดหลัก) — ไม่ใช้ `rounded-xl` ขึ้นไป

## Type scale

| ระดับ | ขนาด | ใช้กับ |
|---|---|---|
| Micro label | `text-[11px]` (+ `uppercase tracking-wide` ถ้าเป็น section label) | ป้ายหมวด, label ใต้ตัวเลข, ชิป, badge ตัวเลขในวงกลมเล็ก |
| Meta/caption | `text-xs` (12px) | คำอธิบายใต้หัวข้อ, timestamp |
| Body | `text-sm` (14px) | ชื่อรายการ, ปุ่ม, หัวข้อ tile (`font-bold`) |
| หัวข้อใหญ่ | `text-2xl`/`text-3xl` | ตัวเลขสถิติเด่น, hero heading |

ไม่มีขนาดต่ำกว่า `text-[11px]` ในระบบอีก — ถ้า element เดิมมี padding/ขนาดกล่องแคบเกินจะไม่พอ (เช่นวงกลม badge ตัวเลข) ให้ขยายกล่องตาม ไม่ลดขนาดตัวอักษรกลับ

## สี semantic สำหรับสถานะใบงาน (ยกเว้นจากกฎ accent เดียว)

ใช้กับ `MiniStat`/สถิติย่อยที่นับตามสถานะจริงเท่านั้น ไม่ใช่กับ tile หรือ label ทั่วไป:

| สถานะ | สี |
|---|---|
| รอตรวจสอบ/รอดำเนินการ | `bg-amber-50` / `text-amber-600` |
| กำลังดำเนินการ | `bg-blue-50` / `text-blue-600` |
| เสร็จสิ้น | `bg-emerald-50` / `text-emerald-600` |
| ถูกปฏิเสธ | `bg-red-50` / `text-red-600` |
| เกินกำหนด SLA / แจ้งเตือน | `border-destructive`, `bg-destructive/10`, `text-destructive` |

ตัวเลข/รวม/ประเภทที่ไม่ใช่สถานะ (เช่น "ใบงานรวม", ชิปจังหวัด/ประเภทลูกค้า) → ใช้ `bg-accent`/`text-accent-foreground` เดียวกันหมด ไม่แยกสีตามหมวด

**ปุ่ม action ที่ทำให้สถานะเปลี่ยนไปสู่กลุ่มไหน ใช้สีของกลุ่มปลายทางนั้น** ไม่ใช่คิดสีใหม่ — พบใน CSR Dashboard: ปุ่ม "เริ่มแลกเปลี่ยน/เริ่มลดหนี้" (`ActionButton`) เดิมใช้ส้ม `#E1592A` (สีแบรนด์เก่าของ CSR ที่เลิกใช้แล้ว) แก้เป็นน้ำเงินแทน เพราะกดแล้วสถานะเข้าสู่กลุ่ม "กำลังดำเนินการ" ซึ่งใช้น้ำเงินอยู่แล้วในตาราง semantic ด้านบน — ส่วนปุ่ม "อนุมัติ"/"ปฏิเสธ"/"เสร็จสิ้น" ใช้ emerald/red/emerald ตรงไปตรงมาเพราะปลายทางคือ "เสร็จสิ้น"/"ถูกปฏิเสธ" อยู่แล้ว

## Pattern ต่อชนิด tile (bento grid hub pages)

**Hero tile (ทักทาย)** — `bg-primary` ทึบเต็ม ไม่มี noise/blob, dot สถานะเป็นจุดนิ่ง (ไม่ pulse), `font-bold` ไม่ใช้ `font-black`

**Tile สถานะบัญชี** — `bg-card border border-border`, จุด `animate-ping` สีเขียว (อันเดียวในทั้งหน้าที่ยอมให้มี animation เพราะสื่อสถานะ live จริง)

**Tile ลิงก์ทั่วไป (ไม่ featured)** — `bg-card border border-border`, ไอคอนสี่เหลี่ยม `bg-accent text-accent-foreground`, hover เปลี่ยนแค่สี border (`hover:border-primary/50`)

**Tile ลิงก์ featured/มีตัวเลขเด่น** — เหมือน tile ทั่วไป แต่เพิ่ม `border-l-[3px] border-l-primary` (แถบซ้าย) และไอคอนเป็น `bg-primary text-primary-foreground` แทน accent tint — ใช้แถบสีบอกความสำคัญแทนการทาสีทั้งใบแบบเดิม

**Tile placeholder ("เร็วๆ นี้")** — `border-dashed border-border`, `bg-secondary` สำหรับไอคอน/badge, `opacity-80`

**แถบไอคอน pipeline (ตกแต่งล้วน ไม่ใช่ stepper จริง)** — วงกลม `bg-accent`, ไอคอน `text-accent-foreground`, เส้นประคั่น `border-border` — เดิมเป็นสีเฉพาะ tile (indigo) ตอนนี้ใช้ accent เดียวกับทั้งหน้า

## Page container / topbar มาตรฐาน

พบว่า topbar ของหน้าหลัก (hub) กับหน้าย่อย (เช่น history/workflow ของ Sale) ขนาดไม่ตรงกัน เพราะแต่ละหน้าตั้ง `max-w-*`/padding/ขนาดปุ่มเองแยกกันตอนสร้างทีละหน้า ไม่ได้อ้างอิงกฎกลาง — กฎที่ใช้แก้ (ทำแล้วกับ Sale ทั้ง 3 หน้า):

- **Container**: `max-w-6xl mx-auto w-full` เดียวกันทุกหน้าในโซนเดียวกัน (hub และหน้าย่อยทั้งหมด) ไม่ว่าเนื้อหาข้างในจะเป็น bento grid, list, หรือ board ก็ตาม — ถ้าเนื้อหาแคบกว่าธรรมชาติ ปล่อยให้มีที่ว่างข้างเอง ไม่ลด container ให้แคบลงเฉพาะหน้า
- **Padding แนวนอน**: `px-4 md:px-6` ทุกหน้า (ไม่ใช่ `px-6` ตายตัวแบบที่ hub เคยใช้)
- **Padding แนวตั้งก่อน topbar**: `pt-6` เท่ากันทุกหน้า
- **ปุ่ม logout**: `px-4 py-2.5 text-sm` + ไอคอน `w-5 h-5` (สี่เหลี่ยมจัตุรัสเท่ากันทั้งกว้าง/สูง — เดิม hub มีบั๊ก `w-4 h-5` ที่ทำให้ไอคอนบิดเบี้ยว แก้แล้ว) ปุ่ม/ลิงก์ "ย้อนกลับ" ฝั่งซ้ายของหน้าย่อยก็ใช้ scale ข้อความเดียวกัน (`text-sm font-semibold`)

**ยังไม่ได้ตัดสินใจ**: topbar ของ hub มี `NotificationBell` อยู่ข้าง logout แต่หน้าย่อย (history/workflow) ไม่มี — ทำให้ฝั่งขวาของ topbar หน้า hub "กว้างกว่า" หน้าย่อยอยู่ดีแม้ปุ่ม logout จะขนาดเท่ากันแล้ว เป็นคำถามเชิง product (จะให้เห็นกระดิ่งแจ้งเตือนได้จากทุกหน้าย่อยด้วยไหม) ไม่ใช่แค่ layout เฉยๆ จึงยังไม่แก้เอง

### Topbar แบบ "sticky" (หน้าย่อยลึกที่ไม่ใช่ hub — CSR dashboard/customers/tracking/reports/chatbot)

หน้าเหล่านี้ใช้ topbar อีกแบบ ต่างจาก brand bar ของ hub — เป็นแถบบางลอยติดขอบบน (`sticky top-0 z-30`) มีแค่ปุ่ม "ย้อนกลับ" + หัวข้อ 2 บรรทัด + ปุ่ม/badge ฝั่งขวา ไม่ใช่โลโก้แบรนด์เต็ม — พบว่าหน้า CSR ทั้ง 5 มีโครง/ขนาดตรงกันอยู่แล้วเกือบหมด (icon `size={15}` `strokeWidth={2.5}`, ปุ่ม `px-3 py-2`, container `max-w-6xl`) มีจุดเดียวที่ไม่ตรง: **`chatbot/page.tsx` ใช้ `max-w-4xl`** ขณะที่อีก 4 หน้าใช้ `max-w-6xl` — แก้ให้ตรงกันแล้ว พร้อมแปลง `bg-white/70 backdrop-blur-xl border-white/50` (กระจกโปร่งแสง) → `bg-card border-border` (โทเคนกลาง) และ subtitle `text-[10px] md:text-[11px]` → `text-[11px]` ตาม floor เดียวกับกฎ font size ด้านบน

(อัปเดต: เนื้อหาใต้ topbar ของทั้ง 5 หน้าทำเสร็จตามมาในรอบถัดมาแล้ว — ดูหัวข้อแรกของไฟล์นี้)

## Footer / header มาตรฐาน

Header แบรนด์บนสุดของทุก hub: `bg-card border border-border rounded-lg`, โลโก้ `bg-primary text-primary-foreground rounded-md`, ปุ่ม logout `bg-background hover:bg-secondary border border-border`

Footer: `border-t border-border` เส้นเดียว ไม่มี gradient divider, ข้อความ `text-muted-foreground` ล้วน ยกเว้นชื่อระบบที่ตัวหนา `text-foreground`

สีเชิงความหมายเพิ่มเติมที่พบระหว่างทำหน้า Sale: **ป้ายแผนกเจ้าของงาน** (CSR/โลจิสติกส์/คลังสินค้า ใน `SaleActiveWorkflow.tsx`) นับเป็น semantic เหมือนสถานะ — อำพัน/ฟ้า/ม่วงต่อแผนก คงไว้ตามเดิม ไม่ต้องรวมเป็น accent เดียว

## ความมีชีวิตชีวา (liveliness หลัง flat design)

หลังทำ Option B เสร็จรอบแรก feedback คือดู "เย็นชา" (cold) เกินไป — flat design ที่ตัด gradient/เงา/สีรุ้งออกหมดทำให้ดูมืออาชีพขึ้นจริง แต่ขาดชีวิตชีวา แก้ด้วยเทคนิคที่**ไม่ย้อนกลับไปเป็น AI slop เดิม** (ไม่ใช้ gradient หลายสี/บล็อบสุ่มสี/glassmorphism) — ใช้ 4 เทคนิคนี้ซ้ำทุกโซนที่ทำแล้ว:

1. **Hero glow วงเดียว** — `<div className="absolute -top-16 -right-16 w-64 h-64 rounded-full bg-primary-foreground/10 blur-2xl pointer-events-none" />` ใน hero/welcome banner ที่เป็น `bg-primary` ทึบอยู่แล้ว สีเดียวจากพื้นเอง (opacity 10%) ไม่ใช่บล็อบหลายสีแบบ AI slop
2. **Hero quick-stat row** — แถวสถิติย่อยที่ดึงข้อมูลที่ fetch ไว้อยู่แล้วมาโชว์ใน hero (ไม่ query เพิ่ม) คั่นด้วย `border-t border-primary-foreground/15` ด้านล่างเนื้อหาทักทายเดิม + ปุ่ม/ลิงก์ CTA มุมขวา ให้ hero ไม่ดูโล่ง
3. **Hover shadow + lift บน tile/element ที่คลิกได้จริงเท่านั้น** — `hover:shadow-md hover:-translate-y-0.5 transition-all duration-200` (hub tiles) หรือ `shadow-sm` เฉยๆ สำหรับ element เล็กในลิสต์แน่น (เช่น sidebar tab ของ WH/Logistics ที่ไม่มีที่ให้ยกตัวขึ้นเยอะ) — **สำคัญ**: ห้ามใส่กับ element ที่ไม่ได้คลิกได้จริง (เช่น StatCard สรุปตัวเลขของ WH/Logistics เป็น display-only ไม่มี `onClick` จึงไม่ใส่ hover effect เพราะจะเป็น false affordance)
4. **เงาสีตามสีของ element เอง** — ไม่ใช่เงาเทาทึบมาตรฐาน แต่ผูกกับสีไอคอนนั้นเอง เช่น `shadow-primary/30`/`shadow-accent/40` (ไอคอนที่ใช้สี token) หรือ `shadow-amber-400/30`/`shadow-blue-400/30`/`shadow-indigo-400/30` (ไอคอนที่ใช้สี semantic ของ WH/Logistics ตามตารางสถานะด้านบน) — ห้ามผสมสีเงากับสีไอคอนที่ต่างกัน

ใช้แล้วที่:

- [app/admin/sale/page.tsx](app/admin/sale/page.tsx) — hero glow + hover lift บน 2 featured card — quick-stat row เดิมโชว์ total/active ซ้ำกับป้ายตัวเลขบนการ์ด "Active Workflow"/"ประวัติใบงาน" ด้านล่างเป๊ะๆ (ผู้ใช้ทักว่าดูแปลก เห็นเลขชุดเดียวกันวนซ้ำ 3 รอบ) แก้เป็นสถิติผลงานแทน: **เสร็จสิ้นแล้ว** (`completed`) + **อัตราปิดงานสำเร็จ** (`completed/total`) — มุมมองที่การ์ดด้านล่างไม่มี ใช้ `counts` ชุดเดียวกันที่ fetch ไว้อยู่แล้ว ไม่ query เพิ่ม — **หลักการทั่วไป**: เวลาเพิ่ม hero quick-stat ห้ามซ้ำกับตัวเลขที่ขึ้นอยู่แล้วบน tile/การ์ดอื่นในหน้าเดียวกัน ให้เลือกมุมมองใหม่ที่มีอยู่ใน state ที่ fetch ไว้แล้วแทน
- [app/admin/sale/history/page.tsx](app/admin/sale/history/page.tsx), [app/admin/sale/workflow/page.tsx](app/admin/sale/workflow/page.tsx) — เงาสีบนไอคอนหัวข้อ (`shadow-accent/40`)
- [app/admin/sale/component/SaleActiveWorkflow.tsx](app/admin/sale/component/SaleActiveWorkflow.tsx) — `WorkflowCard` เพิ่ม hover state ที่ไม่เคยมีมาก่อนทั้งที่คลิกได้ (`hover:border-primary/30 hover:shadow-sm`)
- [app/admin/manager/page.tsx](app/admin/manager/page.tsx), [app/admin/csr/page.tsx](app/admin/csr/page.tsx) — hero glow + quick-stat row (ใบงานรวม/รอดำเนินการ) + hover lift บน tile ทั้งหมด (ยกเว้น tile placeholder "Download Center" ของ CSR ที่ตั้งใจปิดใช้งานอยู่)
- [app/admin/wh/dashboard/page.tsx](app/admin/wh/dashboard/page.tsx) — hero glow + quick-stat row (ใบงานรวมทั้งหมด/รอตรวจรับ + ปุ่มไปแท็บ "รอตรวจรับ") + เงาสี `shadow-accent/40` บนไอคอนหัวข้อ "Warehouse Operations" + `WHTabButton` เพิ่ม prop `accentShadow` ใส่เงาสีตามสีแท็บ (`shadow-amber-400/30`/`shadow-blue-400/30`) ตอน active เท่านั้น
- [app/admin/logistics/dashboard/page.tsx](app/admin/logistics/dashboard/page.tsx) — hero glow + quick-stat row (ใบงานรวมทั้งหมด/อยู่ระหว่างขนส่ง + ปุ่มไปแท็บ "อยู่ระหว่างขนส่ง") + เงาสีบนไอคอนหัวข้อของ 2 แท็บ (`shadow-blue-400/30`/`shadow-indigo-400/30`) + `LogTabButton` เพิ่ม prop `accentShadow` เหมือนกับ `WHTabButton`

## หน้ากลาง "จัดการบัญชี" (/admin/account)

หน้าใหม่ใช้ร่วมกันทุกแผนก (ไม่ผูกกับ zone ไหน) ให้พนักงาน self-service เปลี่ยน Username/อีเมล/รหัสผ่านของบัญชีตัวเองได้ — เข้าถึงจากการ์ด "บัญชีผู้ใช้" ที่การ์ดคู่ "สถานะบัญชี" ของแต่ละ hub (ตอนนี้ทำแค่ CSR ก่อนตามที่ขอ ยังไม่ได้ผูกจาก Manager/Sale/WH/Logistics):

- [app/admin/account/page.tsx](app/admin/account/page.tsx) — 3 ฟอร์มแยก (Username/อีเมล/รหัสผ่าน) ทุกฟอร์มต้องกรอกรหัสผ่านปัจจุบันเพื่อยืนยันตัวตนก่อนบันทึกเสมอ (self-service ทันที ไม่ต้องรอ manager อนุมัติ ตามที่ผู้ใช้เลือก) — ปุ่ม "ลืมรหัสผ่านปัจจุบัน?" ลิงก์ไปหน้า `/auth/staff-forgot-password` (flow OTP เดิม ไม่ได้สร้างใหม่) — container ใช้ `max-w-6xl` เท่าทุกหน้าสำหรับแถบ back/logout บนสุด แต่เนื้อหาฟอร์มห่อด้วย `max-w-2xl` แคบกว่า เพราะเป็นฟอร์มล้วนไม่ใช่ list/table (ข้อยกเว้นจากกฎ container กว้างเท่ากันทั้ง zone เพราะหน้านี้ไม่ได้อยู่ใน zone ไหน)
- [app/admin/account/layout.tsx](app/admin/account/layout.tsx) — server component เช็ค `getStaffSession()` แล้ว `redirect('/')` ถ้าไม่ได้ login (**ไม่เช็ค department** ต่างจาก layout.tsx ของ csr/manager/sale/wh/logistics ที่ล็อกเฉพาะแผนกตัวเอง เพราะหน้านี้ใช้ร่วมได้ทุกแผนก) — จุดที่พลาดตอนแรก: สร้างหน้าไว้แล้วลืมใส่ layout guard นี้ ทำให้ route ตอบ 200 แทนที่จะ 307 เหมือนหน้า `/admin/*` อื่นทั้งหมด (เช็ค route ด้วย curl เจอเอง ไม่ใช่ security hole จริงเพราะ server action ทุกตัวยัง authenticate ซ้ำเองอยู่แล้ว แต่ behavior ไม่สอดคล้องกับหน้าอื่น)
- [app/actions/auth-staff.ts](app/actions/auth-staff.ts) — เพิ่ม `updateStaffUsername`/`updateStaffEmail`/`updateStaffPassword` แยกจาก `requestStaffPasswordReset`/`resetStaffPassword` เดิม (ฝั่งนั้นคือ "ลืมรหัสผ่าน" ยืนยันด้วย OTP, ฝั่งนี้คือ self-service ยืนยันด้วยรหัสผ่านปัจจุบันโดยตรง) ทั้ง 3 ฟังก์ชัน authenticate ผ่าน `getStaffSession()` เอง ไม่รับ staffId จาก client กัน แก้บัญชีคนอื่น, ใช้ rate-limit bucket ร่วมกัน (`staff-account-update:{staffId}`) กันเดารหัสผ่านผ่านฟังก์ชันไหนก็ได้เกินงบรวม — `updateStaffPassword` revoke session อื่นทั้งหมด "ยกเว้น session ปัจจุบัน" (ต่างจาก `resetStaffPassword` ที่ revoke ทุก session รวมของตัวเองด้วย เพราะยืนยันตัวตนใหม่ผ่าน OTP ไม่ใช่ session เดิม)
- `staff_account_change_logs` (ตาราง DB ใหม่ ผ่าน migration `supabase/migrations/20260822160000_add_staff_account_change_logs.sql` — apply แล้วผ่าน Supabase MCP) — audit log แยกจาก `staff_password_reset_logs` เก็บทุกครั้งที่แก้บัญชีสำเร็จ (`field`, `old_value`/`new_value` สำหรับ username/email เท่านั้น — `password` ไม่เก็บค่าใดๆ แม้แต่ hash กัน risk ถ้าตารางรั่ว) RLS ล็อกเหมือนตาราง audit อื่น (`deny_client_access` — เขียน/อ่านได้เฉพาะ service_role)
- [test/fakeSupabase.ts](test/fakeSupabase.ts) — ขยาย fake ให้ `.update()` เช็ค unique-constraint (23505) ด้วยเหมือน `.insert()` (เดิมเช็คแค่ insert) เพราะ `updateStaffUsername` ต้องมี test path ที่จำลอง username ซ้ำได้จริงตอน update ไม่ใช่แค่ตอนสมัครใหม่ — เพิ่ม `staff_users: ['username']` เข้า `UNIQUE_COLUMNS`
- test ครบทั้ง 3 ฟังก์ชันใน `app/actions/__tests__/auth-staff.test.ts` (rate limit, validation, wrong password, duplicate username, session revocation, audit log) ตามธรรมเนียม "ทุก server action ต้องมี test coverage" ของ repo นี้

## โครงหน้า Logistics Dashboard ปรับใหม่ (เลิกใช้ tab แยกเป็นหน้าย่อยจริง)

ผู้ใช้ขอปรับ [app/admin/logistics/dashboard/page.tsx](app/admin/logistics/dashboard/page.tsx) จากระบบ sidebar tab (3 แท็บ: อนุมัติรับคืนสินค้า/อยู่ระหว่างขนส่ง/อัปโหลดรูป สลับดูทีละแท็บบนหน้าเดียว) เป็นหน้ากลางแบบ hub เล็กๆ 2 การ์ด **กดเข้าไปเจอเนื้อหาที่หน้าแยกจริง** (ตอนแรกลองทำเป็น 2 การ์ดหัวข้อ static โชว์เนื้อหาเต็มพร้อมกันในหน้าเดียว แต่ผู้ใช้แก้ไขว่าไม่ใช่ ต้องการให้กดการ์ดแล้ว "เด้งไปหน้าอื่น" แบบเดียวกับที่ Sale hub ลิงก์ไปหน้า history/workflow):

1. **หัวข้อสถิติใน Welcome Banner** ([app/admin/logistics/dashboard/page.tsx](app/admin/logistics/dashboard/page.tsx)) — เพิ่ม "อนุมัติรับคืนสินค้า" แทรกก่อน "อยู่ระหว่างขนส่ง" ปุ่ม CTA "ดูรายการขนส่ง" เปลี่ยนจากสลับ tab เป็น `Link` ไปหน้า `/admin/logistics/dashboard/in-transit` ตรงๆ
2. **ตัดสถิติสรุปย่อ 3 การ์ด + sidebar tab เดิมออกทั้งหมด** — ลบ component `StatCard`/`LogTabButton` (ไม่มีที่ใช้แล้ว เคยมีไว้ไฮไลต์ tab ที่ active) พร้อม state `activeTab`/`expandedReq`/`transitModal` ทั้งชุดออกจากหน้ากลาง (ย้ายไปอยู่ที่หน้าย่อยตามเนื้อหาที่ใช้จริงแทน) หน้ากลางเหลือแค่ fetch ตัวเลขสรุปมาโชว์ hero + การ์ด ไม่ต้องมี state ของ modal/รายการใบงานอีก
3. **หน้าย่อยใหม่ 2 หน้า** ลิงก์จากการ์ด "ส่งรถไปรับคืนสินค้า" และ "รถขนส่งรับคืนสินค้าถึงคลัง" บนหน้ากลาง (การ์ดสไตล์เดียวกับ tile ของ Sale/CSR hub: icon เหลี่ยม + ตัวเลขนับมุมขวาบน + `border-l-[3px]` สีตามความหมายสถานะ + hover shadow+lift):
   - [app/admin/logistics/dashboard/approved/page.tsx](app/admin/logistics/dashboard/approved/page.tsx) — "ส่งรถไปรับคืนสินค้า" (เดิมแท็บ "อนุมัติรับคืนสินค้า") มี modal "ยืนยันส่งรถไปรับคืน" อยู่ที่นี่ (ทำงานเฉพาะรายการ `approved`)
   - [app/admin/logistics/dashboard/in-transit/page.tsx](app/admin/logistics/dashboard/in-transit/page.tsx) — "รถขนส่งรับคืนสินค้าถึงคลัง" รวมเดิม 2 แท็บเข้าด้วยกันตามที่ขอ ("อยู่ระหว่างขนส่ง" + "อัปโหลดรูปสินค้ารับคืน") อยู่การ์ดเดียวกัน คั่นด้วย `border-t` — placeholder อัปโหลดรูปยังเป็น "กำลังพัฒนา" เหมือนเดิมทุกประการ ไม่ได้แตะ logic
   - แต่ละหน้าย่อย fetch ข้อมูลของตัวเองอิสระ (เรียก `getLogisticsDashboardData()` ซ้ำแล้วกรองฝั่ง client เอง เหมือน pattern Sale history/workflow ที่ไม่ผูก state ร่วมข้ามหน้า) — โครง topbar ใช้แบบ "bar เดียวไม่ sticky" ของ `app/admin/sale/history/page.tsx` (ไม่ใช่แบบ sticky ของหน้า dashboard เดิม)
   - [app/admin/logistics/layout.tsx](app/admin/logistics/layout.tsx) (auth guard เช็ค `session.department === 'log'`) ครอบคลุมหน้าย่อยใหม่ทั้ง 2 อัตโนมัติอยู่แล้วเพราะ Next.js layout ใช้กับทุก route ที่ซ้อนอยู่ข้างใต้ ไม่ต้องสร้าง layout ใหม่

**component กลางที่แยกออกมา**: [app/admin/logistics/dashboard/component/LogisticsRequestList.tsx](app/admin/logistics/dashboard/component/LogisticsRequestList.tsx) — ย้าย `LogisticsRequestList`/`StatusBadge`/`LOGISTICS_STATUS`/pagination ออกจาก page.tsx เดิมมาไว้ตรงนี้ ให้ 2 หน้าย่อยเรียกใช้ร่วมกันได้ (เดิมอยู่ไฟล์เดียวกับหน้า dashboard เพราะใช้แค่ที่เดียว) — component นี้**ไม่มี wrapper `<section>` ของตัวเองอีกต่อไป** (ถอดออกจากตอนที่ยังลองทำเป็นการ์ดสแตติกในหน้าเดียว กันซ้อนกรอบกับการ์ดของหน้าย่อยที่ห่อเองแล้ว) — **หลักการทั่วไป**: ก่อนยุบ component ที่มี wrapper ของตัวเองมาไว้ "ข้างใน" การ์ดอื่น ต้องเช็คว่า component นั้น render กรอบ/พื้นหลังซ้อนกับกรอบใหม่หรือไม่ ถ้าใช่ต้องถอดออกก่อน

หน้า "รถขนส่งรับคืนสินค้าถึงคลัง" ไม่มีปุ่ม "ส่งรถไปรับคืน" เลย (ปุ่มนั้นทำงานเฉพาะ `req.current_status === 'approved'` ซึ่งหน้านี้กรองเฉพาะ `in_transit` อยู่แล้ว) ส่ง `onSendTruck` เป็น no-op function เข้าไปเพราะ prop เป็น required แต่ปุ่มจะไม่แสดงผลจริงอยู่ดี

เพิ่มการ์ดคู่ "สถานะบัญชี + บัญชีผู้ใช้" ให้หน้ากลาง (`app/admin/logistics/dashboard/page.tsx`) ด้วย ยกโครงมาจาก CSR hub ตรงๆ (การ์ดสถานะ `flex-1` + การ์ด username กว้างคงที่ `w-28 md:w-32` เรียงชิดกันทุกขนาดจอ ลิงก์ไป `/admin/account`) — หน้านี้เดิมไม่เคยมีการ์ดแบบนี้เลย (มีแค่ป้าย "Active" เล็กๆ ฝังอยู่มุมขวาบนของ hero ไม่มีนาฬิกาเข็ม) ตอนเพิ่มการ์ดใหม่เลยตัดป้ายเดิมในตัว hero ออกด้วย (ซ้ำซ้อนกันถ้าเก็บไว้ทั้งคู่) พร้อมเพิ่ม `now`/`AnalogClock` state ที่หน้านี้ไม่เคยมีมาก่อน (WH ก็มีป้ายแบบเดียวกันฝังใน hero โดยยังไม่ได้ทำตามในรอบนี้ รอ request เพิ่มเติม)

รอบถัดมา: ผู้ใช้ส่งภาพหน้า CSR มาเทียบ อยากให้ "ขนาดและจัดเรียง" ของ hero หลัก log เหมือน CSR คือ hero กับการ์ดคู่สถานะบัญชี/บัญชีผู้ใช้ **อยู่แถวเดียวกัน ข้างๆ กัน** (ไม่ใช่การ์ดคู่ตกลงมาเป็นแถวแยกด้านล่าง hero แบบที่เพิ่งทำไปรอบก่อน) แก้โดยห่อทั้งสองด้วย `grid grid-cols-1 md:grid-cols-6 gap-4` (grid 6 คอลัมน์เหมือนโครง bento ของ CSR/Manager/Sale hub) hero ได้ `md:col-span-4`, การ์ดคู่ได้ `md:col-span-2` — **ต่างจาก CSR ตรงที่**: CSR hero เป็น `md:row-span-2` เพราะมี tile ที่ 2 ("กรอกแบบฟอร์มแทนลูกค้า") ต่อคิวใต้การ์ดคู่สถานะ/username ให้ความสูงรวมของฝั่งขวาเท่า hero พอดี — หน้า Logistics นี้ไม่มี tile ที่ 2 แบบนั้นในบริเวณเดียวกัน (มีแค่การ์ดคู่เดียว) เลยปล่อยเป็นแถวเดียว ไม่ใส่ row-span ให้ hero — ผลคือ grid default (`align-items: stretch`) จะยืดการ์ดคู่ให้สูงเท่า hero เอง ซึ่งยอมรับได้เพราะเนื้อหาในการ์ดใช้ `items-center justify-center` อยู่แล้ว (ต่างจากบั๊ก stretch ที่เจอตอนแก้ Sale ตรงที่การ์ดนั้นมีไอคอนตรึงตำแหน่งตายตัวแยกจากเนื้อหา ไม่ใช่กลุ่มเดียว centered)

## Sale "ศูนย์รายงาน (Report Center)" — ต่อยอดจาก placeholder เป็นหน้าจริง

ผู้ใช้ขอให้ทำหน้านี้ต่อ (เดิมเป็น placeholder "เร็วๆ นี้" ใน hub) ให้แสดงสถิติแบบเดียวกับที่ทำให้ Manager/CSR แล้ว **แต่กรองเฉพาะลูกค้าของ sale คนนั้นๆ ตาม `org_type` + `sale_provinces` เดิม (logic เดิมเลย ไม่แตะ)**:

- [app/admin/sale/reports/page.tsx](app/admin/sale/reports/page.tsx) — ก็อปโครงมาจาก [app/admin/csr/reports/page.tsx](app/admin/csr/reports/page.tsx) เกือบทั้งหมด (filter bar วันที่/สถานะ/ประเภท/ค้นหา + `ManagerInsights` + ตารางรายการ + pagination) ต่างแค่แหล่งข้อมูล: `getSaleCustomerHistory()`/`getSaleStatusLogs()` แทน `getCSRDashboardData()`/`getManagerStatusLogs()` และปุ่มย้อนกลับไป `/admin/sale`
- [app/admin/sale/reports/export/route.ts](app/admin/sale/reports/export/route.ts) — Export Excel เหมือน CSR ทุกประการ (3 sheet: สรุปภาพรวม/รายการใบงาน/รายการยา) ต่างแค่ auth gate เป็น `getSaleCoverage()` (เช็ค org_type/sale_provinces จริง ไม่ใช่แค่ department) และแหล่งข้อมูลเหมือนหน้าเว็บ
- [app/actions/sale-actions.ts](app/actions/sale-actions.ts) — เพิ่ม `getSaleStatusLogs()` (คู่แฝด `getManagerStatusLogs()` ใน manager-actions.ts แต่เกตด้วย `assertDepartmentAccess(session, 'sale')` แทน — ไม่แก้ตัวเดิมของ Manager/CSR เพราะ `assertDepartmentAccess` เช็คได้แค่ 1 department ต่อครั้ง ไม่อยากขยาย gate เดิมให้ sale เข้าถึงฟังก์ชันอื่นที่ผูกกับ `getManagerOrCsrSession()` ไปด้วยโดยไม่ตั้งใจ เช่น chatbot unanswered questions ของ CSR)
- Migration [supabase/migrations/20260823000000_widen_get_sale_customer_history_for_report_center.sql](supabase/migrations/20260823000000_widen_get_sale_customer_history_for_report_center.sql) — เพิ่ม `r.updated_at`/`r.return_reason` เข้า `get_sale_customer_history` RPC (เดิมไม่ select เพราะตอนแรกแค่ต้องการโชว์ประวัติ ไม่ได้ทำสถิติเชิงลึก) **ไม่แตะ logic กรอง org_type/province เดิมเลย** — apply ขึ้น Supabase project จริงแล้วผ่าน MCP

**ปัญหา type ที่เจอและวิธีแก้**: `ManagerInsights`/`computeManagerStats` (lib/manager-stats.ts) เดิม type เข้มเป็น `RequestRow[]`/`DrugItemRow[]` เต็มรูปแบบ (`select('*')` ของ CSR/Manager) แต่ RPC ข้ามแผนกของ Sale (join ผ่าน organizations) select เฉพาะคอลัมน์ที่จำเป็นเท่านั้น ไม่ครบตาม RequestRow (ไม่มี `phone`, `contact_name`, `exchange_product` ฯลฯ) — แก้ด้วยการเพิ่ม type ย่อ `ReportRequestRow`/`ReportDrugItemRow` ใน [lib/types.ts](lib/types.ts) (ใช้ `Pick<>` ดึงมาจาก RequestRow/DrugItemRow เฉพาะ field ที่ ManagerInsights ใช้จริง) แล้วเปลี่ยน `ManagerInsights`, `computeManagerStats`, `summarizeManagerStatsForChatbot`, `filterCsrRequests` ให้รับ type ย่อนี้แทน (บาง function ทำเป็น generic `<T extends ReportRequestRow>` เพื่อคง type เดิมไว้ที่ output ไม่กระทบ caller เดิม) — **RequestRow เต็มรูปแบบยัง assignable เข้า `ReportRequestRow` ได้เสมอ** (เป็น superset ของ field ที่ต้องใช้) จึง **ไม่กระทบ CSR/Manager/staff-chat bot ที่ใช้ `RequestRow[]` เดิมอยู่แล้วเลย** — **หลักการทั่วไป**: เวลาจะ reuse component/lib ที่ type เข้มกับ full-table shape ให้ข้ามแผนกที่ query แบบจำกัดคอลัมน์ (join ผ่าน view/RPC เฉพาะ) อย่าไปเติม field ปลอมเป็น `null` ให้ตรง type เดิม (โกหก shape ข้อมูล) ให้สร้าง type ย่อยที่ตรงกับสิ่งที่ใช้จริงด้วย `Pick<>` แทน แล้วให้ type เต็มรูปแบบ widen ลงมาเอง

Sale hub tile "ศูนย์รายงาน" ([app/admin/sale/page.tsx](app/admin/sale/page.tsx)) เปลี่ยนจาก placeholder (`border-dashed`, badge "เร็วๆ นี้") เป็นการ์ดลิงก์จริงสไตล์เดียวกับ "ประวัติใบงาน"/"Active Workflow" ข้างๆ — ตั้งใจไม่ใส่ป้ายตัวเลขมุมขวาบนเหมือน 2 การ์ดนั้น เพราะจะไม่มีตัวเลขใหม่ที่ไม่ซ้ำกับ `counts.total` ที่โชว์อยู่แล้ว (บทเรียนเดิมจาก hero quick-stat ที่เคยซ้ำกัน)

## Skeleton สำหรับ ManagerInsights (กราฟ/สถิติ) — CSR, Manager, Sale

เดิม skeleton ตอนโหลดหน้าของ "ศูนย์รายงาน" (CSR/Sale) และแท็บ "ภาพรวม & สถิติ" (Manager) มีแค่ topbar/filter/ตาราง ไม่มีโครงของส่วนกราฟ/สถิติ (`ManagerInsights.tsx`) เลย ทำให้พอโหลดเสร็จเนื้อหากราฟทั้งก้อน "โผล่" แทรกกลางหน้าแบบกระโดด (shape jump) — เพิ่ม `SkeletonManagerInsights` ใน [components/skeletons/DashboardSkeleton.tsx](components/skeletons/DashboardSkeleton.tsx) แก้:

- โครง: หัวข้อ + การ์ดสถิติ 2 แถว (4 ใบ/แถว ตรงกับ `grid-cols-2 lg:grid-cols-4` ของจริง) + กราฟ 6 บล็อก (`SkeletonChartCard` — สูงปรับได้ต่อบล็อก ประมาณสัดส่วนกราฟจริงคร่าวๆ) — **ไม่ได้ไล่ทำครบทุกกราฟจริง (มี 9 ใบ)** เพราะไม่คุ้มต้องตามแก้ทุกครั้งที่ `ManagerInsights.tsx` เพิ่ม/ลดกราฟ แค่ให้จังหวะ/สัดส่วนใกล้เคียงพอไม่รู้สึกกระโดด
- ใช้ที่ [app/admin/csr/reports/page.tsx](app/admin/csr/reports/page.tsx) และ [app/admin/sale/reports/page.tsx](app/admin/sale/reports/page.tsx) — แทรกระหว่าง `SkeletonFilterBar` กับ `SkeletonTableRows`
- ใช้ที่ [app/admin/manager/staff-approvals/page.tsx](app/admin/manager/staff-approvals/page.tsx) **แบบมีเงื่อนไข**: `activeTab === 'insights' ? <SkeletonManagerInsights /> : <SkeletonSimpleRows rows={4} />` เพราะหน้านี้มีหลายแท็บ ต้อง skeleton ให้ตรงกับแท็บที่จะโชว์จริงหลังโหลดเสร็จ (`activeTab` รู้ค่าแล้วตั้งแต่ก่อนเริ่ม fetch เพราะอ่านจาก URL `?tab=`) — Suspense fallback ชั้นนอกสุดของไฟล์เดียวกัน (ก่อน mount, ยังไม่รู้ `activeTab`) ตั้งใจปล่อยเป็น `SkeletonSimpleRows` แบบเดิม ทำให้ tab-aware ไม่ได้อยู่แล้ว

## Toast notification แทน alert() ทั้งระบบ

ผู้ใช้ขอให้เลิกใช้ `alert()`/`window.alert()` (browser-native dialog บล็อกทั้งหน้าจอ ดูหลุดธีม) เปลี่ยนเป็น toast แบบเดียวกันทั้งแอป — แทนที่ครบทั้ง **~63 จุด ใน 19 ไฟล์** (ทั้งฝั่ง staff portal และฝั่งฟอร์มลูกค้า):

- [components/ui/toast.tsx](components/ui/toast.tsx) — hand-roll เอง (ไม่ดึง sonner/react-hot-toast เพิ่ม เพราะ UI primitive อื่นในระบบ เช่น `Skeleton`/`PasswordInput` ก็ทำเองทั้งหมดอยู่แล้ว) `ToastProvider` (Context) + `useToast()` hook คืน `{ success, error }` — ตั้งใจไม่ทำ generic `toast()`/`info` variant เพราะทุกจุดที่แทน `alert()` แบ่งได้แค่ 2 กลุ่มพอดี (validation/error message ↔ success confirmation)
- Toast การ์ดสไตล์ตรงกับ token ของระบบ (`bg-card`/`border`/`rounded-lg` เหมือน modal อื่น, `border-l-[3px]` สีเขียว/แดงตามชนิด, icon `CheckCircle2`/`XCircle`) ซ้อนกันเป็น stack (toast ใหม่ล่าสุดอยู่บนสุด), dismiss เอง 5 วิ + กดปิดเองได้ — `z-[100]` สูงกว่า modal (`z-50`) เพราะบางจุด modal ปิดพร้อม toast ผลลัพธ์โผล่พร้อมกัน
  - **ตำแหน่ง — responsive**: บนขวา (`top-right`) บนจอ `md` ขึ้นไป, บนกลาง (`top-center`) บนมือถือ — เดิมลองมุมล่างขวา (`bottom-right`) ก่อนตามธรรมเนียมทั่วไป แต่ผู้ใช้ทักถามแล้วพบปัญหาจริงระหว่างหารือ: (1) ชนกับ `BottomNav.tsx` ที่ CSR/Manager hub มี `fixed bottom-0` อยู่แล้วบนมือถือ (2) คีย์บอร์ดเสมือนตอนกรอกฟอร์มบนมือถือบังของที่อยู่ล่างสุดจอ — ย้ายขึ้นบนแก้ทั้งสองจุด ส่วนมือถือใช้ top-center แทน top-right เพราะขวาบนของหลายหน้ามีปุ่ม/กระดิ่งแจ้งเตือนอยู่แล้ว (`left-1/2 -translate-x-1/2` ที่ base, `md:left-auto md:translate-x-0 md:right-4` ที่ md+)
  - **จุดที่พลาดตอนแรก**: วาง `top-4` เฉยๆ ตอนย้ายขึ้นบนรอบแรก แล้วไปทับ header กลาง 56px ที่ครอบทุกหน้าจาก `app/layout.tsx` (โลโก้ GPO + "สาขาภาคใต้") — เจอจากการทดสอบจริงในเบราว์เซอร์ ไม่ใช่แค่ดู code แล้วเดา แก้เป็น `top-[72px]` (56px header + 16px เว้นระยะ)
  - **สังเกตระหว่างทาง (ยังไม่ได้แก้)**: หน้า `/admin/*` มี topbar ของตัวเองอยู่แล้ว (เช่น sticky topbar ของ CSR dashboard) ซ้อนอยู่ใต้ header กลางตัวนี้อีกชั้น (ทุกหน้าถูก `app/layout.tsx` ครอบเป็น root layout เดียวทั้งระบบ ไม่มี route group แยก) กลายเป็น 2 header ซ้อนกัน — เป็นเรื่องที่มีอยู่ก่อนแล้ว ไม่เกี่ยวกับ toast แต่ควรรู้ไว้เผื่อพบปัญหา clearance ของ toast กับ topbar เฉพาะหน้าในอนาคต
- Mount ที่ [app/layout.tsx](app/layout.tsx) (root layout เดียว ครอบทั้งฝั่งลูกค้า+staff) ห่อ `{children}` ทั้งหมดไว้ใน `<ToastProvider>`
- ทุกไฟล์ที่แก้: เพิ่ม `import { useToast } from '@/components/ui/toast'` + `const toast = useToast();` ในตัว component (บางไฟล์มีมากกว่า 1 component ย่อยที่เรียก `alert()` เช่น `OrgTypeEditor` ใน csr/customers/page.tsx ต้องเรียก `useToast()` แยกของตัวเองเพราะเป็นคนละ component scope) แล้วเปลี่ยน `alert(X)` → `toast.error(X)` (error/validation) หรือ `toast.success(X)` (สำเร็จ)
- **ตัด prefix ภาษาอังกฤษทั่วไปออก** (`'Error: ' + ...`, `"เกิดข้อผิดพลาด: " + ...`) เพราะ toast มีสี/ไอคอนบอกอยู่แล้วว่าเป็น error ไม่ต้องพิมพ์ซ้ำ — **แต่ prefix ภาษาไทยที่มีความหมายเฉพาะเจาะจง** (เช่น `'บันทึกไม่สำเร็จ: '`, `'ยืนยันไม่สำเร็จ: '`, `'บันทึกประเภทสินค้าไม่สำเร็จ: '`) **เก็บไว้** เพราะบอกบริบทว่า "อะไร" ล้มเหลว ไม่ใช่แค่ "มีอะไรผิดพลาด" — ใช้ template literal คงรูปแบบเดิมแทนการต่อสตริงตรงๆ (ต่อสตริงตรงๆ กับ union type ผลลัพธ์ของ server action บางตัวชน type error เพราะ TS infer เป็น `string | {}` ในบางเคส — template literal `` `${...}` `` coerce เป็น string ปลอดภัยกว่า)
- `window.confirm()` (ปุ่มยืนยันลบ/ยกเลิก) **ไม่ได้แตะ** — คนละปัญหากับ `alert()` (ต้องมีปุ่มตกลง/ยกเลิกจริง ไม่ใช่แค่แจ้งเตือน) ทำเป็น modal เองได้ในงานถัดไปถ้าต้องการ ไม่ใช่ scope ของรอบนี้ — **ทำแล้วในรอบถัดไป ดู [Confirm dialog แทน window.confirm() ทั้งระบบ](#confirm-dialog-แทน-windowconfirm-ทั้งระบบ) ด้านล่าง**

## Confirm dialog แทน window.confirm() ทั้งระบบ

จุดต่อเนื่องจากงาน toast ด้านบน — ผู้ใช้สังเกตว่าหลังตัด `alert()` ออกหมดแล้ว `window.confirm()` ยังเหลือเป็น browser-native dialog อยู่ ดูหลุดธีม ขอให้แปลงเป็น modal เดียวกับธีมระบบ โดยกำชับว่า **ห้ามกระทบ logic เดิม**

- สแกนทั้ง `app/` + `components/` (2 รอบ ด้วย grep คนละ pattern กันหลุด false positive จากตัวแปรชื่อ `confirmModal`/`isConfirming` ฯลฯ) ครอบคลุมทุกโซนที่ผู้ใช้ขอให้ตรวจ (Logistics, WH, Sale, ฝั่งลูกค้า) — พบ `window.confirm()` จริงแค่ **4 จุด** ทั้งหมดอยู่ใน 2 ไฟล์: [app/admin/csr/customers/page.tsx](app/admin/csr/customers/page.tsx) (ต่ออายุ/ยกเลิกสิทธิ์/เปิดใช้งานอีกครั้ง — 3 จุด) และ [app/admin/manager/staff-approvals/page.tsx](app/admin/manager/staff-approvals/page.tsx) (อนุมัติพนักงาน — 1 จุด) — ไม่มีจุดอื่นใน Logistics/WH/Sale/ฝั่งลูกค้าจริง
- [components/ui/confirm-dialog.tsx](components/ui/confirm-dialog.tsx) — `ConfirmDialogProvider` (Context) + `useConfirm()` hook คืนฟังก์ชันรับได้ทั้ง string เปล่า (`confirm('ข้อความ')`) หรือ options object (`confirm({ message, title?, variant?, confirmLabel?, cancelLabel? })`) — ตั้งใจให้รับ string เปล่าได้ด้วยเพื่อให้ใกล้เคียง signature เดิมของ `window.confirm()` มากที่สุด เปลี่ยนจุดเรียกน้อยที่สุด
  - **จุดสำคัญที่ทำให้ "ไม่กระทบ logic"**: `window.confirm()` คืนค่า boolean แบบ synchronous (บล็อกทั้งหน้าจอรอ) แต่ modal จริงต้องรอ user คลิกปุ่ม จึงต้องเป็น `Promise<boolean>` แทน — ทุก handler ที่เรียกอยู่แล้วเป็น `async function` อยู่ก่อนแล้ว (เพราะเรียก server action ต่อ) จุดที่ต้องแก้จริงมีแค่เติม `await` หน้า `confirm(...)` (`if (!confirm(...)) return;` → `if (!(await confirm(...))) return;`) ส่วน logic ของ `renewCustomerAccess`/`cancelCustomerAccess`/`reactivateCustomerAccess`/`approveStaff` และ state management รอบๆ (`setAccessProcessingIds`, `toast.success`/`toast.error`, `fetchAccessData`/`fetchData`) **ไม่แตะเลย**
  - UI ใช้ modal shell pattern เดียวกับ modal อื่นในระบบ (overlay `bg-slate-900/50 backdrop-blur-sm` + card `bg-card rounded-lg shadow-lg` + แถบสีบนหัว `h-1.5`) เพิ่ม icon circle (`HelpCircle` สีเขียวสำหรับ `variant: 'default'`, `AlertTriangle` สีแดงสำหรับ `variant: 'destructive'`) และปุ่ม 2 ช่อง (`grid-cols-2`) ยกเลิก/ยืนยัน
  - **Semantic variant ตามอนุสัญญาสีเดิมของระบบ** (แดง = เสี่ยง/กลับไม่ได้, เขียว = เป็นกลาง/บวก): ต่ออายุ = `default`, เปิดใช้งานอีกครั้ง = `default`, อนุมัติพนักงาน = `default` — มีแค่ "ยกเลิกสิทธิ์การเข้าใช้งาน" จุดเดียวที่เป็น `variant: 'destructive'` เพราะข้อความเองก็ระบุผลที่กลับสภาพทันที ("ลูกค้าจะไม่สามารถเข้าสู่ระบบได้ทันที")
  - Mount ที่ [app/layout.tsx](app/layout.tsx) ซ้อนอยู่ *ใน* `<ToastProvider>` (ไม่ใช่คู่ขนาน) — `z-[110]` สูงกว่า toast (`z-[100]`) เพราะบางจุด modal ปิดพร้อม toast ผลลัพธ์โผล่ขึ้นมา อยากให้ toast ยังเห็นชัดอยู่แม้ modal เพิ่งปิดไป
- ยืนยันไม่กระทบอะไร: `npx tsc --noEmit` ผ่านสะอาด, `npx vitest run` ผ่านครบ (325 เทสต์ — มี 1 เทสต์ timeout ที่ไม่เกี่ยวกัน คือ OTP email ของ `auth-actions.test.ts`, รันแยกเดี่ยวๆ ผ่านปกติ ยืนยันว่า flaky ไม่ใช่ผลจากงานนี้) และเปิดแอปจริงเช็ค console ไม่มี runtime error จาก provider ตัวใหม่ — **ยังไม่ได้คลิกทดสอบจริงผ่าน UI** เพราะไม่มี staff test credentials ในมือ (ไม่ได้ลองเดา/brute-force login) ถ้าต้องการยืนยันภาพจริงเพิ่มเติม ต้องล็อกอินเป็น CSR หรือ Manager แล้วลองกดปุ่มที่เกี่ยวข้อง

## ประสิทธิภาพ (real performance) — debounce ช่องค้นหา + code-split ManagerInsights

จุดต่อเนื่องจากงาน confirm dialog — ผู้ใช้ถามว่าควรทำเทคนิคอะไรต่อให้ระบบมีประสิทธิภาพมากขึ้น (ต่างจากงาน toast/confirm ที่เป็น perceived-quality) เลยทำ 2 อย่างที่เป็น real performance จริง:

**1) Debounce ช่องค้นหาในหน้ารายงาน** — [lib/hooks/useDebouncedValue.ts](lib/hooks/useDebouncedValue.ts) (hook debounce ค่าทั่วไป ใช้ `useEffect`+`setTimeout` เดียวกับ pattern ที่ `CustomerPicker.tsx`/`OrganizationPicker.tsx` ใช้อยู่แล้ว แต่แยกเป็น hook กลางเพราะตอนนี้ใช้ซ้ำ 2 จุดขึ้นไป)
  - **ปัญหาจริงที่เจอ (ไม่ใช่ network — เป็น client-side re-compute)**: `getCSRDashboardData()`/ข้อมูลลูกค้าโหลดจาก server แค่ครั้งเดียวตอนเข้าเพจ ตัวกรอง/ค้นหาเป็น client-side filter ล้วนๆ (`filterCsrRequests`) ปัญหาคือ `filteredRequests` ถูกส่งเข้า `ManagerInsights` เป็น prop `requests` ซึ่งข้างในมี `useMemo` ~15 ตัวคำนวณกราฟ (reduce/forEach ข้อมูลทั้งก้อนหลายรอบ) ทุกครั้งที่ array reference เปลี่ยน — พิมพ์คำค้น 1 ตัวอักษรเดิม = คำนวณกราฟทั้ง 9 ใบใหม่ทันที
  - ใช้ที่ [app/admin/csr/reports/page.tsx](app/admin/csr/reports/page.tsx) และ [app/admin/sale/reports/page.tsx](app/admin/sale/reports/page.tsx): แยก state `searchInput` (bind กับ `<input>` ตรงๆ ตอบสนองทันที) ออกจาก `filters.search` (ตัวที่ trigger `filterCsrRequests`/`ManagerInsights`/`searchSuggestions`/`exportHref`) — `filters.search` อัปเดตจาก `useDebouncedValue(searchInput, 300)` ผ่าน `useEffect` (มีเช็ค `f.search === (debouncedSearch || undefined)` กัน set state ซ้ำค่าเดิมโดยไม่จำเป็น) ส่วนคลิกเลือกจาก suggestion dropdown ตั้งใจให้ set ทั้ง `searchInput` และ `filters.search` พร้อมกันทันที ไม่ผ่าน debounce เพราะเป็น deliberate action ของผู้ใช้ ไม่ใช่พิมพ์ต่อเนื่อง
  - Manager staff-approvals ไม่ต้องแก้ เพราะแท็บ "ภาพรวม & สถิติ" ส่ง `requests`/`statusLogs` ตรงๆ ไม่มีช่องค้นหากรองก่อนหน้า `ManagerInsights`

**2) Code-split `ManagerInsights.tsx`** (`recharts` + ~730 บรรทัด) ด้วย `next/dynamic` — ใน [app/admin/manager/staff-approvals/page.tsx](app/admin/manager/staff-approvals/page.tsx), [app/admin/csr/reports/page.tsx](app/admin/csr/reports/page.tsx), [app/admin/sale/reports/page.tsx](app/admin/sale/reports/page.tsx)
  - `dynamic(() => import('.../ManagerInsights'), { loading: () => <SkeletonManagerInsights />, ssr: false })` — ใช้ `SkeletonManagerInsights` เดิม (จากงาน skeleton ก่อนหน้า) เป็น loading fallback โดยตรง ไม่ต้องสร้างใหม่ — `ssr: false` เพราะ `recharts` วัดขนาด DOM ฝั่ง client เท่านั้น และทุกหน้าที่ใช้เป็น `'use client'` อยู่แล้ว ไม่ได้ประโยชน์จาก SSR ของกราฟ
  - **ผลต่างกันตามหน้า**: หน้า Manager มี "ภาพรวม & สถิติ" อยู่หลัง tab ที่ต้องกดเอง — code-split ที่นี่คือของจริง (ไม่โหลด `recharts` เลยจนกว่าจะกดแท็บ) ส่วนหน้า CSR/Sale reports โชว์กราฟทันทีที่เข้าหน้า ไม่ได้ "เลื่อนเวลาที่เห็นกราฟ" ออกไปจริงจัง แต่ยังได้ลด initial JS bundle ของหน้าให้เล็กลง (แยก `recharts` เป็น chunk คู่ขนาน)
  - ไม่กระทบ logic เลย เปลี่ยนแค่จังหวะ/วิธีโหลด component ไม่แตะการคำนวณข้างในหรือ props ที่ส่งเข้าไป
- ยืนยัน: `npx tsc --noEmit` ผ่านสะอาด, `npx vitest run` ผ่านครบ 325/325 ทั้ง 2 รอบ (หลัง debounce และหลัง code-split) — เช็ค console ของแอปจริงไม่มี compile/runtime error ใหม่ (หน้าที่แก้ทั้ง 3 ไฟล์ mount/redirect ได้ปกติตอน unauthenticated ซึ่งแปลว่า component รวมถึงบรรทัด `dynamic()` compile ผ่าน) — **ยังไม่ได้คลิกทดสอบพิมพ์ค้นหา/สลับแท็บจริงในเบราว์เซอร์** เพราะไม่มี staff test credentials เช่นเดียวกับงาน confirm dialog ก่อนหน้า

**3) รวม debounce เดิมของ `CustomerPicker.tsx`/`OrganizationPicker.tsx` เข้ากับ `useDebouncedValue`** — ทั้งสองไฟล์เดิมมี `useRef<setTimeout>` + `clearTimeout` มือเขียนเองแยกกันคนละไฟล์ (ก่อน `useDebouncedValue` จะถูกสร้างขึ้นสำหรับงาน debounce ช่องค้นหารายงาน) ผู้ใช้ขอให้รวมมาใช้ hook กลางตัวเดียวกัน ลดโค้ดซ้ำ โดยกำชับห้ามกระทบ logic เดิม
  - **จุดที่ต้องระวังไม่ให้พฤติกรรมเปลี่ยน**: โค้ดเดิมมี 2 พฤติกรรมปนอยู่ใน `useEffect` เดียว — (1) เคลียร์ `results`/`error` **ทันที** (ไม่ดีเลย์) ตอนพิมพ์สั้นกว่า 2 ตัวอักษร (2) หน่วง 350ms **เฉพาะจุดยิงค้นหา** ตอนพิมพ์ครบ ≥2 ตัวอักษร — ถ้ารวมทั้งสองเข้า `useDebouncedValue` เฉยๆ โดยไม่แยก จะทำให้การเคลียร์ผลลัพธ์เก่าดีเลย์ไปด้วย (ผู้ใช้ลบคำค้นจนเหลือ 0-1 ตัวอักษร แต่ผลลัพธ์เก่ายังค้างจออยู่ 350ms) ต่างจากพฤติกรรมเดิม — แก้ด้วยการแยกเป็น **2 `useEffect`**: ตัวแรกฟัง `query` ตรงๆ (ไม่ debounce) จัดการแค่เคลียร์ค่าเมื่อสั้นกว่า 2 ตัวอักษร ตัวที่สองฟัง `debouncedQuery` จาก `useDebouncedValue(query, 350)` จัดการแค่ยิง `searchB2BCustomers`/`searchOrganizations` เมื่อ ≥2 ตัวอักษร — ผลลัพธ์เหมือนเดิมทุกจังหวะเวลา
  - ไม่ได้เพิ่ม cancellation/AbortController ใดๆ เข้าไปแม้จะเห็น race condition เล็กๆ ที่มีอยู่แต่เดิม (ถ้า fetch เก่ายังไม่ resolve แล้ว query ใหม่ settle ก่อน อาจมีผลลัพธ์เก่ามาทับใหม่ทีหลัง) — เป็นพฤติกรรมเดิมที่มีอยู่ก่อนแล้ว ไม่ใช่สิ่งที่ต้องแก้ในงานรวม hook ครั้งนี้ ตั้งใจคงไว้เป๊ะเพื่อไม่ให้กระทบ logic ตามที่กำชับ
  - JSX ทุกจุด (placeholder, เงื่อนไขแสดง "ไม่พบ...", loading/error state) **ไม่ได้แตะ** — ยังอ้างอิง `query` (ค่าพิมพ์สด ไม่ใช่ `debouncedQuery`) เหมือนเดิมทุกจุด เพราะเป็นแค่ debounce จุดยิง fetch เท่านั้น ไม่ใช่ debounce การแสดงผล UI
- ยืนยัน: `npx tsc --noEmit` ผ่านสะอาด, `npx vitest run` ผ่าน 325/325 (มี 1 flaky OTP test เดิมที่ไม่เกี่ยวข้อง รันแยกผ่านปกติ) — ยังไม่ได้คลิกทดสอบพิมพ์ค้นหาจริงในเบราว์เซอร์เพราะไม่มี staff credentials เช่นเดียวกับงานก่อนหน้า

## หน้า "บัญชีผู้ใช้" ฝั่งลูกค้า (self-service account)

ผู้ใช้ขอให้ลูกค้าแก้ไขข้อมูลบัญชีตัวเองได้เหมือนที่ทำให้ staff (`/admin/account`) แล้ว — ปุ่มบน Sidebar (desktop) + BottomNav (มือถือ) ฝั่งลูกค้า กำชับว่าข้อมูลต้องแยกจาก staff อย่างชัดเจน และ UI ต้องธีมลูกค้า ไม่ใช่ Option B — เข้า plan mode ก่อนทำ (spawn 3 Explore agent คู่ขนาน สำรวจ nav/theme ฝั่งลูกค้า, staff account page pattern, และ customer auth/RLS) แล้วถามผู้ใช้ 1 คำถามเรื่องดีไซน์ตารางก่อนเขียนโค้ดจริง

- **จุดสำคัญที่พบระหว่างวิจัย**: ระบบ auth ของลูกค้าเป็น custom session (`customer_session` cookie + ตาราง `sessions`) **ไม่ใช่ Supabase Auth จริง** แม้แต่ตอน login ผ่าน Google (ใช้ Supabase Auth แค่ยืนยันตัวตนชั่วคราวแล้ว `signOut()` ทันที) — ไม่มี `auth.uid()` ให้ผูก RLS ได้ ทุกตารางในระบบนี้ (รวมตารางใหม่นี้) จึงใช้ pattern เดียวกันหมด: `enable row level security` + policy `deny_client_access` (`for all to anon, authenticated using (false) with check (false)`) แล้วให้ server action ผ่าน service-role client (`supabaseAdmin`) เป็นคนตรวจสิทธิ์เองทั้งหมด — ไม่ใช่ RLS ผูก user จริง
- **ถามผู้ใช้ก่อนตัดสินใจ schema** (ผ่าน AskUserQuestion): ตารางใหม่ที่ขอควรเป็น "audit log ประวัติแก้ไข" (มิเรอร์ `staff_account_change_logs` เป๊ะ ข้อมูลจริงยังแก้ลง `b2b_customers` เดิมตรงๆ) หรือ "ตารางโปรไฟล์แยกทั้งชุด" (ย้าย email/contact_name/phone/position ออกจาก `b2b_customers` ทั้งหมด) — ผู้ใช้เลือกแบบแรก ตรงกับที่ staff ทำอยู่แล้วเป๊ะ (`staff_users` แก้ตรงๆ + `staff_account_change_logs` แยกต่างหาก) และตรงกับทิศทางที่โค้ดเบสพยายามทำอยู่แล้ว (ลด column ซ้ำซ้อน ไม่ใช่เพิ่ม — ดู `organizations` refactor ที่ย้าย hospital_name/customer_code ออกจาก `b2b_customers` ไปแล้ว)

**1) Migration** — [supabase/migrations/20260823010000_add_customer_account_change_logs.sql](supabase/migrations/20260823010000_add_customer_account_change_logs.sql): ตาราง `customer_account_change_logs` มิเรอร์ `staff_account_change_logs` ทุกจุด (`enable row level security` + policy `deny_client_access` เหมือนกันเป๊ะ) ต่างแค่ผูกกับ `b2b_customers` แทน `staff_users` และ `field` มี 3 ค่า (`email`/`password`/`contact_info` — เพิ่ม `contact_info` เพราะลูกค้ามีฟิลด์โปรไฟล์ที่ staff ไม่มี) — apply เข้า live Supabase project จริงแล้วผ่าน MCP (`qgztveswduffskucqppt`) เช็ค `get_advisors` แล้วไม่มี finding ใหม่จากตารางนี้ (มีแค่ warning เดิมที่ไม่เกี่ยวข้อง — `auth_leaked_password_protection`, ไม่เกี่ยวเพราะแอปนี้ไม่ได้ใช้ Supabase Auth สำหรับ login อยู่แล้ว)

**2) Server actions ใหม่** — [app/actions/auth-actions.ts](app/actions/auth-actions.ts): `updateCustomerEmail`/`updateCustomerPassword`/`updateCustomerContactInfo` มิเรอร์ `updateStaffEmail`/`updateStaffPassword` ใน `auth-staff.ts` ทุกจุด (`getCustomerSession()` ยืนยันเอง ไม่รับ id จาก client, rate limit bucket เดียวกันทั้ง 3 ฟังก์ชัน, ยืนยัน current password ก่อนแก้ email/password, `updateCustomerPassword` revoke session อื่นทั้งหมดยกเว้นตัวปัจจุบันเหมือน staff) — ต่างจาก staff ตรงที่มีฟอร์มที่ 3 เพิ่ม (`updateCustomerContactInfo`: contact_name/phone/position) ที่**ไม่บังคับรหัสผ่านปัจจุบัน** เพราะไม่ใช่ identity credential — ไม่แตะ hospital_name/customer_code/province เพราะเป็นข้อมูลระดับ `organizations` ที่ผู้ติดต่อรายบุคคลแก้เองไม่ได้ (กระทบผู้ติดต่อคนอื่นในหน่วยงานเดียวกัน)

**3) หน้าใหม่** — [app/(authenticated)/account/page.tsx](app/(authenticated)/account/page.tsx): ไม่สร้าง layout guard แยกแบบ staff เพราะ `app/(authenticated)/layout.tsx` มี `getCustomerSession()` + `<Sidebar>`/`<BottomNav>` ครอบอยู่แล้วทุกหน้าในกลุ่มนี้ — ตั้งใจให้ลูกค้าเห็น nav ค้างอยู่ตอนกดเข้าหน้าบัญชี (ต่างจาก staff ที่ซ่อน hub chrome ทั้งหมดตอนอยู่หน้า account เพราะ admin ไม่มี persistent sidebar แบบนี้) ธีมทำตาม `StaffSignUpForm.tsx`/`Sidebar.tsx` (teal→emerald gradient, `rounded-3xl`, `font-black`, `shadow-lg shadow-teal-500/20`) คนละธีมกับ staff โดยเจตนา

**4) ปุ่ม nav** — [components/Sidebar.tsx](components/Sidebar.tsx) และ [components/BottomNav.tsx](components/BottomNav.tsx): เพิ่มรายการ "บัญชีผู้ใช้" ไอคอน `UserCog` (ตัวเดียวกับที่ staff account page ใช้) ไปที่ `/account` — Sidebar เพิ่ม tint bucket ใหม่ `amber` (จากเดิมมี emerald/blue/violet — เลือกสีที่ยังไม่ถูกใช้)

- ยืนยัน: `npx tsc --noEmit` ผ่านสะอาด, `npx vitest run` ผ่านครบ 325/325, migration apply สำเร็จ + `get_advisors` ไม่มี finding ใหม่, เช็ค console แอปจริงทั้ง `/welcome` และ `/account` ไม่มี compile/runtime error ใหม่ (ทั้งคู่ redirect ไปหน้าแรกตามปกติตอนยังไม่ login ซึ่งแปลว่า route ใหม่ compile ผ่าน) — **ยังไม่ได้คลิกทดสอบกรอกฟอร์มจริงในเบราว์เซอร์** (เปลี่ยนอีเมล/รหัสผ่าน/ข้อมูลติดต่อ) เพราะไม่มี customer test credentials เช่นเดียวกับงานก่อนหน้าทั้งหมด

**บั๊กที่ผู้ใช้เจอจริงหลัง deploy (แก้แล้ว)**: ผู้ใช้ทดสอบเปลี่ยนชื่อผู้ติดต่อจริง เจอ error message ขึ้นเป็น `[object Object]` — root cause 2 ชั้นซ้อนกัน:
  1. **`b2b_customers` ไม่มีคอลัมน์ `updated_at`** (ต่างจาก `staff_users` ที่มี) — ตอนเขียน `updateCustomerEmail`/`updateCustomerPassword`/`updateCustomerContactInfo` เผลอก็อป `updated_at: new Date().toISOString()` มาจาก pattern ของ staff ทั้ง 3 ฟังก์ชัน โดยไม่ได้เช็ค schema จริงของ `b2b_customers` ก่อน (ยืนยันด้วย `information_schema.columns` ผ่าน Supabase MCP หลังผู้ใช้รายงาน) ทำให้ `.update()` ทุกจุดพัง (`column b2b_customers.updated_at does not exist`) — แก้โดยตัด `updated_at` ออกจากทั้ง 3 จุด ให้เหมือน `resetCustomerPassword` (โค้ดเดิมที่ทำงานถูกอยู่แล้ว ไม่มี `updated_at` เช่นกัน)
  2. **`getErrorMessage()` ([lib/error-message.ts](lib/error-message.ts)) เช็คแค่ `error instanceof Error`** — แต่ Postgrest error object (`{ message, details, hint, code }`) ที่ Supabase คืนมาไม่ใช่ instance ของ `Error` class จริง เป็น plain object ธรรมดา พอ throw แล้วเข้า catch, `getErrorMessage` fallback ไป `String(error)` ซึ่งได้ `"[object Object]"` แทนข้อความจริง — เป็นบั๊กแฝงที่มีอยู่ก่อนแล้วในโค้ดกลาง (ใช้ร่วมกันหลายไฟล์) ไม่เคยโดนชนมาก่อนเพราะ action อื่นส่วนใหญ่ดัก error code เฉพาะ (เช่น `23505`) ก่อนถึง throw ทั่วไป — แก้โดยเพิ่มเงื่อนไขเช็ค `error` เป็น object ที่มี `.message` เป็น string ก่อน fallback ไป `String()` ครอบคลุมทั้ง Postgrest error และ error-like object อื่นๆ ทั่วทั้งระบบ ไม่ใช่แค่จุดนี้
  - ยืนยันหลังแก้: `npx tsc --noEmit` ผ่าน, `npx vitest run` ผ่าน 325/325 (มี 2 flaky OTP test ที่ไม่เกี่ยวข้อง รันแยกผ่านปกติทั้งคู่), เช็ค console ไม่มี error ใหม่ — ยังไม่ได้คลิกยืนยันซ้ำในเบราว์เซอร์จริงเพราะไม่มี credentials เหมือนเดิม รอผู้ใช้ลองอีกครั้ง

**ตามคำขอผู้ใช้ ("ลองดูหัวข้อย่อยอื่นอีก 2 หัวข้อในการ์ดนี้ด้วย ตรวจสอบให้ดี") — ไล่ตรวจฟอร์มอีเมล/รหัสผ่านซ้ำอีกรอบ พบ 2 จุดเพิ่ม (ไม่ใช่บั๊กที่ทำให้พัง แต่เป็นปัญหาจริงที่ควรแก้)**:

1. **ตรวจ zod error message extraction จริงด้วยสคริปต์ทดสอบแยก** (`EmailSchema`/`ContactInfoSchema` ผ่าน `parsedX.error.issues[0]?.message`) — รันจริงผ่าน `npx tsx` ยืนยันว่าได้ข้อความไทยที่ถูกต้องทุกเคส (ไม่ใช่ zod default message ภาษาอังกฤษ หรือ `[object Object]` ซ้ำแบบเดิม) — ไม่พบปัญหา แค่ยืนยันว่าปลอดภัย ไม่ใช่บั๊กใหม่
2. **พบบั๊กจริงจุดที่ 2**: `useEffect` ที่ seed ค่าเริ่มต้นให้ฟอร์ม "ข้อมูลติดต่อ" ([app/(authenticated)/account/page.tsx](app/(authenticated)/account/page.tsx)) เดิม dep เป็น `[customer]` (ทั้ง object) ไม่ใช่ `[customer?.id]` — เพราะฟอร์มอีเมลกับข้อมูลติดต่อทั้งคู่เรียก `setCustomer((c) => ({ ...c, ... }))` ตอนบันทึกสำเร็จ (สร้าง object ใหม่ทุกครั้ง) จึง trigger effect นี้ซ้ำทุกครั้งที่ฟอร์มไหนก็ตามบันทึกสำเร็จ รีเซ็ต `contactName`/`phone`/`position` ทับข้อมูลที่ผู้ใช้เพิ่งพิมพ์ค้างไว้ในฟอร์มข้อมูลติดต่อ (ทั้งที่ยังไม่ได้กดบันทึกฟอร์มนั้นเลย) — เช่น ผู้ใช้แก้ชื่อผู้ติดต่อค้างไว้ แล้วไปกดบันทึกอีเมลก่อน ชื่อที่พิมพ์ค้างจะหายกลับไปเป็นค่าเดิม — แก้เป็น `[customer?.id]` (id ไม่เปลี่ยนตราบใดที่ login เป็นคนเดิม effect จึงรันแค่ครั้งแรกตอน session โหลดเสร็จจริงๆ)
3. **พบบั๊กจริงจุดที่ 3**: Sidebar/BottomNav อ่านข้อมูลลูกค้าจาก `getCustomerSession()` ที่เรียกฝั่ง server ใน `app/(authenticated)/layout.tsx` (Server Component, render ครั้งเดียวต่อการ navigate) — หน้า `/account` เป็น client component ที่อัปเดตแค่ client state ของตัวเอง ไม่มีทางสั่งให้ layout ฝั่ง server re-fetch ได้เอง ผลคือหลังบันทึก "ข้อมูลติดต่อ" สำเร็จ Sidebar จะยังโชว์ชื่อ/ตัวอักษรแรกของ avatar เดิมค้างอยู่ (`customer?.contact_name?.charAt(0)` ใน `Sidebar.tsx`) จนกว่าจะ navigate ไปหน้าอื่นแล้วกลับมา หรือ refresh หน้าเอง — แก้โดยเรียก `router.refresh()` (จาก `next/navigation`) หลังบันทึกอีเมล/ข้อมูลติดต่อสำเร็จ บังคับ Next.js re-fetch server component ใหม่ (ไม่ใส่ในฟอร์มรหัสผ่าน เพราะรหัสผ่านไม่ได้แสดงผลที่ไหนอีก ไม่มีอะไรค้างให้ต้อง refresh)
- ยืนยันหลังแก้รอบนี้: `npx tsc --noEmit` ผ่าน, `npx vitest run` ผ่าน 325/325 (มี 1 flaky OTP test เดิม รันแยกผ่านปกติ), เช็ค console ไม่มี error ใหม่ — ยังไม่ได้คลิกทดสอบจริงในเบราว์เซอร์เพราะไม่มี credentials เหมือนเดิม

**ตัดฟีเจอร์ "แก้ไขอีเมล" ออกจากหน้าบัญชีผู้ใช้ (การตัดสินใจสุดท้ายของผู้ใช้)** — ระหว่างคุยกันพบว่าอีเมลผูกกับ "Sign in with Google" โดยตรง (`loginCustomerByVerifiedEmail` ใน `auth-actions.ts` จับคู่ด้วยอีเมลที่ Google verify มาแบบ exact-match กับ `b2b_customers.email`) ถ้าลูกค้าแก้อีเมลเองได้ Google Sign-In เดิมจะหลุดทันที — เสนอทางแก้ (ผูก Google identity ที่เสถียรแทนอีเมล เช่น `data.user.id`/Google `sub` claim) แต่พบว่ามี security trade-off เพิ่ม (ถ้าเคยมี Google account ไหน link ไว้ การเปลี่ยนอีเมลจะไม่มีผลตัดสิทธิ์นั้นอีกต่อไป ต่างจากพฤติกรรมเดิมที่เปลี่ยนอีเมล = Google เดิมหลุดไปเอง) — ผู้ใช้ชั่งน้ำหนักแล้วตัดสินใจว่า **อีเมลสำคัญเกินกว่าจะให้ลูกค้าแก้เองได้** ตัดฟีเจอร์นี้ออกทั้งหมด เหลือแค่เปลี่ยนรหัสผ่าน + ข้อมูลติดต่อ ให้อีเมลแก้ได้เฉพาะผ่าน CSR (กระบวนการอนุมัติปกติที่มีอยู่แล้ว) — **ง่ายและปลอดภัยกว่าการพยายามทำให้ Google Sign-In ทำงานต่อเนื่องหลังเปลี่ยนอีเมล**

- ลบ `updateCustomerEmail` ออกจาก [app/actions/auth-actions.ts](app/actions/auth-actions.ts) ทั้งฟังก์ชัน (เหลือ `updateCustomerPassword`/`updateCustomerContactInfo` เท่านั้นในหมวด self-service) — คง `EmailSchema` ไว้ (ยังใช้กับ login/OTP reset ที่อื่นในไฟล์เดียวกัน) และคง `verifyCurrentCustomerPassword` ไว้ (ยังใช้กับ `updateCustomerPassword`)
- ลบฟอร์ม "เปลี่ยนอีเมล" ออกจาก [app/(authenticated)/account/page.tsx](app/(authenticated)/account/page.tsx) ทั้งหมด (state/handler/JSX/import ที่เกี่ยวข้อง เช่น icon `AtSign`) เหลือแค่ฟอร์ม "เปลี่ยนรหัสผ่าน" กับ "ข้อมูลติดต่อ"
- [supabase/migrations/20260823020000_drop_email_from_customer_account_change_logs.sql](supabase/migrations/20260823020000_drop_email_from_customer_account_change_logs.sql) — แก้ check constraint ของ `customer_account_change_logs.field` จาก `('email','password','contact_info')` เหลือ `('password','contact_info')` ให้ schema สะท้อนความสามารถจริงของระบบ ไม่มี field ที่ไม่มีทางถูกใช้ค้างอยู่ — เช็คก่อน apply แล้วว่าไม่มีแถวไหน `field = 'email'` อยู่เลย (มีแค่ `contact_info` 1 แถวจากตอนทดสอบ) จึงตัด constraint ได้โดยไม่ต้อง backfill — apply เข้า live project จริงแล้วผ่าน MCP หลังยืนยันชื่อ constraint จริง (`customer_account_change_logs_field_check`) ด้วย `pg_get_constraintdef` ก่อน ไม่เดาชื่อ
- ยืนยัน: `npx tsc --noEmit` ผ่านสะอาด (ไม่มีที่ไหนอ้างอิง `updateCustomerEmail` ที่ถูกลบไปหลงเหลือ), `npx vitest run` ผ่านครบ 325/325 (ไม่มี flaky รอบนี้), `grep` ทั้ง repo ยืนยันไม่มีจุดอ้างอิง `updateCustomerEmail` เหลือเลยนอกจาก comment อธิบายเหตุผลที่ตัดออก — เช็ค browser เจอ error message เก่าที่ค้างใน console history (จาก compile ก่อนหน้าที่ยังไม่ได้ recompile) แต่ยืนยันด้วย screenshot จริงว่าหน้าปัจจุบัน render ปกติไม่มี error overlay หลัง reload

## Redesign หน้า "ประวัติการยื่นคำร้อง" (customer/history) + org-history — ธีมเดียวกัน ไม่แตะ logic

ผู้ใช้ขอให้ปรับหน้า "ประวัติการยื่นคำร้อง" ให้ดูมาตรฐานกว่าเดิม คงทุก logic เดิมไว้ เปลี่ยนแค่สไตล์/layout — ไฟล์จริงที่ต้องแก้คือ [components/history/ExchangeHistoryView.tsx](components/history/ExchangeHistoryView.tsx) (component กลาง ไม่ใช่ page.tsx) เพราะทั้ง `customer/history` และ `customer/org-history` reuse component เดียวกันเป๊ะ — เช็คก่อนแก้ว่า `NotificationBell.tsx` (ใช้ทุกหน้า staff) มีคอมเมนต์อ้างชื่อไฟล์นี้อยู่ (`// โทนเดียวกับ getCardTone ใน ExchangeHistoryView.tsx`) แต่ไม่ได้ import/render จริง — ยืนยันแล้วว่าแก้ไฟล์นี้กระทบแค่ฝั่งลูกค้า 2 หน้า ไม่กระทบฝั่ง staff เลย ตรงกับที่ผู้ใช้กำชับ ("เฉพาะในส่วนของ customer")

**สิ่งที่เปลี่ยน (className/JSX ล้วนๆ ไม่แตะ state/handler/การคำนวณใดๆ)**:
- ไอคอน header: gradient `from-teal-600 to-teal-700` (เฉดเดียว) → `from-teal-500 to-emerald-600` ให้ตรงกับธีม teal→emerald ที่ใช้ทั่วฝั่งลูกค้า (Sidebar avatar, ปุ่ม CTA, header หน้าบัญชีผู้ใช้)
- `StatCard`: ขยายไอคอน `h-8 w-8 rounded-lg` → `h-10 w-10 rounded-xl`, การ์ด `rounded-xl` → `rounded-2xl` + เพิ่ม `shadow-sm hover:shadow-md`, ตัวเลข `text-base` → `text-lg` ให้เด่นขึ้น
- **Tab กรองสถานะ**: เดิมเป็น underline tab (`border-b-2` + `-mx-6 border-b border-border px-6` แบบ full-bleed) เปลี่ยนเป็น **pill tab** (`rounded-full`, active = `bg-teal-600 text-white shadow-sm shadow-teal-200`, inactive = `border border-teal-100 bg-white text-teal-700`) — ให้ตรงกับ pill tab ของ "ประเภทงาน" (`REQUEST_TYPE_TABS` ใน `customer/history/page.tsx`) ที่อยู่เหนือมันในหน้าเดียวกัน เดิมสองแถบ tab ใช้ภาษาภาพคนละแบบ (pill กับ underline) ซ้อนกันในหน้าเดียว ดูไม่เป็นมาตรฐาน — ตอนนี้ตรงกันแล้ว **สังเกตเพิ่ม**: skeleton (`ExchangeCardsSkeleton.tsx`) เดิม mock เป็น pill (`rounded-full`) อยู่แล้วทั้งที่ของจริงตอนนั้นยังเป็น underline — เป็น mismatch เดิมที่มีอยู่ก่อน งานนี้บังเอิญแก้ให้ตรงกันไปด้วย
- Empty state (ทั้ง 2 จุด — ไม่มีคำร้องเลย / ไม่มีคำร้องในแท็บนี้): จาก `border-dashed border-border` เปล่าๆ → `border-dashed border-teal-100 bg-teal-50/30` + ไอคอนใส่ใน circle ขาว `shadow-sm` แทนไอคอนลอยเดี่ยวๆ
- `RequestCard`: เพิ่ม `hover:-translate-y-0.5` (การ์ดยกตัวตอน hover) ให้เข้าธีม motion เดียวกับปุ่ม CTA อื่นๆ ในระบบ — ไม่แตะ `DrugItemRow`/grid รายการยาเลย (ของเดิมออกแบบ responsive ไว้ดีอยู่แล้ว ตามคอมเมนต์ในโค้ด)
- ปุ่มลิงก์ท้ายการ์ด: ข้อความ **"ดูประวัติ Timeline ทั้งหมด" → "ติดตามสถานะคำร้อง"** (ผู้ใช้ไม่ชอบข้อความเดิม เสนอ 3 ตัวเลือก ผู้ใช้เลือกตัวที่แนะนำ — เหตุผลที่แนะนำ: ข้อความเดิมปนอังกฤษ ("Timeline") และคำว่า "ทั้งหมด" ทำให้เข้าใจผิดว่าไปดูภาพรวมทุกคำร้อง ทั้งที่ลิงก์นี้พาไปดูแค่คำร้องใบนี้ใบเดียว (`/customer/tracking?ref=...`) — ข้อความใหม่ตรงกับชื่อหน้าปลายทาง "tracking" พอดี) เพิ่ม `hover:-translate-y-0.5` ด้วยเช่นกัน
- **แก้ skeleton ให้ตรงกับของจริงใหม่** ([components/skeletons/ExchangeCardsSkeleton.tsx](components/skeletons/ExchangeCardsSkeleton.tsx)): `ExchangeStatCardsSkeleton` ขยายขนาด icon/card ให้ตรงกับ `StatCard` ใหม่ (กันการ์ด "โตขึ้น" ตอนโหลดเสร็จ), แถบ tab skeleton ตัด `-mx-6 border-b px-6` (full-bleed) ออกให้ตรงกับ pill tab ที่ไม่มี full-bleed แล้ว
- ตัวแปร logic ทั้งหมด (`tabs`, `activeKey`, `getGroupKey`, `getCardTone`, การคำนวณ stat 5 ตัว, pagination, `fetcher`) **ไม่ถูกแตะแม้แต่บรรทัดเดียว**
- ยืนยัน: `npx tsc --noEmit` ผ่านสะอาด, `npx vitest run` ผ่าน 325/325 (1 flaky OTP test เดิม รันแยกผ่านปกติ), `grep` ยืนยันไม่มี class เก่า (`border-b-2`, `from-teal-600 to-teal-700`) หลงเหลือ — เช็ค browser ยืนยันด้วย screenshot ว่าไม่มี error overlay หลัง reload (console log history มี error เก่าค้างจากงานก่อนหน้าปนอยู่ ไม่ใช่ error จริงของงานนี้) — ยังไม่ได้คลิกดูภาพจริงของหน้าเพราะไม่มี customer credentials เหมือนเดิม

## Mascot "GPO Spark" — ปุ่มลอยเปิดแชท (ChatWidget.tsx)

ผู้ใช้อยากได้ปุ่มลอยของ chatbot เป็น mascot แทนไอคอนกลมธรรมดา (เดิม `MessageCircle` บนพื้นวงกลม teal) และอยากให้ขยับได้ — ถามเรื่องผลกระทบต่อประสิทธิภาพก่อน อธิบายไปว่า CSS `transform`-based animation ทำได้โดยแทบไม่กิน CPU (GPU compositor) ต่างจาก Lottie/WebGL ที่หนักกว่าและเกินความจำเป็นสำหรับปุ่มมุมจอเล็กๆ แบบนี้ — ผู้ใช้ส่งชุดไฟล์มาเอง (`GPO_Spark_Mascot_Kit/`: 6 pose SVG + 10 expression SVG + avatar SVG + CSS animation starter + README)

- **ตรวจสอบความปลอดภัยก่อนใช้**: `grep` ทุกไฟล์ในชุดหา `<script`, `onload=`, `onerror=`, `javascript:`, external `xlink:href` — ไม่พบเลยสักจุด ปลอดภัยที่จะใช้
- **พบ 2 ประเด็นต้องคุยกับผู้ใช้ก่อนเอาไปใช้จริง** (แสดงตัวอย่างผ่าน `mcp__visualize__show_widget` ให้ดูก่อนตัดสินใจ):
  1. โทนสีมาสคอต (navy `#0D3D91`/cyan `#00E5FF`) ไม่ตรงกับธีมจริงของระบบ (teal→emerald ทั้งระบบ) — ผู้ใช้ตัดสินใจ **เก็บสีเดิมไว้** ("อยากให้เกิดความแตกต่างไม่สีเขียวไปทั้งหน้า") ไม่ recolor
  2. README ของชุดเองเตือนว่า text "GPO" บนตัวละครไม่ใช่โลโก้ที่องค์การเภสัชกรรม (หน่วยงานราชการจริง) อนุมัติ แนะนำให้เอาออกก่อนใช้จริง — ผู้ใช้เห็นด้วย สั่งเอาออก
- **ลบข้อความ "GPO" ออกจากทุกไฟล์ในชุด** — grep หา `>GPO<` เจอใน 18 ไฟล์ (svg หลัก 3 + pose 6 + expression 10 ทั้งหมด), element ตรงกันเป๊ะทุกไฟล์ (`<text x="300" y="168" ... fill="#0D3D91">GPO</text>`) ลบด้วย exact string match ผ่านสคริปต์ python เล็กๆ ยืนยันด้วย grep ซ้ำว่าเหลือ 0 จุด — **คง "X" ที่หน้าอกไว้** (ผู้ใช้สั่งแค่เอา GPO ออก ไม่ได้พูดถึง X ซึ่งสื่อถึงคำว่า "Xchange" ไม่ใช่ชื่อองค์กรตรงๆ)
- **Integrate เข้า [components/ChatWidget.tsx](components/ChatWidget.tsx) จริง**:
  - copy `gpo_spark_avatar_1x1.svg` (ไฟล์ที่ README ระบุไว้ว่าออกแบบมาสำหรับ "chatbot launcher" โดยเฉพาะ — มี background วงกลมและ badge ถูกต้องมาแล้ว) ไปที่ [public/mascot/gpo_spark_avatar_1x1.svg](public/mascot/gpo_spark_avatar_1x1.svg)
  - เพิ่ม CSS animation ใน [app/globals.css](app/globals.css) (ก็อปจาก `gpo_spark.css` ที่แถมมา ปรับ amplitude การเด้ง idle ลงจาก 7px เหลือ 4px ให้สัดส่วนเหมาะกับปุ่มขนาดเล็ก 64-80px แทนตัวละครเต็มตัวที่ไฟล์ต้นฉบับออกแบบไว้) — `.gpo-spark` เด้งเบาๆ ตลอดเวลา (`spark-idle` 3.6s loop), โบกมือตอน hover (`spark-wave` 1.2s ครั้งเดียว), มี `prefers-reduced-motion` guard
  - แก้ปุ่มลอยใน `ChatWidget.tsx`: **ตอนปิดแชท** = แสดง mascot avatar (`<img>` + class `gpo-spark`) แทน `MessageCircle` icon เดิม — **ตอนเปิดแชทแล้ว** = คงปุ่ม `X` ปิดแบบเดิมเป๊ะ (พื้นวงกลม teal) ไม่เอาไอคอนปิดไปวาดทับหน้ามาสคอต ให้เป็นปุ่มฟังก์ชันธรรมดาชัดเจนกว่า — logic การ toggle `open`/`aria-label`/ตำแหน่ง `fixed bottom-...` เดิมทั้งหมด **ไม่ถูกแตะเลย**
  - **ขนาด responsive ตามที่ขอ**: มือถือ `h-16 w-16` (64px, จากเดิม 56px — ใหญ่ขึ้นพอเห็นชัดแต่ไม่เยอะเกินจนบังเนื้อหา), desktop `md:h-20 w-20` (80px — จอกว้างมีที่ว่างเยอะกว่า ให้เด่นเต็มที่)
  - ลบ import `MessageCircle` ที่ไม่ได้ใช้แล้วออกจาก `lucide-react` import list
- ยืนยัน: `npx tsc --noEmit` ผ่านสะอาด, `npx vitest run` ผ่านครบ 325/325, เปิด `/mascot/gpo_spark_avatar_1x1.svg` ตรงๆ ในเบราว์เซอร์เช็คแล้วว่าไฟล์ serve ได้จริงและไม่มี "GPO" หลงเหลือ, เช็ค console บนหน้า `/welcome` ไม่มี error เลย (ครั้งนี้สะอาดจริง ไม่มี error log เก่าปนแล้ว หลัง reopen browser pane ใหม่) — ยังไม่ได้คลิกทดสอบ hover/เปิดแชทจริงเพราะไม่มี customer credentials เหมือนเดิม

**ตามมาด้วยคำขอเพิ่ม 2 จุด**:

1. **กรอบแชทที่เปิดขึ้นมา (header, user bubble, ช่องพิมพ์ focus border, ปุ่มส่ง, ปุ่ม X ปิด) เปลี่ยนจาก teal → น้ำเงินให้เข้ากับ mascot** — ใช้ hex ตรงจาก gradient "blue" ของตัวมาสคอตเอง (`#0B73E8` → `#0D3D91`) ไม่ใช้ Tailwind `blue-600`/`blue-700` เฉยๆ เพื่อให้สีตรงกับตัวมาสคอตเป๊ะ ไม่ใช่แค่ "โทนน้ำเงินคล้ายๆ กัน" — ขอบเขตจำกัดเฉพาะ `ChatWidget.tsx` เท่านั้น (ปุ่ม/กรอบแชท) ส่วนที่เหลือของแอปยังเป็น teal/emerald เหมือนเดิมทั้งหมด ตรงตามที่ผู้ใช้ตั้งใจไว้ตั้งแต่ตอนเลือกไม่ recolor มาสคอต (อยากให้มาสคอต+แชทมีเอกลักษณ์สีของตัวเอง แยกจากสีเขียวหลักของระบบ)
2. **บับเบิลคำพูดแนะนำตัวลอยค้างไว้ก่อนกดเปิดแชท** — `<div role="status">` ข้อความ "สวัสดีครับ มีคำถามถามผมได้เลยนะครับ" แสดงเมื่อ `!open` เท่านั้น (ตำแหน่งข้าง mascot, ระยะคำนวณจากขนาดปุ่ม) — **ภายหลังผู้ใช้ทดสอบจริงแล้วเห็นว่าบังเนื้อหาหน้าเว็บ สั่งเอาออก** ลบโค้ดทั้งบล็อกออกจาก `ChatWidget.tsx` เรียบร้อย (ไม่มีเหลือค้าง เช็คด้วย grep แล้ว) — บทเรียน: mockup ที่จำลองระยะห่างได้ (ไม่บังปุ่มมาสคอต) ไม่ได้แปลว่าไม่บัง**เนื้อหาอื่นของหน้า**อยู่ดี ต้องเห็นของจริงถึงจะรู้
3. **บั๊กจริงที่เจอจากการทดสอบบนอุปกรณ์จริง — กรอบแชทสูงเกินจอ หัว (header สีน้ำเงิน) โดนดันพ้นขอบบนจอ**: ผู้ใช้ส่ง screenshot จากมือถือจริงมาให้ดู เห็นแค่กล่องข้อความ+ช่องพิมพ์ ไม่เห็น header สีน้ำเงินเลย (แปลว่า header หลุดพ้นขอบจอด้านบนไปจริงๆ ไม่ใช่แค่โดนบัง) — root cause: กรอบแชทใช้ `h-[70vh]` (หน่วย `vh` ธรรมดา) ทั้งที่ทุกจุดอื่นในระบบ (ทั้ง `app/layout.tsx`, `app/(authenticated)/layout.tsx`, `app/welcome/layout.tsx`) ตั้งใจใช้ `dvh` แทนมาตลอดโดยเฉพาะเพื่อแก้ปัญหานี้เป๊ะๆ (มีคอมเมนต์อธิบายไว้ในทั้ง 2 layout อยู่แล้ว) — `vh` บนมือถือคำนวณจาก viewport ที่ใหญ่สุด (ตอน address bar ยุบ) แม้ตอนนั้น address bar จะยังกางอยู่จริง (พื้นที่มองเห็นจริงเล็กกว่า) ทำให้กล่องที่คำนวณจาก `70vh` สูงเกินพื้นที่มองเห็นจริงได้ ส่วนเกินไปโผล่พ้นขอบจอด้านบน (กรอบ fixed ยึดจาก `bottom` เป็นหลัก ส่วนเกินความสูงจึงดันขึ้นด้านบนแทนที่จะดันลงล่าง) — แก้โดยเปลี่ยน `h-[70vh]` → `h-[70dvh]` จุดเดียว ให้ตรงกับ convention เดิมของระบบ
- ยืนยัน: `npx tsc --noEmit` ผ่านสะอาด, `npx vitest run` ผ่านครบ 325/325 ทุกรอบ, เช็ค console หน้า `/welcome` ไม่มี error — ยังไม่ได้คลิกทดสอบจริงในเบราว์เซอร์เพราะไม่มี customer credentials (ผู้ใช้ทดสอบเองบนอุปกรณ์จริงแทน ซึ่งเป็นตัวที่จับบั๊กนี้ได้จริงในที่สุด)

## ยังไม่ได้ทำ

- **โซน CSR ทำครบทั้งหมดแล้ว** (hub + ทุกหน้าย่อย รวมเนื้อหา ไม่ใช่แค่ topbar)
- **WH/Logistics ทำครบแล้ว** (ดูหัวข้อแรกของไฟล์นี้)
- **โซน Manager ทำครบทั้งหมดแล้ว** (hub + staff-approvals ทั้ง 4 แท็บ + sla + tracking + `ManagerInsights.tsx`)
- การ์ด "บัญชีผู้ใช้" ที่ลิงก์ไป `/admin/account` (จัดการบัญชี) ทำแล้วที่ CSR + Manager + Sale hub + Logistics dashboard — เหลือแค่ WH dashboard (ไม่มี hub ของตัวเอง มีแค่หน้า dashboard เดียว เดิมก็ไม่มี tile "สถานะบัญชี" แบบนี้เหมือนที่ Logistics เคยไม่มีมาก่อน) ยังไม่ได้ทำ รอ request เพิ่มเติม
  - Sale hub เดิมไม่มีการ์ด "บัญชีผู้ใช้" อยู่แล้ว (มีแค่ "สถานะบัญชี" เดี่ยวๆ) ต้องสร้างใหม่ทั้งการ์ด ไม่ใช่แค่ผูกลิงก์เหมือน CSR/Manager
  - ลองมาแล้วหลายรอบก่อนจะได้ทรงสุดท้าย: (1) เบียด "บัญชีผู้ใช้" ไว้ในคอลัมน์เดียวกับ "สถานะบัญชี" คู่กับ "ขอบเขตที่ดูแล" ทางขวา → เจอ grid stretch บั๊ก (คอลัมน์ซ้ายสูงขึ้น ดันให้ "ขอบเขตที่ดูแล" ถูกยืดสูงตามจนมีช่องว่างเปล่าๆ กลางการ์ด) (2) สลับคู่กันระหว่าง breakpoint ด้วย grid-cols-2 เดียวกันแล้วสลับ col-span → ผู้ใช้ดูแล้วไม่สวย (3) ยกโครงมาจาก CSR ตรงๆ (สถานะบัญชี+บัญชีผู้ใช้ติดกันเต็มความกว้างทุก breakpoint, ขอบเขตที่ดูแลแยกเต็มแถว) → ที่หน้าจอกว้างปานกลาง (ยังไม่ถึง md) การ์ดสถานะบัญชีถูกบีบเป็น `flex-1` ข้างการ์ด username แคบๆ ทำให้มีที่ว่างเยอะเกินไป ไม่สวยอีกเช่นกัน — สุดท้ายผู้ใช้ขอชัดเจนว่ามือถืออยากให้บัญชีผู้ใช้ไปคู่กับขอบเขตที่ดูแล (ประเภทลูกค้า) แทน ส่วน desktop ให้คงแบบ CSR ไว้เหมือนเดิม
  - **ทรงสุดท้ายที่ใช้จริง**: จับคู่กันคนละแบบจริงๆ ระหว่าง 2 breakpoint (มือถือ: บัญชีผู้ใช้คู่ขอบเขตที่ดูแล / desktop: บัญชีผู้ใช้คู่สถานะบัญชีแบบ CSR) — เพราะ adjacency ของ DOM เปลี่ยนจริง ไม่ใช่แค่ขนาด/col-span จึงเลือก **render wrapper 2 ชุดแยกกัน** แทนการพยายาม merge เป็น class เดียว: เนื้อหาการ์ดทั้ง 3 ใบ (`statusCardBody`/`usernameCardBody`/`scopeCardBody`) แยกเป็นตัวแปร JSX ไว้ก่อน `return` กันโค้ดซ้ำ แล้ว render 2 wrapper คือ `<div className="md:hidden">` (โครงมือถือ: การ์ดสถานะเดี่ยวเต็มแถวบน + `grid-cols-2` บัญชีผู้ใช้/ขอบเขตที่ดูแลแถวล่าง พร้อม `items-start` กัน grid stretch) กับ `<div className="hidden md:contents">` (โครง desktop: `display:contents` ถอด wrapper ออกให้ลูกกลายเป็น grid item ของ bento grid หลักตรงๆ แบบ CSR/Manager) — **บทเรียน**: ถ้าการ์ดต้อง "จับคู่กันคนละแบบจริงๆ" ระหว่าง breakpoint (ไม่ใช่แค่ขนาด/ทิศทางเปลี่ยน) การพยายามใช้ responsive class เดียวมักจบด้วยการประนีประนอมที่ไม่มีฝั่งไหนสวยจริง — render 2 โครงแยกกัน (ใช้ `md:hidden` / `hidden md:contents` คู่กัน) แล้วดึงเนื้อหาการ์ดมาใช้ร่วมผ่านตัวแปร JSX แทน ตรงไปตรงมากว่าและแก้แต่ละฝั่งได้อิสระโดยไม่กระทบกัน
  - รอบถัดมา: ผู้ใช้เห็นว่าการ์ด "บัญชีผู้ใช้" กับ "ขอบเขตที่ดูแล" (แถวล่างบนมือถือ) สูงไม่เท่ากัน (ขอบเขตที่ดูแลสูงกว่ามากเพราะมีชิป 2 กลุ่ม) อยากให้สูงเท่ากัน — ต่างจากบั๊กรอบแรกที่ต้องใส่ `items-start` กันการยืด คราวนี้คือ**อยากให้ยืด** จึงตัด `items-start` ออกจาก grid แถวล่างของชุดมือถือ (เหลือแค่ `grid grid-cols-2 gap-3`) ปล่อย default `align-items: stretch` ทำงาน — ใช้ได้เพราะการ์ด "บัญชีผู้ใช้" ทั้งกลุ่ม (ไอคอน+username+label) อยู่ใน container เดียวที่มี `items-center justify-center` อยู่แล้ว พอถูกยืดสูงขึ้นเนื้อหาทั้งกลุ่มเลยแค่เลื่อนไปกึ่งกลางกล่อง ไม่ได้แหวกช่องว่างประหลาดแบบที่การ์ด "ขอบเขตที่ดูแล" เคยเป็น (อันนั้นไอคอนตรึงบนแยกจากเนื้อหาด้านล่างที่เป็น `flex-1` คนละก้อน) — **บทเรียนเสริม**: จะยืดการ์ดให้เท่ากันได้อย่างปลอดภัย เนื้อหาข้างในต้องเป็นกลุ่มเดียวที่ centered ทั้งกลุ่ม ไม่ใช่แยกเป็นส่วนที่ pin ตำแหน่งตายตัว (เช่น ไอคอนตรึงบน) ผสมกับส่วนที่ centered แยกต่างหาก
  - รอบถัดมาอีก: การ์ด "สถานะบัญชี" แถวบนบนมือถือ (เดี่ยว เต็มความกว้าง) ใช้ `justify-between` อยู่ (ตอนที่เพิ่งแยกออกมาจากการจับคู่กับบัญชีผู้ใช้ คิดว่าไม่มีการ์ดข้างๆ แล้วน่าจะใช้ได้) แต่พอเต็มความกว้างจอจริงๆ กลับดันข้อความ/นาฬิกาไปสุดขอบจนช่องว่างตรงกลางดูแปลกอีก (ปัญหาเดิมจาก CSR ย้อนกลับมาแต่คนละสาเหตุ — รอบนี้เพราะการ์ด "กว้างเกินไป" ไม่ใช่เพราะ "มีการ์ดข้างๆ" มาบีบ) แก้เหมือนกันคือเปลี่ยนเป็น `justify-center gap-6` ให้ทั้งกลุ่มอยู่กึ่งกลางการ์ดแทน — **บทเรียนสรุปรวม**: `justify-between` ในการ์ดที่มีแค่ 2 ก้อนเนื้อหา (ข้อความ + องค์ประกอบตกแต่งอย่างนาฬิกา) มีความเสี่ยงสูงที่จะดูช่องว่างแปลกได้แทบทุกกรณีที่ความกว้างการ์ดไม่คงที่/ไม่เล็กพอดี — `justify-center` + gap คงที่ ปลอดภัยกว่าเป็นค่าเริ่มต้นสำหรับ pattern นี้
- หน้าลูกค้า (`app/welcome`, `app/(authenticated)/customer/*`) ยังไม่แตะ — ตามข้อเสนอเดิมควรใช้ Option B เช่นกันแต่ยังไม่ได้ทำจริง
- Component กลางที่ยังฝังสี/gradient ตรงโค้ด (`Sidebar.tsx`, ฯลฯ) ยังไม่ได้แก้ เพราะกระทบหลายหน้าพร้อมกัน ต้องคุยขอบเขตก่อน
- `AnalogClock.tsx`/`NotificationBell.tsx` ยังไม่ได้แก้ (ใช้ร่วมกับ CSR/Manager เหมือนกัน) — `StatCard.tsx`/`MiniStat.tsx` แก้แล้ว (ดูด้านบน): เปลี่ยน `bg-white`→`bg-card`, ตัด shadow, `rounded-2xl/xl`→`rounded-lg/md`, `font-black`→`font-bold`, label 9-10px→11px ครบ — **ผลข้างเคียงที่ตั้งใจ**: หน้า CSR/WH/Logistics/Manager staff-approvals ที่ยังไม่ได้ทำ Option B เต็มรูปแบบ จะเห็น StatCard/MiniStat ในหน้าเหล่านั้นเปลี่ยนไปแล้วเหมือนกัน (สีธีมเดิมของแต่ละหน้ายังอยู่ เพราะ `iconBg`/`iconText` ยังส่งเป็น prop เหมือนเดิม เปลี่ยนแค่ radius/shadow/font ของตัวการ์ดเอง) — ถือเป็นการเริ่มไล่ปรับ shared component ไปพร้อมกันโดยตั้งใจ ไม่ใช่บั๊ก
- `SkeletonStatCards` และ `SkeletonTopBar` ใน `components/skeletons/DashboardSkeleton.tsx` แก้ตามให้ตรงกับของจริงแล้วทั้งคู่ (กันการ์ด/แถบ "เปลี่ยนรูปทรง" ตอนโหลดเสร็จ) — ส่วนอื่นในไฟล์เดียวกัน (`SkeletonSubTabs`, `SkeletonFilterBar`, `SkeletonTableRows` ฯลฯ) ยังเป็นของเดิม ไม่ได้แตะ เพราะเป็น skeleton ของเนื้อหาที่ยังไม่ได้ทำ Option B
