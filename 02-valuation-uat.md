# การประเมินความพร้อม (Valuation) และแผนทดสอบ UAT
### GPO Xchange Portal — ก่อนขึ้นระบบ Production

**จัดทำเมื่อ:** 30 กรกฎาคม 2569 · **ปรับปรุงล่าสุด (ฉบับละเอียด):** 2 สิงหาคม 2569
**อ้างอิงจาก:** `SYSTEM_ASSESSMENT.md` (ประเมินฉบับแรก 27 ก.ค. + ติดตามผล 29 ก.ค.), `01-ภาพรวมระบบ.md`, Supabase security/performance advisor (ตรวจสดวันที่ปรับปรุงฉบับนี้), live schema 15 ตารางผ่าน Supabase MCP, และโค้ดจริงใน `app/actions/*.ts`, `app/admin/*`, `components/auth/RegisterForm.tsx`

---

## สารบัญ

- [ส่วนที่ 1 — Valuation: ประเมินความพร้อมสู่ Production](#ส่วนที่-1--valuation-ประเมินความพร้อมสู่-production)
  - [1.1 วิธีประเมินและเกณฑ์ให้คะแนน](#11-วิธีประเมินและเกณฑ์ให้คะแนน)
  - [1.2 คะแนนราย Dimension พร้อมหลักฐาน](#12-คะแนนราย-dimension-พร้อมหลักฐาน)
  - [1.3 ความคืบหน้าเทียบกับรอบประเมินก่อนหน้า](#13-ความคืบหน้าเทียบกับรอบประเมินก่อนหน้า)
  - [1.4 Risk Register — ความเสี่ยงคงเหลือ](#14-risk-register--ความเสี่ยงคงเหลือ)
  - [1.5 เงื่อนไขที่ต้องผ่านก่อนเข้าสู่ Production เต็มรูปแบบ (Go-Live Gate)](#15-เงื่อนไขที่ต้องผ่านก่อนเข้าสู่-production-เต็มรูปแบบ-go-live-gate)
- [ส่วนที่ 2 — แผนทดสอบ UAT](#ส่วนที่-2--แผนทดสอบ-uat-user-acceptance-testing)

---

## ส่วนที่ 1 — Valuation: ประเมินความพร้อมสู่ Production

### 1.1 วิธีประเมินและเกณฑ์ให้คะแนน

ให้คะแนนความพร้อม **6 มิติ** (เพิ่มจาก 5 มิติในฉบับก่อน — แยก "ความสมบูรณ์ของฟีเจอร์" ออกจาก "คุณภาพ business logic" เพื่อความชัดเจน) ตามเกณฑ์ 1–5:

| คะแนน | ความหมาย |
|---|---|
| 1 | ยังไม่พร้อม — มีช่องโหว่/บั๊กที่กระทบข้อมูลหรือธุรกรรมจริง |
| 2 | มีความเสี่ยงชัดเจนที่ต้องแก้ก่อนใช้งานแม้ในวงจำกัด |
| 3 | ใช้งานได้ในวงจำกัด แต่มีช่องว่างที่ควรปิดก่อนขยายผล |
| 4 | พร้อมสำหรับ pilot/UAT เต็มรูปแบบ เหลือเฉพาะจุดเสริมความมั่นคง |
| 5 | พร้อมสำหรับ production เต็มรูปแบบ ไม่มีเงื่อนไขค้าง |

ทุกคะแนนอ้างอิงหลักฐานที่ตรวจสอบได้จริง (โค้ด/live schema/advisor) ไม่ใช่การประเมินเชิงความรู้สึก

### 1.2 คะแนนราย Dimension พร้อมหลักฐาน

#### ① ความครบถ้วนของฟีเจอร์ (Feature Completeness) — **4.5/5**

ครอบคลุม 6 บทบาทผู้ใช้ (ลูกค้า, CSR, คลัง, โลจิสติกส์, ผู้จัดการ, ฝ่ายขาย) พร้อม workflow 10 สถานะ, chatbot 2 ตัว, PDF 2 ประเภท, Excel export 2 ชุด, และระบบติดตามสถานะสาธารณะ — เทียบกับ pain point เดิม (งานหาย/ตามงานไม่ได้/ไม่มีหลักฐานย้อนหลัง) ระบบตอบโจทย์ครบทุกข้อ

หักคะแนนเล็กน้อยเพราะ: (1) `app/welcome/dashboard/page.tsx` เป็นหน้าที่ยังสร้างไม่เสร็จและไม่มีจุดใดลิงก์ถึง (ดู `01-ภาพรวมระบบ.md` หัวข้อ 10) (2) สถานะ `out_for_delivery` มีอยู่ใน DB constraint แต่ยังไม่มีโค้ดจุดไหน set สถานะนี้จริง — เจตนาการใช้งานยังไม่ชัดเจน (3) chatbot ลูกค้ายังไม่รองรับการเช็คสถานะคำร้องของตัวเอง (จงใจไม่ทำเพราะ auth-scoping ยังไม่รอบคอบพอ — เป็นการตัดสินใจที่ถูกต้อง ไม่ใช่บั๊ก แต่ก็เป็นฟีเจอร์ที่ยังขาด)

#### ② ความปลอดภัย (Security) — **4.5/5**

ช่องโหว่ระดับวิกฤตทั้ง 5 จุดที่เคยพบ (rate-limit ใช้งานไม่ได้จริงใน 7 จุด, debug code หลุด production, RLS policy ขาดหายใน 9 ตาราง, ระบบ auth สองระบบปนกัน, ไม่มี automated test) **ปิดครบ 100%** และระหว่างการแก้ไขยังพบและปิดช่องโหว่เพิ่มอีก 14 จุดที่ไม่เคยอยู่ใน assessment รอบแรก (ดูรายละเอียดทั้งหมดใน `SYSTEM_ASSESSMENT.md` หัวข้อ 0) รวมถึงช่องโหว่ที่มีผลกระทบจริง เช่น policy บน `b2b_customers` ที่เคยเปิดให้ `anon` อ่านข้อมูลลูกค้าทั้งตารางได้โดยไม่ต้องล็อกอิน

**สถานะ live วันนี้ (ตรวจสดผ่าน `get_advisors`):**
- Security advisor: เหลือ **WARN 1 รายการ** — `auth_leaked_password_protection` (ยังไม่เปิด HaveIBeenPwned check ใน Supabase Auth settings)
- ทั้ง 15 ตารางเปิด RLS ครบ 100% (ยืนยันจาก `list_tables` verbose)
- Sale role ออกแบบขอบเขตข้อมูลจาก session ที่ verify แล้วเท่านั้น ไม่รับ parameter จาก client — ป้องกันการปลอมขอบเขตดูข้อมูลนอกเขต

หักคะแนนเพราะยังเหลือคำเตือน 1 รายการที่ยังไม่ปิด (ใช้เวลาแก้ไม่ถึง 5 นาทีจาก dashboard แต่ยังไม่ได้ทำ) — ไม่ใช่เพราะมีช่องโหว่ใหม่

#### ③ คุณภาพ Business Logic / โครงสร้างโค้ด (Code Quality) — **3.5/5**

Workflow/state machine ออกแบบไว้ชัดเจนสอดคล้องกับ CHECK constraint ระดับฐานข้อมูล และมี pattern "single source of truth" ที่ใช้ซ้ำได้ดี (`lib/manager-stats.ts`, `lib/sale-coverage.ts`, `lib/return-policy.ts`) แต่ยังมีจุดที่ควรปรับ:

- Logic คำนวณสถานะรวมจาก drug items ยัง implement ซ้ำกันคนละที่ระหว่าง `wh-actions.ts` และ `logistics-actions.ts` แทนที่จะรวมศูนย์ที่ `app/repositories/ReturnRepository.ts` ที่ตั้งใจไว้เป็นที่รวม logic (ปัจจุบันมีแค่ helper `sanitizeDate` สั้นๆ)
- Lint error เดิมค้างอยู่ ~200 จุด (ส่วนใหญ่ `@typescript-eslint/no-explicit-any`) ยังไม่ block CI (`continue-on-error: true` พร้อมคอมเมนต์อธิบายเหตุผลไว้ชัดเจนว่าเป็นแผนแก้ทีหลัง ไม่ใช่ถูกละเลย)
- ยังไม่มี E2E test ระดับ UI (มีแค่ unit test ระดับ server action/business logic)

#### ④ ความสามารถในการทดสอบ/บำรุงรักษา (Testability & Maintainability) — **4/5**

มี CI (typecheck → unit test → lint (non-blocking) → build) รันอัตโนมัติทุก push และทุก PR, unit test **54 เคส** ครอบคลุม state machine ของ WH/Logistics/CSR และ chatbot function-calling tools, มี chatbot regression eval แยกต่างหาก (`npm run eval:chatbot`) ที่ออกแบบให้ skip อัตโนมัติเมื่อไม่มี API key (ป้องกันเสีย quota โดยไม่ตั้งใจ) — เป็นแนวทางที่รอบคอบ

Migration history sync กับ production จริงแล้ว (35 ไฟล์ใน `supabase/migrations/`) ทำให้ reproduce schema ได้จากโค้ดโดยไม่ต้องพึ่ง Supabase Studio

หักคะแนนเพราะ: (1) ไม่มี E2E test ระดับ UI เลย (2) `getCSRDashboardData`/`getManagerStatusLogs` ยังดึงข้อมูลทั้งหมดไม่มี pagination — ยังไม่กระทบตอนนี้ (ข้อมูลหลักสิบแถว) แต่จะช้าลงเมื่อข้อมูลโต

#### ⑤ ความพร้อมด้าน Compliance/PDPA — **4/5**

มี `access_logs` (log การเข้าถึงเอกสารทุกครั้ง), `pdpa_consented_at` (timestamp ยินยอมจริง ไม่ใช่แค่ checkbox UI), เอกสารยินยอมพร้อมลายเซ็นดิจิทัล, และ RLS ครบทุกตารางที่เก็บข้อมูลส่วนบุคคล — แนวทางถูกต้องตามเจตนารมณ์ PDPA

หักคะแนนเพราะยังไม่มีนโยบายเก็บรักษา/ลบข้อมูล (data retention policy) ที่เป็นลายลักษณ์อักษรอย่างเป็นทางการ — มีแค่กลไกทางเทคนิคพร้อมใช้ (log ครบ) แต่ยังไม่มีนโยบายกำกับว่าจะเก็บนานเท่าไหร่/ลบเมื่อไหร่

#### ⑥ ความพร้อมด้านโครงสร้างพื้นฐาน/Self-host — **4.5/5**

เลือกใช้ PostgreSQL แทน Redis สำหรับ rate-limit และไม่ผูก vendor lock-in ที่แก้ยาก, ระบบ session เขียนเอง (ไม่พึ่ง `auth.users` ของ Supabase Auth เป็นระบบจริง) ทำให้ self-host ได้ยืดหยุ่นกว่าโปรเจกต์ทั่วไป — รายละเอียดเต็มอยู่ใน `04-self-hosting-101.md`

**แก้ไขแล้วในรอบนี้ (2 ส.ค. 2569):** `next` เดิม pin เป็น `"latest"` (เสี่ยงต่อ build ไม่ reproducible และเคยพบ CVE ระดับ high ในเวอร์ชันที่ resolve มา) → pin เป็น `16.2.9` แล้ว พร้อมอัปเดต `eslint-config-next` ให้ตรงเวอร์ชันตามด้วย (ระหว่างอัปเดตพบว่า `eslint-config-next` 16.x เปลี่ยนไปส่งออก flat config แบบ native ทำให้ `eslint.config.mjs` เดิมพังด้วย circular JSON error — แก้แล้วโดยเปลี่ยนวิธี import config)

หักคะแนนเล็กน้อยเพราะยังไม่มีการทดสอบ self-host จริงบน instance แยกต่างหาก (มีแค่คู่มือ ยังไม่เคยลองทำจริง)

### คะแนนรวมเฉลี่ย: **4.17/5 — สถานะ "พร้อมสำหรับ UAT/Pilot เต็มรูปแบบ ใกล้เกณฑ์ production"**

(ปรับขึ้นจาก 3.9/5 ในฉบับ 30 ก.ค. 2569 — ส่วนใหญ่มาจากการปิด gate เรื่อง Next.js version pin และการยืนยัน staff-chat ใช้งานได้จริง)

---

### 1.3 ความคืบหน้าเทียบกับรอบประเมินก่อนหน้า

| Finding เดิม (จาก SYSTEM_ASSESSMENT.md) | ระดับ | สถานะ |
|---|---|---|
| 5.1 Rate-limit bypass 7 จุด | 🔴 วิกฤต | ✅ ปิดแล้ว |
| 5.2 Debug code หลุด production | 🔴 วิกฤต | ✅ ปิดแล้ว |
| 5.3 ไม่มี RLS policy ใน 9 ตาราง | 🔴 วิกฤต | ✅ ปิดแล้ว |
| 5.4 Auth สองระบบปนกัน + RLS policy อ้างอิง auth.jwt() ที่ตายแล้ว | 🔴 วิกฤต | ✅ ปิดแล้ว |
| 5.5 ไม่มี automated test/CI | 🔴 วิกฤต | ✅ ปิดแล้ว (54 test cases + CI) |
| 5.6 Redirect ผิด/ซ้ำซ้อน | 🟡 ปานกลาง | ✅ ปิดแล้ว |
| 5.7 Logic คำนวณสถานะซ้ำกันคนละที่ | 🟡 ปานกลาง | ⬜ ยังไม่แก้ (มี test คลุมพฤติกรรมแล้ว ปลอดภัยขึ้นสำหรับ refactor ในอนาคต) |
| 5.8 `lib/storage.ts` bucket public ไม่ปลอดภัย | 🟡 ปานกลาง | ✅ ปิดแล้ว (ลบไฟล์ทิ้ง) |
| 5.9 จับคู่สถานะ/เหตุผลด้วย regex ข้อความไทย | 🟡 ปานกลาง | ✅ ปิดแล้ว (lookup table + enum) |
| 5.10 ไม่มี pagination หน้าสถิติ | 🟡 ปานกลาง | ⬜ ยังไม่แก้ (ยังไม่กระทบเพราะข้อมูลน้อย) |
| 5.11 Next.js pin เป็น `"latest"` | 🟡 ปานกลาง | ✅ **ปิดแล้ว 2 ส.ค. 2569** — pin `16.2.9` + sync `eslint-config-next` |
| เนื้อหาซ้ำ `chatbot-knowledge.ts` vs `ReturnPolicySection.tsx` | 🟢 เล็กน้อย | ✅ ปิดแล้ว (รวมเป็น `lib/return-policy.ts`) |
| เนื้อหาซ้ำ `app/guide/page.tsx` vs `customer/manual/page.tsx` | 🟢 เล็กน้อย | ⬜ ยังไม่แก้ |
| `console.log` หลุด browser | 🟢 เล็กน้อย | ✅ ปิดแล้ว |
| `lib/supabase/config.ts` dead code | 🟢 เล็กน้อย | ✅ ปิดแล้ว |
| staff-chat ยังไม่ verify live กับ Gemini | ⬜ ค้างการยืนยัน | ✅ **ยืนยันแล้ว 2 ส.ค. 2569** โดยเจ้าของระบบ — ใช้งานได้จริง |

**สรุป:** จุดวิกฤตทั้งหมด (5.1–5.5) ปิดครบ 100% ตั้งแต่รอบก่อน ฉบับนี้ปิดเพิ่มอีก 2 gate สำคัญ (Next.js version pin + staff-chat live verification) จุดที่เหลือ (5.7, 5.10, เนื้อหาซ้ำ guide/manual) เป็นระดับ "ควรทำ" ทั้งหมด ไม่มีจุดไหนเป็น "ต้องทำก่อนใช้งานจริง" อีกต่อไป

---

### 1.4 Risk Register — ความเสี่ยงคงเหลือ

| ความเสี่ยง | โอกาสเกิด | ผลกระทบ | สถานะ |
|---|---|---|---|
| Leaked-password protection ยังไม่เปิด | ต่ำ (ต้องรอผู้ใช้ตั้งรหัสผ่านที่เคยรั่วมาก่อน) | ต่ำ-กลาง (เฉพาะกรณี Google OAuth ล้มเหลวแล้ว fallback มาใช้รหัสผ่าน — ปัจจุบันลูกค้าใช้ OTP ไม่ใช้รหัสผ่านอยู่แล้ว) | เปิดได้ทันทีจาก dashboard |
| ไม่มี pagination หน้าสถิติ | สูงขึ้นเรื่อยๆ ตามข้อมูลที่เพิ่ม | กลาง (หน้าโหลดช้า ไม่ใช่ข้อมูลผิด) | ยังไม่กระทบที่ปริมาณข้อมูลปัจจุบัน (< 100 แถวต่อตารางหลัก) |
| Logic คำนวณสถานะซ้ำกันคนละที่ (wh/logistics) | กลาง (แก้จุดหนึ่งแล้วลืมอีกจุด) | กลาง (มี test คลุมพฤติกรรมแล้ว ลดความเสี่ยงจาก regression) | มี safety net จาก unit test แล้ว |
| ยังไม่มี data retention policy เป็นลายลักษณ์อักษร | แน่นอน (ยังไม่มีเลย) | กลาง-สูงถ้าถูกตรวจสอบ PDPA | ต้องกำหนดก่อน go-live เต็มรูปแบบ |
| `out_for_delivery` มีใน constraint แต่ไม่มีโค้ดใช้ | ต่ำ | ต่ำ (ไม่ใช่บั๊ก แค่ยังไม่ implement) | ควรยืนยันเจตนากับทีมพัฒนา |
| README.md ยังเป็น boilerplate ไม่ตรงโปรเจกต์ | แน่นอน | ต่ำ (ไม่กระทบระบบ แต่กระทบ onboarding ทีมใหม่) | ควรปรับก่อนมีคนนอกทีมเข้าร่วมพัฒนา |

---

### 1.5 เงื่อนไขที่ต้องผ่านก่อนเข้าสู่ Production เต็มรูปแบบ (Go-Live Gate)

1. เปิด **Leaked Password Protection** ใน Supabase Auth settings (คำเตือนเดียวที่เหลือจาก security advisor)
2. ยืนยันผลการรัน UAT ทุก test case ในส่วนที่ 2 ผ่านทั้งหมด โดยไม่มี defect ระดับ Critical/High ค้างอยู่
3. ~~ทดสอบ staff-chat function calling กับ Gemini API จริงอีกครั้ง~~ **ยืนยันแล้ว 2 สิงหาคม 2569 โดยเจ้าของระบบ — ทดสอบจริงผ่าน ใช้งานได้ปกติ**
4. กำหนดนโยบาย data retention/backup อย่างเป็นทางการ (ดูหัวข้อ 4 ในเอกสาร self-hosting)
5. ~~Pin เวอร์ชัน Next.js ให้ชัดเจนแทน `"latest"`~~ **แก้แล้ว 2 สิงหาคม 2569** — pin เป็น `16.2.9` พร้อมอัปเดต `eslint-config-next` ให้ตรงเวอร์ชันตามแล้ว

**เหลือ 2 จาก 5 ข้อ** ที่ยังไม่ปิด (ข้อ 2 รอผลการรัน UAT จริง, ข้อ 4 รอการกำหนดนโยบายซึ่งเป็นการตัดสินใจเชิงองค์กรไม่ใช่งานเทคนิค)

---

## ส่วนที่ 2 — แผนทดสอบ UAT (User Acceptance Testing)

### 2.1 ขอบเขตและเป้าหมาย

ทดสอบว่าระบบทำงานตรงตามความต้องการทางธุรกิจจริงจากมุมมองผู้ใช้ปลายทางทั้ง **6 บทบาท** (เพิ่มบทบาทฝ่ายขาย/Sale จากฉบับก่อนที่มี 5 บทบาท) ก่อนอนุมัติให้เปิดใช้งานกับข้อมูลลูกค้าจริงในวงกว้าง โดยเน้นการไหลของงานข้ามแผนก (end-to-end) ไม่ใช่แค่ทดสอบฟังก์ชันเดี่ยว

### 2.2 สภาพแวดล้อมทดสอบ

- ใช้ Supabase project แยกต่างหาก (staging) ที่ clone schema จาก production ผ่าน migration files ใน `supabase/migrations/` (35 ไฟล์) ห้ามทดสอบบนฐานข้อมูล production จริง
- ตั้งค่า `RESEND_API_KEY` แบบ sandbox/test mode เพื่อไม่ให้อีเมลจริงถูกส่งออกนอกวงทดสอบ
- เตรียมบัญชีทดสอบครบ **6 บทบาท**: ลูกค้า (2 บัญชี ต่างหน่วยงาน + 1 บัญชีที่ใช้ `customer_code` ร่วมกับอีกบัญชีเพื่อทดสอบ org-history), CSR, WH, Logistics, Manager, **Sale** (ตั้งขอบเขต 1 จังหวัด + 1 ประเภทลูกค้า เพื่อทดสอบการกรอง)

### 2.3 Test Case หลัก แยกตาม Flow

#### A. การสมัครสมาชิกและอนุมัติลูกค้าใหม่

| ID | สถานการณ์ | ผลลัพธ์ที่คาดหวัง | ระดับความสำคัญ |
|---|---|---|---|
| UAT-A1 | ลูกค้าใหม่กรอกฟอร์มสมัคร พร้อมเซ็นชื่อดิจิทัลและยินยอม PDPA | สร้างแถวใน `clients` สถานะ `pending`, บันทึก `pdpa_consented_at` | สูง |
| UAT-A1b | เลือกตำแหน่ง "อื่นๆ (ระบุ)" แล้วไม่กรอกช่อง "ระบุตำแหน่ง" ก่อนกดยืนยัน | ระบบปฏิเสธการบันทึก แจ้ง "กรุณาระบุตำแหน่ง" ที่ช่องระบุเอง | กลาง |
| UAT-A1c | เลือกตำแหน่ง "อื่นๆ (ระบุ)" แล้วกรอกช่องระบุ เช่น "ผู้ช่วยเภสัชกร" | ค่าที่บันทึกใน `clients.position` ต้องเป็นข้อความที่ผู้ใช้พิมพ์เอง ไม่ใช่คำว่า "อื่นๆ" | กลาง |
| UAT-A2 | CSR อนุมัติลูกค้าโดยไม่กรอกรหัสลูกค้า | ระบบปฏิเสธ ต้องกรอกรหัสลูกค้าก่อนอนุมัติเสมอ | สูง |
| UAT-A3 | CSR อนุมัติลูกค้าสำเร็จ | สร้างแถวใน `b2b_customers` พร้อม `customer_code`, สร้าง PDF ยืนยันการลงทะเบียน, ส่งอีเมลแจ้งลูกค้าพร้อมลิงก์ดาวน์โหลด | สูง |
| UAT-A4 | กดปุ่มอนุมัติซ้ำ 2 ครั้งติดกันเร็วๆ (double-submit) หรือเปิด 2 แท็บ | ต้องมีการอนุมัติสำเร็จเพียงครั้งเดียว ไม่เกิด duplicate key error | สูง (เคย regression) |
| UAT-A5 | CSR ปฏิเสธคำขอสมัคร | สถานะ `clients.status` เปลี่ยนเป็น `rejected` ไม่สร้างบัญชี `b2b_customers` | กลาง |
| UAT-A6 | ลูกค้าล็อกอินด้วย Google OAuth ด้วยอีเมลที่ยังไม่เคยอนุมัติ | ระบบปฏิเสธพร้อมข้อความแจ้งให้ลงทะเบียน/ใช้ OTP ก่อน | กลาง |

#### B. การยื่นคำขอคืน/แลกเปลี่ยนสินค้า (ลูกค้า)

| ID | สถานการณ์ | ผลลัพธ์ที่คาดหวัง | ระดับความสำคัญ |
|---|---|---|---|
| UAT-B1 | ลูกค้าล็อกอินด้วย OTP แล้วยื่นคำขอ 1 รายการยา | สร้างแถว `requests` + `drug_items` สถานะ `pending_review`, ได้ `ref_id` รูปแบบ `REF-XXXXXXXX` | สูง |
| UAT-B2 | ยื่นคำขอที่มีรายการยาเกิน 5 รายการ | ระบบปฏิเสธพร้อมข้อความจำกัดสูงสุด 5 รายการ | กลาง |
| UAT-B3 | ยื่นคำขอโดยไม่มีลายเซ็น หรือลายเซ็นไฟล์ใหญ่เกิน 2MB | ระบบปฏิเสธการบันทึก | กลาง |
| UAT-B4 | ยื่นคำขอถี่เกิน 10 ครั้ง/ชั่วโมงจากบัญชีเดียวกัน | ระบบบล็อกพร้อมข้อความ "ส่งคำร้องถี่เกินไป" (ตรวจว่า rate-limit ทำงานจริง) | สูง (เคยเป็นบั๊ก) |
| UAT-B5 | มูลค่ารวมที่แสดงในเอกสารตรงกับผลรวมที่ server คำนวณ ไม่ใช่ค่าที่ client ส่งมา | คำนวณจาก `value_amount` ของแต่ละรายการที่ server เท่านั้น | สูง |
| UAT-B6 | ลูกค้า A (login คนละบัญชีแต่ `customer_code` เดียวกับ B) ยื่นคำขอ แล้วลูกค้า B เข้าดู "ประวัติทั้งหน่วยงาน" | เห็นคำขอของ A ด้วย พร้อมชื่อผู้ยื่น (`showSubmitter`) ส่วนหน้า "ประวัติของฉัน" ของ B ต้องไม่เห็นคำขอของ A | กลาง (แยก scope ถูกต้อง) |

#### C. CSR — ตรวจสอบและอนุมัติคำขอ

| ID | สถานการณ์ | ผลลัพธ์ที่คาดหวัง | ระดับความสำคัญ |
|---|---|---|---|
| UAT-C1 | CSR อนุมัติคำขอทั้งที่ยังมีรายการยาสถานะ `pending_review` ค้างอยู่ | ระบบปฏิเสธ ต้องอนุมัติ/ปฏิเสธรายการยาให้ครบก่อน | สูง |
| UAT-C2 | CSR ปฏิเสธรายการยาโดยไม่เลือกเหตุผล (dropdown) | ระบบปฏิเสธการบันทึก บังคับเลือกเหตุผลเสมอ | สูง |
| UAT-C3 | CSR กรอกฟอร์มแทนลูกค้า (walk-in) | สร้างคำขอโดย `submission_channel = 'csr_manual'` พร้อมบันทึก `created_by_staff_id` | กลาง |
| UAT-C4 | CSR เริ่มกระบวนการแลกเปลี่ยนหลังอนุมัติครบ | สถานะเปลี่ยนเป็น `exchanging` เฉพาะรายการที่ไม่ถูกปฏิเสธ | กลาง |
| UAT-C5 | CSR ค้นหาประวัติคำขอของลูกค้ารายหนึ่ง | แสดงเฉพาะคำขอของลูกค้ารายนั้นจริง ไม่รั่วไปยังลูกค้ารายอื่น | สูง (ตรวจสิทธิ์) |
| UAT-C6 | CSR ใช้ Report Center กรองตามช่วงวันที่ + สถานะ + ประเภทคำขอ แล้ว export Excel | ไฟล์ `.xlsx` มี 3 ชีท (สรุปภาพรวม/รายการใบงาน/รายการยา) ตรงกับตัวกรองที่เลือก | กลาง |
| UAT-C7 | Staff แผนกอื่น (WH/Logistics/Sale) พยายามเรียก endpoint export ของ CSR โดยตรง | ระบบตอบ 403 เพราะ `getManagerOrCsrSession()` จำกัดเฉพาะ manager/CSR | สูง (ตรวจสิทธิ์ endpoint) |

#### D. Logistics — ขนส่ง

| ID | สถานการณ์ | ผลลัพธ์ที่คาดหวัง | ระดับความสำคัญ |
|---|---|---|---|
| UAT-D1 | เปลี่ยนสถานะคำขอจาก `approved` → `in_transit` | บันทึก `status_logs` ทุกรายการยาในคำขอ | กลาง |
| UAT-D2 | ยืนยันรับเข้าคลังแบบ batch โดยบางรายการปฏิเสธ บางรายการผ่าน | สถานะคำขอสรุปเป็น `at_warehouse` ถ้ามีอย่างน้อย 1 รายการผ่าน มิฉะนั้นเป็น `rejected` | สูง (logic ซับซ้อน) |
| UAT-D3 | ปฏิเสธรายการโดยไม่ระบุเหตุผล | ระบบปฏิเสธการบันทึก | สูง |

#### E. Warehouse — รับสินค้าเข้าคลัง

| ID | สถานการณ์ | ผลลัพธ์ที่คาดหวัง | ระดับความสำคัญ |
|---|---|---|---|
| UAT-E1 | ตรวจสภาพสินค้ารายชิ้น (checked-in) ครบทุกรายการในคำขอ | สถานะคำขอเปลี่ยนเป็น `checked_in` อัตโนมัติเมื่อครบ | สูง |
| UAT-E2 | ยืนยันตรวจรับทั้งใบ (batch) ก่อนตรวจครบทุกรายการ | ระบบปฏิเสธ แจ้งว่ายังมีรายการค้าง | กลาง |
| UAT-E3 | จัดเก็บเข้าคลัง (receiving) โดยมีบางรายการถูกปฏิเสธก่อนหน้า | สถานะคำขอคำนวณถูกต้องตามสัดส่วนรายการที่ผ่าน/ไม่ผ่าน | สูง |

#### F. Manager — ภาพรวมและอนุมัติพนักงาน

| ID | สถานการณ์ | ผลลัพธ์ที่คาดหวัง | ระดับความสำคัญ |
|---|---|---|---|
| UAT-F1 | Manager อนุมัติบัญชีพนักงานใหม่ (ทุกแผนกรวม Sale) | พนักงานสามารถล็อกอินได้หลังอนุมัติเท่านั้น (`is_approved = true`) | สูง |
| UAT-F2 | ดูกราฟสรุปเวลาเฉลี่ยแต่ละขั้นตอนและเหตุผลปฏิเสธยอดนิยม | ตัวเลขตรงกับข้อมูลจริงใน `status_logs` | กลาง |
| UAT-F3 | ถามบอทพนักงานว่า "ลูกค้ารายนี้เคยคืนกี่ครั้ง" | บอทตอบด้วยข้อมูลสดที่ query ได้จริง ไม่ error "เชื่อมต่อผู้ช่วย AI ไม่สำเร็จ" — **ยืนยันแล้ว 2 ส.ค. 2569 ว่าใช้งานได้จริง** ควรรวมไว้ใน smoke test รอบสุดท้ายก่อน go-live ด้วย | สูง |
| UAT-F4 | ทบทวนคำถามที่บอทลูกค้าตอบว่า "ไม่แน่ใจ" | แสดงรายการจาก `chatbot_unanswered_questions` ครบถ้วน | ต่ำ |

#### G. ลูกค้า — ติดตามสถานะสาธารณะ

| ID | สถานการณ์ | ผลลัพธ์ที่คาดหวัง | ระดับความสำคัญ |
|---|---|---|---|
| UAT-G1 | ติดตามสถานะด้วย ref_id โดยไม่ต้องล็อกอิน | เห็นเฉพาะข้อมูลที่ปลอดภัย (ไม่เห็นมูลค่าเงิน/เลขที่ invoice/compliance_remark) | สูง (data leak) |
| UAT-G2 | ค้นหา ref_id ที่ไม่มีอยู่จริงซ้ำหลายครั้ง | ถูก throttle เข้มกว่าปกติ (8 ครั้ง/15 นาที) ป้องกัน enumeration | สูง |
| UAT-G3 | กดปุ่ม "เร่งงาน" ซ้ำภายใน 6 ชั่วโมง | ระบบปฏิเสธ (cooldown ต่อคำร้อง) | ต่ำ |

#### H. ฝ่ายขาย (Sale) — บทบาทใหม่ล่าสุด

| ID | สถานการณ์ | ผลลัพธ์ที่คาดหวัง | ระดับความสำคัญ |
|---|---|---|---|
| UAT-H1 | Sale ที่กำหนดขอบเขต "จังหวัดสงขลา + ลูกค้าเอกชน" เข้าดูประวัติลูกค้า | เห็นเฉพาะคำขอของลูกค้าที่ `province='สงขลา'` และ `org_type` อยู่ใน bucket private (โรงพยาบาลเอกชน/คลินิก/ร้านยา) เท่านั้น | สูง (ตรวจ scope) |
| UAT-H2 | Sale คนเดียวกันพยายามเปิดดูรายละเอียดใบงาน (`getSaleRequestDetail`) ของลูกค้านอกเขต โดยรู้ `requestId` ตรงๆ (เช่น เดา/ไล่เลขจาก URL) | ระบบปฏิเสธด้วย "ไม่มีสิทธิ์เข้าถึงข้อมูลใบงานนี้" แม้จะรู้ ID ตรงก็ตาม | สูง (IDOR check) |
| UAT-H3 | Sale ที่ยังไม่ได้กำหนดขอบเขต (จังหวัด/ประเภทลูกค้าว่างเปล่า) เข้าดูประวัติ | ระบบคืนรายการว่างเปล่า ไม่ error และไม่เห็นข้อมูลลูกค้าใดๆ | กลาง |
| UAT-H4 | Sale พยายามเข้าถึงฟังก์ชันอนุมัติ/ปฏิเสธคำขอ (ที่ CSR/WH/Logistics ใช้) | ระบบปฏิเสธ เพราะ Sale เป็น read-only ไม่มีสิทธิ์แก้ไขสถานะใดๆ | สูง (ตรวจสิทธิ์บทบาท) |

### 2.4 เกณฑ์ผ่าน/ไม่ผ่าน (Exit Criteria)

- Test case ระดับ **สูง** ต้องผ่าน 100% ไม่มีข้อยกเว้น
- Test case ระดับ **กลาง** ต้องผ่านอย่างน้อย 90% ส่วนที่เหลือต้องมีแผนแก้ไขพร้อมกำหนดเวลาก่อน go-live
- ไม่มี defect ระดับ Critical (ข้อมูลรั่ว/เข้าถึงข้ามสิทธิ์/สูญหายของธุรกรรม) ค้างอยู่โดยไม่มีแผนแก้
- ผลการทดสอบต้องบันทึกเป็นลายลักษณ์อักษรพร้อมผู้ทดสอบและวันที่ เพื่อใช้เป็นหลักฐานอนุมัติ go-live

### 2.5 บทบาทในการทดสอบ (แนะนำ)

| บทบาท | ผู้รับผิดชอบ |
|---|---|
| ผู้ประสานงาน UAT | เจ้าของระบบ/Product Owner ฝ่าย GPO |
| ผู้ทดสอบตัวแทนลูกค้า | ตัวแทนโรงพยาบาลนำร่อง 1–2 แห่ง (แนะนำให้มีอย่างน้อย 1 แห่งที่มีผู้ใช้ 2 คนใน `customer_code` เดียวกัน เพื่อทดสอบ org-history จริง) |
| ผู้ทดสอบฝั่งพนักงาน | ตัวแทนแต่ละแผนก (CSR, WH, Logistics, **Sale**) อย่างน้อยแผนกละ 1 คน |
| ผู้ตรวจสอบความปลอดภัย/PDPA | ทีมกฎหมาย/compliance ภายในหน่วยงาน |
