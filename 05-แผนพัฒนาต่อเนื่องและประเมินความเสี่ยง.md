# แผนพัฒนาต่อเนื่องและการประเมินความเสี่ยงของระบบ
### GPO Xchange Portal

**จัดทำเมื่อ:** 5 สิงหาคม 2569
**อ้างอิงจาก:** โค้ดจริงใน repository `newgpo-exchange` (branch `RLS_supabase`) ณ วันที่จัดทำ, live schema/RLS policies/function grants/storage buckets/security & performance advisors จริงบน Supabase project `tanagritroj-gpo` (`qgztveswduffskucqppt`, Postgres 17.6), migration history 43 ไฟล์, และเอกสารชุดก่อนหน้า (`01-ภาพรวมระบบ.md`, `02-valuation-uat.md`, `03-roadmap-features-uxui.md`, `04-self-hosting-101.md`, `SYSTEM_ASSESSMENT.md`) — ทุกข้อเสนอในเอกสารนี้ตรวจสอบกับสถานะปัจจุบันจริง ไม่ใช่การคัดลอกจากฉบับก่อนหน้าโดยไม่ตรวจสอบซ้ำ

**สถานะของเอกสาร:** นี่คือรายงาน**ข้อเสนอแนะเท่านั้น** ยังไม่มีการแก้ไขโค้ด/schema/การตั้งค่าใดๆ ในระบบจริงทั้งสิ้น

**หมายเหตุการจัดเก็บเอกสาร:** ไฟล์ `01`–`05` ในโฟลเดอร์นี้เป็นเอกสารประเมินระบบสำหรับใช้งานภายในเท่านั้น ไม่ commit เข้า git ตามข้อตกลงเดิม — ดูหัวข้อ 7.5 สำหรับความเสี่ยงที่เกี่ยวข้องกับข้อตกลงนี้

---

## สารบัญ

1. [ขอบเขตและสิ่งที่เปลี่ยนแปลงตั้งแต่ฉบับก่อน](#1-ขอบเขตและสิ่งที่เปลี่ยนแปลงตั้งแต่ฉบับก่อน)
2. [แนวทางพัฒนาต่อเนื่อง — Function และ Business Logic](#2-แนวทางพัฒนาต่อเนื่อง--function-และ-business-logic)
3. [แนวทางพัฒนาต่อเนื่อง — การจัดการข้อมูล (Data Management)](#3-แนวทางพัฒนาต่อเนื่อง--การจัดการข้อมูล-data-management)
4. [แนวทางพัฒนาต่อเนื่อง — UX/UI ตามมาตรฐานเว็บไซต์ชั้นนำปี 2026](#4-แนวทางพัฒนาต่อเนื่อง--uxui-ตามมาตรฐานเว็บไซต์ชั้นนำปี-2026)
5. [Supabase: มาตรฐานการจัดเก็บข้อมูล ความปลอดภัย และการรองรับ Self-host](#5-supabase-มาตรฐานการจัดเก็บข้อมูล-ความปลอดภัย-และการรองรับ-self-host)
6. [ตารางลำดับความสำคัญ (Roadmap Priority)](#6-ตารางลำดับความสำคัญ-roadmap-priority)
7. [การประเมินความเสี่ยงของระบบและแนวทางการแก้ไข (Risk Assessment)](#7-การประเมินความเสี่ยงของระบบและแนวทางการแก้ไข-risk-assessment)

---

## 1. ขอบเขตและสิ่งที่เปลี่ยนแปลงตั้งแต่ฉบับก่อน

เอกสาร `03-roadmap-features-uxui.md` และ `04-self-hosting-101.md` จัดทำเมื่อ 30 กรกฎาคม 2569 นับจากนั้นมีการเปลี่ยนแปลงเชิงโครงสร้างที่สำคัญหลายจุด (ตรวจสอบจาก `git log` และ migration history จริง) ทำให้ข้อเสนอเดิมบางส่วนล้าสมัยหรือ "ทำเสร็จแล้ว" และมีพื้นที่ใหม่ที่ควรพัฒนาต่อเกิดขึ้น:

| การเปลี่ยนแปลง | Migration/Commit อ้างอิง |
|---|---|
| เพิ่มตาราง `organizations` เป็นเจ้าของข้อมูลระดับหน่วยงาน (1 `hospital_name` = 1 `customer_code`) | `20260804150815`, `20260804151638`, `20260804151648` |
| เพิ่มบทบาท **Sale** พร้อมขอบเขตดูแลตามจังหวัด+ประเภทลูกค้า | `20260801072318`, `20260801072339`, `20260801074056`, `20260801161542` |
| เพิ่มสถานะ `credit_note` ในคำขอ/รายการยา | `20260803154221` |
| เพิ่ม `unit_price` ในตาราง `drug_items` | `20260803165015` |
| ซ่อนข้อมูลติดต่อลูกค้า (ชื่อ/อีเมล) ใน CSR CustomerPicker | commit `bb2b197` |
| Redesign Manager Portal ให้ใช้ pattern เดียวกับ Sale/CSR | commit `dcc5f6c` |
| Restyle ธีมสี Sale/CSR เป็น indigo/mustard/orange | commit `51694cb` |

**สิ่งที่ยังไม่เปลี่ยน (ยืนยันซ้ำจาก live advisor):** RLS ยังเปิดครบทุกตาราง (17 ตารางแล้วหลังเพิ่ม `organizations`), security advisor เหลือคำเตือนเดียวกัน 1 รายการ (leaked password protection) — แสดงว่าฟีเจอร์ใหม่ที่เพิ่มเข้ามาไม่ได้ลดมาตรฐานความปลอดภัยเดิมลง เป็นสัญญาณที่ดี

หัวข้อ 2–5 ด้านล่างคือแนวทางพัฒนาต่อเนื่องที่จัดกลุ่มใหม่ตามคำขอ (function / data management / UX-UI / Supabase-self-host) โดยตัดข้อเสนอที่ทำสำเร็จแล้วออก (เช่น การซ่อน PII ลูกค้าใน CSR) และเพิ่มข้อเสนอใหม่ที่พบจากการตรวจสอบรอบนี้

---

## 2. แนวทางพัฒนาต่อเนื่อง — Function และ Business Logic

### 2.1 ปิดช่องว่างที่พบจากการตรวจสอบ state machine กับโค้ดจริง

- **`credit_note` ยังไม่ถูกบันทึกไว้ใน state diagram ของ `01-ภาพรวมระบบ.md`** — ยืนยันจาก live CHECK constraint ว่า `requests.current_status`/`drug_items.current_status` อนุญาตค่า `credit_note` จริง (เพิ่มโดย migration `20260803154221`) และมีการใช้งานจริงใน `app/actions/csr-actions.ts`, `lib/manager-stats.ts`, `lib/tracking-status.ts` — **แต่ตรวจสอบแล้วว่าสถานะนี้ปรากฏเฉพาะฝั่ง CSR/Manager เท่านั้น ไม่ปรากฏในโค้ด WH/Logistics เลย** ซึ่งสอดคล้องกับการตีความว่าเป็นสถานะเฉพาะของคำขอประเภท "รับคืนลดหนี้" ที่ไม่ต้องเคลื่อนย้ายสินค้าจริงผ่านคลัง/โลจิสติกส์ — **ควรยืนยันการตีความนี้เป็นลายลักษณ์อักษร** (เช่น comment ในฐานข้อมูลหรือ README ของ workflow) เพื่อไม่ให้ผู้พัฒนาถัดไปเข้าใจผิดว่าเป็นบั๊กที่ตกหล่น
- **`out_for_delivery`** ยังคงเป็นค่าที่อนุญาตในฐานข้อมูลแต่ไม่มีจุดใดในโค้ด (WH/Logistics/CSR) ที่ set สถานะนี้จริง (ตรวจซ้ำแล้วในรอบนี้) — ถ้าไม่มีแผนใช้งานในอนาคตอันใกล้ ควรพิจารณาถอดออกจาก CHECK constraint เพื่อลด state ที่ไม่มีความหมาย มิฉะนั้นควรบันทึกเจตนาไว้ในเอกสาร

### 2.2 ต่อยอดจากโครงสร้างที่มีอยู่แล้ว (ทำได้เร็ว เพราะ backend พร้อม)

- **SLA tracking + auto-escalation** — ตาราง `request_pings` และ `status_logs.log_date` มีข้อมูลพอคำนวณ "เวลาที่คำขอค้างในแต่ละสถานะ" ได้แล้ว ควรเพิ่ม scheduled job (ใช้ `pg_cron` ที่ติดตั้งอยู่แล้วสำหรับ `cleanup_expired_rate_limits`) ตรวจคำขอที่ค้างเกิน threshold แล้วแจ้งเตือน manager แทนที่จะรอลูกค้ากดปุ่มเร่งงานเอง
- **In-app notification center** — ยังไม่มีการใช้ Supabase Realtime เลยในโค้ดปัจจุบัน (ตรวจสอบแล้วไม่พบการ subscribe ใดๆ) ทั้งที่ตาราง `status_logs`/`request_pings` พร้อมใช้เป็นแหล่งข้อมูล real-time ได้ทันที
- **Workflow ฝั่งขาย (Sale) ยังเป็น read-only ทั้งหมด** — ปัจจุบัน Sale เห็นได้อย่างเดียว ไม่มีช่องทางสื่อสารกลับ (เช่น comment/note ถึง CSR เกี่ยวกับลูกค้าที่ตนดูแล) อาจพิจารณาเพิ่มฟีเจอร์ "ฝากข้อความถึง CSR" แบบเบา (ไม่ต้องมีสิทธิ์แก้ไขข้อมูลจริง) เพื่อให้บทบาท Sale มีมูลค่าเชิงปฏิบัติการมากขึ้น
- **Barcode/QR scanning ฝั่งคลัง** (ข้อเสนอเดิมจาก 30 ก.ค. ยังไม่ implement — ยังคงมีความสำคัญเพราะ WH dashboard ยังเป็นฟอร์มกรอกมือ)
- **Document versioning** สำหรับ PDF ที่ generate ซ้ำ (ข้อเสนอเดิม ยังไม่ implement — ตรวจสอบแล้วตาราง `document_attachments` ยังไม่มีคอลัมน์ version/is_current)

### 2.3 ฟีเจอร์ที่ควรชะลอไว้ก่อนโดยเจตนา (ยืนยันซ้ำ)

- Chatbot ลูกค้าเช็คสถานะคำร้องได้เอง — ยังไม่ควรทำจนกว่าจะออกแบบ auth-scoping ให้บอทลูกค้ารอบคอบกว่านี้ (บอทลูกค้าปัจจุบันไม่ผูก session ตามเจตนา เพื่อกันข้อมูลรั่ว — การเปิดให้เช็คสถานะจะขัดกับหลักการออกแบบเดิมโดยตรง)

---

## 3. แนวทางพัฒนาต่อเนื่อง — การจัดการข้อมูล (Data Management)

### 3.1 ความสอดคล้องของข้อมูลระดับหน่วยงาน (Data Integrity)

- ตาราง `organizations` (เพิ่มใหม่ 4 ส.ค.) แก้ปัญหาความเสี่ยง "1 หน่วยงานมีหลายรหัสลูกค้า" ได้ดีแล้วด้วย UNIQUE constraint จริงระดับ DB — **ขั้นต่อไปที่ควรทำ** คือเพิ่ม data-quality job ตรวจสอบเป็นระยะว่า `b2b_customers.organization_id` ไม่มีค่า NULL หลงเหลือจากบัญชีที่สร้างก่อนมี migration นี้ (ตรวจสอบตอนนี้มี `b2b_customers` 4 แถว ยังจัดการมือไหว แต่ควรมี query ตรวจสอบอัตโนมัติก่อนข้อมูลโตขึ้น)
- **Foreign key `b2b_customers.organization_id` ยังไม่มี index รองรับ** (ยืนยันจาก live performance advisor รอบนี้ — เป็นจุดใหม่ที่เพิ่มเข้ามาพร้อมตาราง `organizations`) ควรเพิ่ม index ก่อนข้อมูลโต เพราะ query อย่าง `get_org_history`/`get_sale_customer_history` join ผ่านคอลัมน์นี้โดยตรง

### 3.2 มาตรฐานการเก็บข้อมูลมูลค่าทางธุรกิจ

- `drug_items.value_amount`, `drug_items.unit_price` (เพิ่มใหม่), `requests.total_value` เป็น `numeric` แล้ว (ถูกต้อง ไม่ใช้ `float`) — ควรคงมาตรฐานนี้ต่อไปสำหรับคอลัมน์การเงินใดๆ ที่เพิ่มในอนาคต
- ยังไม่มี constraint ระดับ DB ที่บังคับว่า `unit_price × qty` ต้องสอดคล้องกับ `value_amount` (ตรวจสอบจาก schema แล้วไม่พบ CHECK/trigger นี้) — ปัจจุบันคำนวณฝั่ง server ใน `create_exchange_request` RPC (ปลอดภัยเพราะไม่เชื่อ client) แต่ถ้ามีการแก้ไขค่าย้อนหลัง (ผ่านฟีเจอร์ `data_correction_logs` ที่เตรียมไว้) ควรพิจารณา trigger ตรวจความสอดคล้องเพิ่มเติมเพื่อกัน data drift

### 3.3 การจัดการ Audit Trail ระยะยาว

- `status_logs` (16 แถว), `access_logs` (9 แถว) ยังมีขนาดเล็กมาก แต่เป็นตารางที่จะโตเร็วที่สุดเมื่อใช้งานจริง — ควรวางแผน **retention/archiving policy** ล่วงหน้า (เช่น ย้ายข้อมูลเก่ากว่า N ปีไปยัง cold storage) ตั้งแต่ตอนนี้ เพราะข้อมูล PDPA-related มักมีข้อกำหนดเรื่องระยะเวลาเก็บรักษาที่ชัดเจนจากหน่วยงานกำกับดูแล ซึ่งควรออกแบบไว้ก่อนข้อมูลสะสมมาก ไม่ใช่แก้ทีหลัง
- `data_correction_logs` ยังไม่เคยถูกใช้งานจริง (0 แถว) — เป็นโอกาสดีที่จะเขียน **runbook** สั้นๆ ว่า "เมื่อไหร่ที่ควรใช้ฟีเจอร์นี้" ก่อนมี use case แรกเกิดขึ้นจริง เพื่อให้พนักงานใช้สม่ำเสมอตรงกัน ไม่ใช่ต่างคนต่างตีความ

### 3.4 การ Export และรายงาน

- Report Center (CSR) export Excel อยู่แล้ว 3 ชีท — ควรพิจารณาเพิ่ม endpoint export สำหรับ `status_logs`+`access_logs` แยกต่างหาก (ตามที่เสนอไว้ในฉบับก่อน ยังไม่ implement) เพื่อรองรับกรณีหน่วยงานกำกับดูแลภายนอกขอ audit trail แบบเจาะจงช่วงเวลา โดยไม่ต้องให้ทีมพัฒนาช่วย query มือทุกครั้ง

---

## 4. แนวทางพัฒนาต่อเนื่อง — UX/UI ตามมาตรฐานเว็บไซต์ชั้นนำปี 2026

หัวข้อ UX/UI พื้นฐาน (form validation, accessibility, responsive, design system) ได้กล่าวไว้ละเอียดแล้วใน `03-roadmap-features-uxui.md` และยังใช้ได้จริงทั้งหมด (ตรวจสอบซ้ำแล้ว ไม่มีอะไรเปลี่ยนในสถาปัตยกรรมฝั่ง frontend) จึงไม่ทวนซ้ำในเอกสารนี้ — ด้านล่างคือประเด็นที่**ตรวจพบใหม่ในรอบนี้** และมาตรฐานปี 2026 ที่ยังไม่เคยกล่าวถึง:

### 4.1 Dark mode ยังแทบไม่ได้ใช้งานจริง

ตรวจสอบพบว่าโปรเจกต์ติดตั้ง `next-themes` ไว้ แต่**พบการอ้างอิงถึง dark mode class/hook เพียง 1 ไฟล์ในทั้งโปรเจกต์** — แปลว่าฟีเจอร์ที่ติดตั้งไว้แล้วแทบไม่ได้ถูกใช้งานจริงในหน้าส่วนใหญ่ ทั้งที่เป็นความต้องการที่มีเหตุผลชัดเจน (พนักงานคลัง/โลจิสติกส์ทำงานได้ทั้งกลางวัน/กลางคืน) ควรเลือกทางใดทางหนึ่งอย่างจริงจัง: (ก) ลงทุนทำให้ครบทุกหน้าตามมาตรฐาน design token เดียว หรือ (ข) ถอด dependency ออกถ้าตัดสินใจไม่ทำ เพื่อไม่ให้เป็น dead weight ในโปรเจกต์

### 4.2 มาตรฐาน UX ปี 2026 ที่ยังไม่ถูกพิจารณา

- **Core Web Vitals / INP (Interaction to Next Paint)** — มาตรฐานวัดผล UX หลักของปี 2026 (แทนที่ FID เดิม) ยังไม่มีการวัดผลใดๆ ในโปรเจกต์ (ไม่มี Web Vitals reporting, ไม่มี RUM) ควรเพิ่มอย่างน้อย `next/web-vitals` reporting พื้นฐานก่อนเปิดใช้งานจริงกับผู้ใช้จำนวนมาก เพื่อรู้ตัวเลขจริงไม่ใช่คาดเดา
- **Optimistic UI สำหรับ action ที่พนักงานกดบ่อย** — เช่น ปุ่มอนุมัติ/ปฏิเสธของ CSR/WH/Logistics ปัจจุบันต้องรอ round-trip เต็มก่อนเห็นผล (ตรวจสอบจาก pattern การใช้ server action ทั่วไปในโค้ด) การใช้ React 19 `useOptimistic` (มี React 19 อยู่แล้วในโปรเจกต์) จะทำให้ UI รู้สึกเร็วขึ้นมากสำหรับงานที่ทำซ้ำหลายสิบครั้งต่อวัน
- **PWA/Offline-first สำหรับหน้าคลังสินค้า (WH)** — พนักงานคลังทำงานพื้นที่ที่สัญญาณ WiFi อาจไม่เสถียร (โกดัง/คลังสินค้า) การทำ PWA พื้นฐาน (installable + cache ข้อมูลรายการที่ต้องตรวจล่าสุดไว้ใน local) จะลดปัญหา "กดแล้วค้างเพราะเน็ตหลุด" ได้ตรงจุดกว่าการปรับ UI อย่างเดียว — เป็นมาตรฐานที่เว็บแอปสำหรับงานภาคสนาม (field operations) ใช้กันทั่วไปในปี 2026
- **Passwordless/Passkey สำหรับพนักงาน** — ปัจจุบันพนักงานยัง login ด้วย username/password (bcrypt) ล้วน ขณะที่ลูกค้าใช้ OTP/Google OAuth อยู่แล้ว มาตรฐานปี 2026 นิยมเสริม WebAuthn/Passkey ให้พนักงานที่ login บ่อยทุกวันแทน password เพื่อทั้งความปลอดภัยและความเร็วในการเข้าใช้งาน (เป็นตัวเลือกเสริม ไม่จำเป็นต้องแทนที่ระบบเดิมทั้งหมด)
- **Skeleton loading ที่สะท้อนโครงจริงของเนื้อหา** — ตรวจสอบว่าปัจจุบันหลายจุดใช้ spinner กลมทั่วไปแทน skeleton ที่สะท้อนโครง layout จริง (เช่น ตาราง dashboard) — มาตรฐานปี 2026 นิยม skeleton ที่ตรงรูปทรงเนื้อหาจริงเพื่อลด layout shift และให้ความรู้สึก perceived performance ที่ดีกว่า

---

## 5. Supabase: มาตรฐานการจัดเก็บข้อมูล ความปลอดภัย และการรองรับ Self-host

รายละเอียดขั้นตอน self-host แบบครบถ้วนอยู่ใน `04-self-hosting-101.md` แล้วและยังใช้ได้จริงทั้งหมด (สถาปัตยกรรม session แบบเขียนเองไม่ผูก Supabase Auth ไม่เปลี่ยน) — หัวข้อนี้เพิ่มเฉพาะสิ่งที่ตรวจพบใหม่ในรอบนี้ที่ยังไม่เคยบันทึกไว้:

### 5.1 Storage buckets ไม่มีการจำกัด size/mime-type ที่ระดับ Supabase

ตรวจสอบ live `storage.buckets` ทั้ง 4 bucket (`return-documents`, `registration-documents`, `signatures`, `FM-AJJ0-008-form`) พบว่า**ทุก bucket ตั้งเป็น private ถูกต้อง แต่ไม่มี bucket ใดตั้งค่า `file_size_limit` หรือ `allowed_mime_types` ไว้เลย** (ทั้งสองคอลัมน์เป็น `null` ทุก bucket) — ปัจจุบันระบบจำกัดขนาดไฟล์ลายเซ็น 2MB ที่ชั้นแอปพลิเคชันเท่านั้น ตรงตามหลัก defense-in-depth ที่โปรเจกต์นี้ยึดถือ (เช่นเดียวกับ RLS) **ควรตั้งค่า `file_size_limit`/`allowed_mime_types` ที่ตัว bucket เป็นเกราะชั้นที่สอง** ไม่ต่างจากที่ทำกับ RLS ของตาราง เพื่อป้องกันกรณีโค้ดฝั่งแอปมีบั๊กหรือถูก bypass ในอนาคต

### 5.2 `storage.objects` ไม่มี RLS policy ใดๆ เลย (ไม่ใช่แค่ deny-all แบบตาราง public)

ตรวจสอบ `pg_policies` ของ `storage.objects` พบว่า**ไม่มี policy อยู่เลยแม้แต่ policy เดียว** ต่างจากตารางใน `public` schema ทุกตารางที่มี policy `deny_client_access` (`qual: false`) ระบุไว้อย่างชัดเจน — ผลลัพธ์ในทางปฏิบัติเหมือนกัน (RLS เปิดอยู่โดย default ของ Supabase Storage แปลว่าไม่มี policy = ปฏิเสธทุกคำขอจาก `anon`/`authenticated` โดยปริยาย) **แต่เป็น "การปฏิเสธโดยไม่มีอะไรระบุ" ไม่ใช่ "การปฏิเสธที่ตั้งใจเขียนไว้ชัดเจน"** ต่างจากแนวทางที่เหลือทั้งระบบ แนะนำเพิ่ม explicit deny-all policy บน `storage.objects` ให้สอดคล้องกับรูปแบบเดียวกับตาราง `public` ทั้งหมด เพื่อไม่ให้ขึ้นอยู่กับพฤติกรรม default ของแพลตฟอร์มเพียงอย่างเดียว (สำคัญมากขึ้นไปอีกถ้าวันหนึ่งย้ายไป self-host ที่พฤติกรรม default อาจต่างจาก Supabase Cloud)

### 5.3 Extension ที่ติดตั้งจริงบน Postgres (verify ล่าสุด)

ยืนยันจาก `list_extensions` สด: `pgcrypto`, `uuid-ossp`, `pg_stat_statements`, `pg_cron`, `supabase_vault`, `plpgsql` — **ไม่มีการเปลี่ยนแปลงจากฉบับก่อน** สอดคล้องกับคำแนะนำเดิมใน `04-self-hosting-101.md` ว่า self-host image ต้องรองรับ extension ชุดนี้ครบ ไม่มี extension ใหม่ที่ผูกกับ Supabase Cloud โดยเฉพาะ (เช่น `pgvector`, `pg_graphql`) ที่จะเป็นอุปสรรคเพิ่มเติมต่อการ self-host — เป็นสัญญาณที่ดีว่าทีมยังคุมขอบเขต dependency ได้ดีต่อเนื่อง

### 5.4 Function grants ยังคุมเข้มครบทุกตัว (verify ล่าสุด)

ตรวจสอบ `pg_proc`/`has_function_privilege` ของทุกฟังก์ชันใน schema `public` (12 ฟังก์ชัน รวม 2 ฟังก์ชันใหม่ `get_org_history`, `get_sale_customer_history`) พบว่า**ทุกฟังก์ชัน `SECURITY DEFINER` ถูก revoke สิทธิ์ `EXECUTE` จาก `anon`/`authenticated` ครบ 100%** — มาตรฐานนี้ถูกรักษาไว้ต่อเนื่องแม้เพิ่มฟังก์ชันใหม่ 2 ตัวล่าสุด เป็นหลักฐานว่า checklist ในหัวข้อ 4.2 ของ `04-self-hosting-101.md` ถูกนำไปใช้จริง ไม่ใช่แค่เขียนไว้เฉยๆ

### 5.5 ไม่มี Observability/APM layer

ตรวจสอบ `package.json` และโค้ดทั้งหมดแล้ว**ไม่พบเครื่องมือ error tracking/APM ใดๆ** (ไม่มี Sentry, ไม่มี OpenTelemetry, ไม่มี Vercel Analytics) การมองเห็นปัญหาการทำงานจริงพึ่งพา `pg_stat_statements` ที่ระดับฐานข้อมูลเท่านั้น ไม่มีการมองเห็นระดับ application layer (Next.js server actions, API route) เลย — สำคัญมากขึ้นเมื่อระบบเข้าใกล้ production/pilot จริงตามที่ระบุใน `02-valuation-uat.md` ควรเพิ่มก่อนขยายผู้ใช้จริง ไม่ใช่หลังพบปัญหาแล้ว (รายละเอียดเพิ่มเติมในหัวข้อ 7)

---

## 6. ตารางลำดับความสำคัญ (Roadmap Priority)

| ลำดับ | รายการ | เหตุผล |
|---|---|---|
| **เร่งด่วน (ก่อน pilot ขยายผู้ใช้จริง)** | เปิด leaked-password protection, แก้ `.env.example` ให้ตรงกับตัวแปรจริง (ดู 7.4), เพิ่ม `file_size_limit`/`allowed_mime_types` ที่ bucket, เพิ่ม explicit deny policy บน `storage.objects`, เพิ่ม APM/error tracking พื้นฐาน | ทำได้เร็ว ความเสี่ยงต่ำแต่กระทบมากถ้าละเลยตอนขยายผู้ใช้ |
| **ระยะสั้น (1–2 เดือน)** | เพิ่ม index บน `b2b_customers.organization_id`, SLA tracking + notification center (Realtime), บันทึกเจตนา `credit_note`/`out_for_delivery` ในเอกสาร, วางแผน retention policy สำหรับ audit log | ต่อยอดจากโครงสร้างที่มีอยู่แล้ว ป้องกันปัญหาก่อนข้อมูลโต |
| **ระยะกลาง (3–6 เดือน)** | Barcode scanning ฝั่งคลัง, PWA/offline-first สำหรับ WH, document versioning, Web Vitals reporting, dark mode ให้ครบหรือถอดออก | ยกระดับประสิทธิภาพหน้างานจริงและวัดผล UX เชิงตัวเลข |
| **ระยะยาว (6 เดือนขึ้นไป)** | Passkey สำหรับพนักงาน, Integration ERP โรงพยาบาล, SSO, forecast เชิงลึก | ต้องมีปริมาณผู้ใช้/ข้อมูลมากพอจะคุ้มค่าการลงทุน |

---

## 7. การประเมินความเสี่ยงของระบบและแนวทางการแก้ไข (Risk Assessment)

**หลักเกณฑ์ระดับความเสี่ยง:** ประเมินจาก (ก) โอกาสเกิด และ (ข) ผลกระทบหากเกิดขึ้นจริง — ไม่มีข้อใดในรายการนี้เป็นช่องโหว่ที่ถูกโจมตีอยู่ตอนนี้ เป็นการประเมินเชิงป้องกันล่วงหน้าทั้งหมด

| # | ความเสี่ยง | ระดับ | หลักฐาน | แนวทางแก้ไขที่แนะนำ |
|---|---|---|---|---|
| 7.1 | `.env.example` ระบุชื่อตัวแปรผิดและไม่ครบ | **กลาง** | โค้ดจริงอ่าน `NEXT_PUBLIC_SUPABASE_ANON_KEY` แต่ `.env.example` เขียนไว้เป็น `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` (ชื่อไม่ตรงกัน) และไม่มี `GEMINI_API_KEY`/`RESEND_API_KEY`/`SUPABASE_SERVICE_ROLE_KEY`/`OTP_PEPPER` เลยทั้งที่โค้ดใช้จริงทั้ง 4 ตัว (verify ด้วย grep `process.env.*` ทั่วโปรเจกต์) | แก้ `.env.example` ให้ตรงชื่อตัวแปรจริงและครบทุกตัว (ไม่ต้องใส่ค่าจริง ใส่ placeholder พอ) — ความเสี่ยงคือถ้ามีผู้พัฒนาใหม่ตั้งค่าตาม `.env.example` ตรงตัวจะเชื่อมต่อ Supabase ไม่ได้และไม่รู้สาเหตุ |
| 7.2 | Storage bucket ไม่มี `file_size_limit`/`allowed_mime_types` ระดับ Supabase | **ต่ำ-กลาง** | ยืนยันจาก live `storage.buckets`: ทั้ง 4 bucket มีค่า `null` ทั้งสองคอลัมน์ พึ่งพา validation ฝั่งแอปอย่างเดียว | ตั้งค่า `file_size_limit`/`allowed_mime_types` ที่ bucket โดยตรงเป็นเกราะชั้นที่สอง สอดคล้องกับหลัก defense-in-depth ที่ใช้กับ RLS อยู่แล้ว |
| 7.3 | `storage.objects` ไม่มี policy ระบุไว้อย่างชัดเจน (implicit deny) | **ต่ำ** | ยืนยันจาก `pg_policies` ว่า `storage.objects` มี 0 แถว ต่างจากทุกตาราง `public` ที่มี policy `deny_client_access` ชัดเจน | เพิ่ม explicit deny-all policy บน `storage.objects` ให้ตรงรูปแบบเดียวกับตารางอื่น ลดการพึ่งพาพฤติกรรม default ของแพลตฟอร์มเพียงอย่างเดียว โดยเฉพาะสำคัญก่อนย้าย self-host |
| 7.4 | ไม่มี index รองรับ FK ใหม่ `b2b_customers.organization_id` | **ต่ำ (ตอนนี้) → เพิ่มขึ้นเมื่อข้อมูลโต** | ยืนยันจาก live performance advisor — เป็นจุดใหม่ที่เกิดจากตาราง `organizations` (4 ส.ค.) ที่ยังไม่มีในฉบับก่อน รวมทั้งหมดตอนนี้มี unindexed FK 5 จุด (ฉบับก่อนบันทึกไว้ 3 จุด — ข้อมูลใน `01-ภาพรวมระบบ.md` คลาดเคลื่อนจากของจริงแล้ว) | เพิ่ม index ให้ FK ทั้ง 5 จุดก่อนข้อมูลโต โดยเฉพาะ `b2b_customers.organization_id` ที่ RPC ใหม่ (`get_org_history`, `get_sale_customer_history`) join ผ่านโดยตรง |
| 7.5 | เอกสารประเมินระบบ (นี่รวมถึงไฟล์นี้) ไม่ commit เข้า git ตามข้อตกลง แต่เป็นแหล่งความรู้ operational เพียงแหล่งเดียวในหลายเรื่อง | **กลาง** | ตรวจสอบว่า README.md ยัง 100% เป็น boilerplate Vercel/Supabase starter (ไม่มีคำอธิบายเฉพาะโปรเจกต์เลย) ขณะที่ความรู้จริง (workflow, ตัวแปรที่ต้องตั้ง, สถาปัตยกรรม auth แบบเขียนเอง) อยู่ในเอกสาร `01`-`05` ที่ตั้งใจไม่ commit | แยกเนื้อหาเป็น 2 ชั้น: (1) ข้อมูล "ต้องมี" สำหรับ onboarding ผู้พัฒนาใหม่ (env vars ที่ต้องตั้ง, ขั้นตอน local dev, สถาปัตยกรรมโดยย่อ) ควร commit ไว้ใน README.md จริง ไม่ใช่พึ่งเอกสารประเมินที่ตั้งใจเก็บไว้ local-only เท่านั้น ส่วน (2) เนื้อหาเชิงกลยุทธ์/ประเมินผล (เอกสารชุดนี้) เก็บ local-only ต่อไปตามเดิม — ถ้าเครื่องนี้เสียหายหรือเปลี่ยนผู้พัฒนา ความรู้ operational พื้นฐานจะหายไปทั้งหมดถ้าไม่แยกชั้นนี้ |
| 7.6 | ไม่มี Observability/APM ระดับ application | **กลาง (เพิ่มขึ้นเมื่อเข้าสู่ production/pilot)** | ตรวจสอบ `package.json` และโค้ดทั้งหมด ไม่พบ Sentry/OpenTelemetry/RUM ใดๆ มีแค่ `pg_stat_statements` ระดับฐานข้อมูล | เพิ่ม error tracking พื้นฐาน (เช่น Sentry self-hostable ได้ สอดคล้องกับแนวทาง self-host ของโปรเจกต์) ก่อนขยาย pilot ตาม `02-valuation-uat.md` เพราะปัจจุบันถ้าเกิด error ฝั่ง server action จะไม่มีใครรู้จนกว่าผู้ใช้แจ้งเอง |
| 7.7 | เอกสารภาพรวมระบบ (`01`) ไม่ตรงกับ live schema ในบางจุด (documentation drift) | **ต่ำ** | ยืนยันแล้วว่า state diagram ในหัวข้อ 4 ของ `01-ภาพรวมระบบ.md` ไม่มี `credit_note` ทั้งที่ live CHECK constraint มีค่านี้จริงตั้งแต่ 3 ส.ค., ตัวเลข unindexed FK/unused index ก็คลาดเคลื่อนจากของจริงแล้วเช่นกัน (ดู 7.4) | ไม่ต้องแก้เร่งด่วน แต่ควรมีขั้นตอน "sync เอกสารกับ live advisor" เป็นส่วนหนึ่งของ checklist ก่อน release แต่ละรอบ ไม่ใช่ทำเป็นครั้งคราวเมื่อมีคนขอรายงานใหม่ |
| 7.8 | ~200 lint error (`no-explicit-any`) ค้างอยู่ ไม่ block CI | **ต่ำ แต่ควรจัดลำดับใหม่** | ยืนยันจาก CI config และเอกสารก่อนหน้า — ยังไม่ลดลงจากฉบับก่อน | แทนที่จะแก้ตามลำดับไฟล์ทั่วไป แนะนำจัดลำดับความสำคัญตามไฟล์ที่แตะข้อมูลการเงิน (`total_value`, `unit_price`, `value_amount`) และข้อมูลส่วนบุคคล (PDPA-related) ก่อน เพราะ `any` ในจุดเหล่านี้ตัดความสามารถของ TypeScript ในการจับผิดตรงจุดที่มีผลกระทบสูงสุดถ้าเกิด bug |
| 7.9 | ไม่มี security headers (CSP/HSTS/X-Frame-Options) กำหนดไว้ใน `next.config.ts` | **ต่ำ** | ตรวจสอบ `next.config.ts` แล้วไม่มีการตั้งค่า `headers()` ใดๆ พึ่งพา default ของ Next.js/hosting platform เท่านั้น | เพิ่ม security headers ชุดพื้นฐาน (CSP อย่างน้อยจำกัด `frame-ancestors`, HSTS) ผ่าน `headers()` ใน `next.config.ts` — สำคัญขึ้นถ้าย้ายไป self-host ที่ไม่มี platform default ของ Vercel มาช่วยแล้ว |
| 7.10 | Business continuity: พัฒนาโดยทีม/ผู้พัฒนาขนาดเล็กมาก | **กลาง (เชิงองค์กร ไม่ใช่เชิงเทคนิค)** | ปริมาณ commit และรูปแบบการพัฒนาบ่งชี้ทีมขนาดเล็ก (ยืนยันจากเอกสารก่อนหน้า) | ไม่ใช่สิ่งที่แก้ด้วยโค้ด แต่ควรพิจารณาควบคู่กับ 7.5 (documentation) และ 7.6 (observability) เพื่อลดการพึ่งพาความรู้เฉพาะบุคคล (bus factor) ก่อนระบบเข้าสู่การใช้งานจริงทั้งองค์กร |

### สรุปหัวข้อความเสี่ยง

ไม่มีความเสี่ยงข้อใดในรอบนี้อยู่ในระดับ **สูง/วิกฤต** — ระบบยังคงรักษามาตรฐานความปลอดภัยหลัก (RLS, function grants, defense-in-depth) ไว้ได้ดีต่อเนื่องแม้เพิ่มฟีเจอร์ใหม่จำนวนมากในรอบนี้ (ตาราง `organizations`, บทบาท Sale, สถานะ `credit_note`) ความเสี่ยงที่พบส่วนใหญ่อยู่ในหมวด **operational readiness** (observability, documentation, storage hardening) มากกว่าช่องโหว่เชิงโค้ด ซึ่งสอดคล้องกับข้อสรุปของ `02-valuation-uat.md` ว่าระบบพร้อมสำหรับ UAT/pilot — แต่ควรปิดรายการ "เร่งด่วน" ในหัวข้อ 6 ก่อนขยายผู้ใช้จริงในวงกว้าง
